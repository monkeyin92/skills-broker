---
phase: 01-prove-the-qa-hero-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - README.zh-CN.md
  - TODOS.md
  - STATUS.md
  - src/hosts/skill-markdown.ts
  - tests/hosts/host-shell-install.test.ts
  - tests/e2e/shared-home-smoke.test.ts
  - tests/e2e/claude-code-smoke.test.ts
autonomous: true
requirements:
  - ENTRY-01
  - ENTRY-02
must_haves:
  truths:
    - "README、README.zh-CN 与已安装宿主说明都把 website QA 放在第一条 hero path。"
    - "Quick Start 明确给出 INSTALL_REQUIRED -> install -> rerun -> doctor 的固定顺序。"
    - "requirements analysis / investigation 仍然可见，但被明确降到 secondary lanes。"
  artifacts:
    - path: "README.md"
      provides: "英文 QA-first Quick Start 与 operator wording"
    - path: "README.zh-CN.md"
      provides: "中文 QA-first Quick Start 与 operator wording"
    - path: "src/hosts/skill-markdown.ts"
      provides: "宿主壳 hero / secondary / clarify 文案生成点"
    - path: "tests/hosts/host-shell-install.test.ts"
      provides: "已安装宿主壳顺序与 contract 断言"
  key_links:
    - from: "README.md"
      to: "README.zh-CN.md"
      via: "双语 Quick Start 使用相同 QA-first 闭环文案"
      pattern: "website QA|INSTALL_REQUIRED|doctor"
    - from: "src/hosts/skill-markdown.ts"
      to: "tests/hosts/host-shell-install.test.ts"
      via: "hero lane 顺序与 decline contract 断言"
      pattern: "### Hero lane: website QA"
---

<objective>
锁定 website QA 的 first-move 入口文案，让 README、README.zh-CN 与安装后的宿主壳都先教同一条 QA hero loop。

Purpose: 满足 ENTRY-01 / ENTRY-02，并按 D-04、D-05 把 QA 维持为唯一默认入口，不把 secondary lanes 平铺成并列首页。
Output: 双语 docs、repo truth 镜像、宿主壳生成文案，以及覆盖这些表面的 smoke / order proof。
</objective>

<execution_context>
@/Users/monkeyin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/monkeyin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-prove-the-qa-hero-loop/01-CONTEXT.md
@.planning/phases/01-prove-the-qa-hero-loop/01-PATTERNS.md
@README.md
@README.zh-CN.md
@TODOS.md
@STATUS.md
@src/hosts/skill-markdown.ts
@tests/hosts/host-shell-install.test.ts
@tests/e2e/shared-home-smoke.test.ts
@tests/e2e/claude-code-smoke.test.ts
@docs/superpowers/plans/2026-04-16-website-qa-default-entry-implementation.md
@docs/superpowers/plans/2026-04-17-semantic-coarse-boundary-hardening-implementation.md

<interfaces>
From src/hosts/skill-markdown.ts:
```ts
type HostShellSkillMarkdownOptions = {
  host: BrokerHost;
  invocationMode: "auto" | "explicit";
  runnerCommand: string;
  maintainedBoundaryExamples?: readonly string[];
};

export function buildHostShellSkillMarkdown(
  options: HostShellSkillMarkdownOptions
): string;
```

From tests/hosts/host-shell-install.test.ts:
```ts
function expectInOrder(text: string, snippets: readonly string[]): void;
function expectHostSkillLayout(
  skill: string,
  host: "claude-code" | "codex",
  maintainedBoundaryExamples: readonly string[]
): void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 对齐 QA-first 文档真相</name>
  <files>README.md, README.zh-CN.md, TODOS.md, STATUS.md</files>
  <read_first>
.planning/PROJECT.md
.planning/REQUIREMENTS.md
.planning/ROADMAP.md
.planning/STATE.md
.planning/phases/01-prove-the-qa-hero-loop/01-CONTEXT.md
README.md
README.zh-CN.md
TODOS.md
STATUS.md
docs/superpowers/plans/2026-04-16-website-qa-default-entry-implementation.md
docs/superpowers/plans/2026-04-17-semantic-coarse-boundary-hardening-implementation.md
  </read_first>
  <action>按 D-04、D-05 重写并同步 README.md 与 README.zh-CN.md 的首屏和 Quick Start，使 website QA 成为唯一 first move，且编号顺序固定为 INSTALL_REQUIRED -> install/approve -> rerun same request -> `npx skills-broker doctor`。保留 requirements analysis / investigation 作为 secondary lanes，并在 README 双语中使用明确句子说明“它们仍然支持，但不该先试”。如果 operator-facing wording 被改动，同步更新 TODOS.md 与 STATUS.md 中对应 QA default-entry / doctor truth 叙述，避免多表面 truth 漂移；不要把 web markdown、OpenCode 或 Phase 2 family-proof 泛化提升成与 QA 并列的首屏入口。</action>
  <acceptance_criteria>
- `rg -n "If you only try one published path, make it this one" README.md` 返回 QA-first hero 文案。
- `rg -n "^### 2\\. Try the website QA install-required -> verify -> reuse loop$" README.md` 与 `rg -n "^### 2\\. 先跑一遍 website QA 的 install-required -> verify -> reuse 闭环$" README.zh-CN.md` 都有命中。
- `rg -n "not the first thing this README should make you try" README.md` 与 `rg -n "不该和 QA 一起抢 README 里的第一步" README.zh-CN.md` 都有命中。
- `rg -n "website QA default-entry|doctor truth|QA default-entry" TODOS.md STATUS.md` 能找到与 README 同步的 QA default-entry / doctor wording。
  </acceptance_criteria>
  <verify>
    <automated>rg -n "If you only try one published path|Try the website QA install-required -> verify -> reuse loop|先跑一遍 website QA 的 install-required -> verify -> reuse 闭环|not the first thing this README should make you try|不该和 QA 一起抢 README 里的第一步|doctor truth|QA default-entry" README.md README.zh-CN.md TODOS.md STATUS.md</automated>
  </verify>
  <done>双语 README、TODOS、STATUS 对外都把 website QA 讲成 Phase 1 的唯一 first move，并明确 Quick Start 顺序与 secondary lanes 边界。</done>
</task>

<task type="auto">
  <name>Task 2: 锁住宿主壳 QA hero 排序与 decline contract</name>
  <files>src/hosts/skill-markdown.ts, tests/hosts/host-shell-install.test.ts, tests/e2e/shared-home-smoke.test.ts, tests/e2e/claude-code-smoke.test.ts</files>
  <read_first>
.planning/phases/01-prove-the-qa-hero-loop/01-CONTEXT.md
.planning/phases/01-prove-the-qa-hero-loop/01-PATTERNS.md
src/hosts/skill-markdown.ts
tests/hosts/host-shell-install.test.ts
tests/e2e/shared-home-smoke.test.ts
tests/e2e/claude-code-smoke.test.ts
config/maintained-broker-first-families.json
  </read_first>
  <action>按 D-04、D-05 与 D-07 维护 `src/hosts/skill-markdown.ts` 作为唯一生成点：继续把 `website QA` hero examples 放在 `### Hero lane: website QA` 下，并让 secondary maintained lanes 与 clarify examples 分段出现；保持 decline contract 明确要求在 `INSTALL_REQUIRED` 后“verify, then rerun the same request”。更新 host-shell install 与 e2e smoke 断言，要求 QA hero lane 顺序、`clarify_before_broker` 例子、`INSTALL_REQUIRED` / `HANDOFF_READY` 指引都在生成后的宿主壳里稳定存在。不要手改安装产物，不要让 host 在入口层决定具体 package、skill 或 MCP 赢家。</action>
  <acceptance_criteria>
- `rg -n "### Hero lane: website QA|### Secondary maintained lanes|## Clarify Before Broker \\(`clarify_before_broker`\\)" src/hosts/skill-markdown.ts tests/hosts/host-shell-install.test.ts` 都有命中。
- `rg -n "INSTALL_REQUIRED.*rerun the same request|HANDOFF_READY" src/hosts/skill-markdown.ts tests/hosts/host-shell-install.test.ts tests/e2e/shared-home-smoke.test.ts tests/e2e/claude-code-smoke.test.ts` 都有命中。
- `npm test -- --run tests/hosts/host-shell-install.test.ts tests/e2e/claude-code-smoke.test.ts tests/e2e/shared-home-smoke.test.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/hosts/host-shell-install.test.ts tests/e2e/claude-code-smoke.test.ts tests/e2e/shared-home-smoke.test.ts</automated>
  </verify>
  <done>Claude Code 与 Codex 的已安装宿主壳都把 QA hero lane 放在最前，并保留 coarse boundary、clarify、INSTALL_REQUIRED rerun contract 的 smoke proof。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host shell -> broker runtime | 未经 broker 选择的宿主入口文案可能诱导宿主越权决定具体能力赢家 |
| repo docs -> operator | 多文档 wording 漂移会让 operator 对默认入口和 doctor 路径形成错误预期 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | E | `src/hosts/skill-markdown.ts` | mitigate | 用 QA hero / secondary / clarify 分段与 smoke 顺序断言锁住宿主只能做 coarse boundary，避免越权选择具体 skill/package。 |
| T-01-01-02 | T | `README.md`, `README.zh-CN.md`, `TODOS.md`, `STATUS.md` | mitigate | 同步更新 operator-facing wording，并用 grep/测试校验 QA first move、INSTALL_REQUIRED rerun、doctor path 在多表面一致。 |
</threat_model>

<verification>
1. 运行 `rg -n "If you only try one published path|Try the website QA install-required -> verify -> reuse loop|先跑一遍 website QA 的 install-required -> verify -> reuse 闭环|not the first thing this README should make you try|不该和 QA 一起抢 README 里的第一步" README.md README.zh-CN.md TODOS.md STATUS.md`
2. 运行 `npm test -- --run tests/hosts/host-shell-install.test.ts tests/e2e/claude-code-smoke.test.ts tests/e2e/shared-home-smoke.test.ts`
</verification>

<success_criteria>
- README、README.zh-CN 与宿主壳都把 website QA 作为唯一 first move。
- 文档明确展示 INSTALL_REQUIRED -> install -> rerun -> doctor 顺序。
- secondary lanes 仍然存在，但不会在首屏与 QA 并列抢优先级。
</success_criteria>

<output>
After completion, create `.planning/phases/01-prove-the-qa-hero-loop/01-prove-the-qa-hero-loop-01-SUMMARY.md`
</output>
