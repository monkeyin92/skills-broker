---
phase: 01-prove-the-qa-hero-loop
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared-home/doctor.ts
  - src/shared-home/format.ts
  - src/bin/skills-broker.ts
  - tests/shared-home/doctor.test.ts
  - tests/cli/lifecycle-cli.test.ts
  - tests/integration/broker-flow.test.ts
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-03
must_haves:
  truths:
    - "同一句 website QA 请求在未安装赢家时返回 INSTALL_REQUIRED。"
    - "安装后重跑同一句 website QA 请求返回 HANDOFF_READY。"
    - "第二个宿主重跑同一句 website QA 请求会复用同一条 QA lane。"
    - "doctor 用 blocked / in_progress / proven 直说 QA loop 当前是否可信。"
  artifacts:
    - path: "src/shared-home/doctor.ts"
      provides: "QA loop verdict aggregation and strict proof evaluation"
    - path: "src/shared-home/format.ts"
      provides: "verdict-first doctor text/json output"
    - path: "src/bin/skills-broker.ts"
      provides: "doctor --strict gate tied to website QA loop trust"
    - path: "tests/integration/broker-flow.test.ts"
      provides: "same-request INSTALL_REQUIRED -> HANDOFF_READY -> cross-host reuse proof"
    - path: "tests/shared-home/doctor.test.ts"
      provides: "QA proof rail unreadable / verdict coverage"
  key_links:
    - from: "tests/integration/broker-flow.test.ts"
      to: "src/shared-home/doctor.ts"
      via: "acquisition memory and verified downstream manifests become doctor proof evidence"
      pattern: "website QA INSTALL_REQUIRED -> install -> rerun -> cross-host reuse"
    - from: "src/shared-home/doctor.ts"
      to: "src/bin/skills-broker.ts"
      via: "`websiteQaLoop.verdict` drives strict doctor exit behavior"
      pattern: "websiteQaLoop|shouldFailStrictDoctorGate"
---

<objective>
把 website QA 变成 verdict-first 的 operator proof loop：同一句请求可经历 INSTALL_REQUIRED、rerun 成功、跨宿主复用，并被 doctor/strict gate 直接证明。

Purpose: 按 D-01、D-02、D-03、D-09、D-10、D-11 完成 ROUTE-01 / ROUTE-02 / ROUTE-03 的 proof loop，不把 Phase 2 的 family 泛化写成 done 条件。
Output: verdict-first doctor surface、strict CLI gate、QA end-to-end proof tests。
</objective>

<execution_context>
@/Users/monkeyin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/monkeyin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-prove-the-qa-hero-loop/01-CONTEXT.md
@.planning/phases/01-prove-the-qa-hero-loop/01-PATTERNS.md
@src/shared-home/doctor.ts
@src/shared-home/format.ts
@src/bin/skills-broker.ts
@tests/shared-home/doctor.test.ts
@tests/cli/lifecycle-cli.test.ts
@tests/integration/broker-flow.test.ts
@README.md
@README.zh-CN.md
@docs/superpowers/plans/2026-04-17-semantic-coarse-boundary-hardening-implementation.md

<interfaces>
From src/shared-home/doctor.ts:
```ts
export type DoctorLifecycleResult = {
  familyProofs: Record<DoctorProofFamily, DoctorFamilyProofSummary>;
  websiteQaLoop: DoctorFamilyProofSummary;
  adoptionHealth: AdoptionHealthResult;
};
```

From src/bin/skills-broker.ts:
```ts
type StrictDoctorGateInput = {
  adoptionHealth: { status: "inactive" | "green" | "blocked" };
  websiteQaLoop: { verdict: "blocked" | "in_progress" | "proven" };
};

export function shouldFailStrictDoctorGate(input: StrictDoctorGateInput): boolean;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 固化 QA verdict-first doctor 与 strict gate</name>
  <files>src/shared-home/doctor.ts, src/shared-home/format.ts, src/bin/skills-broker.ts, tests/shared-home/doctor.test.ts, tests/cli/lifecycle-cli.test.ts</files>
  <read_first>
.planning/phases/01-prove-the-qa-hero-loop/01-CONTEXT.md
README.md
README.zh-CN.md
src/shared-home/doctor.ts
src/shared-home/format.ts
src/bin/skills-broker.ts
tests/shared-home/doctor.test.ts
tests/cli/lifecycle-cli.test.ts
  </read_first>
  <behavior>
    - Test 1: proof rails unreadable 时 `websiteQaLoop.verdict === "blocked"`，`doctor --strict` 非零退出。
    - Test 2: loop 部分完成时 `websiteQaLoop.verdict === "in_progress"`，operator text 首先暴露 QA loop next action。
    - Test 3: loop 达到跨宿主复用后 `websiteQaLoop.verdict === "proven"`，同时保留 detail fields 供 drill-down。
  </behavior>
  <action>按 D-01、D-02、D-03、D-10、D-11 在 `src/shared-home/doctor.ts`、`src/shared-home/format.ts` 与 `src/bin/skills-broker.ts` 中继续把 `websiteQaLoop` 作为 top-level default-entry truth surface 和 strict gate 输入。若为 brownfield 兼容保留 `familyProofs`，也只能把它当 drill-down 细节，不能在本 phase 新增 family-proof 抽象或把 web markdown 提升为同级 done bar。确保 doctor 的 text / JSON 都优先呈现 QA loop 的 `blocked` / `in_progress` / `proven` verdict，并在 acquisition memory 或 verified downstream manifests 不可读时 fail-closed，不允许 adoption health 继续看起来是绿色。</action>
  <acceptance_criteria>
- `rg -n "websiteQaLoop|verdict|blocked|in_progress|proven" src/shared-home/doctor.ts src/shared-home/format.ts src/bin/skills-broker.ts` 返回 QA verdict-first 相关实现。
- `rg -n "treats a blocked website QA verdict as a strict failure|blocks adoption health when website QA proof rails are unreadable" tests/cli/lifecycle-cli.test.ts tests/shared-home/doctor.test.ts` 有命中。
- `npm test -- --run tests/shared-home/doctor.test.ts tests/cli/lifecycle-cli.test.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/shared-home/doctor.test.ts tests/cli/lifecycle-cli.test.ts</automated>
  </verify>
  <done>doctor/strict CLI 直接用 `websiteQaLoop` 表达 QA lane 的可信度，且 unreadable rails 会阻断 strict green truth。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 证明同一句 QA 请求的 install -> rerun -> cross-host reuse 闭环</name>
  <files>tests/integration/broker-flow.test.ts, tests/shared-home/doctor.test.ts, tests/cli/lifecycle-cli.test.ts</files>
  <read_first>
.planning/phases/01-prove-the-qa-hero-loop/01-CONTEXT.md
tests/integration/broker-flow.test.ts
tests/shared-home/doctor.test.ts
tests/cli/lifecycle-cli.test.ts
src/shared-home/doctor.ts
src/bin/skills-broker.ts
  </read_first>
  <behavior>
    - Test 1: 一个固定 website QA 请求先返回 `INSTALL_REQUIRED`，安装后同一句请求返回 `HANDOFF_READY`。
    - Test 2: 同一句 QA 请求从第二个宿主重跑时仍返回 `HANDOFF_READY`，并复用第一宿主已记录的成功路线。
    - Test 3: `doctor` / strict CLI 的断言与 integration proof 使用同一个 QA loop 事实来源，不靠 generic counter 猜测状态。
  </behavior>
  <action>按 D-09、D-10 实施并维护 QA canonical proof：在 `tests/integration/broker-flow.test.ts` 中用同一句 website QA request 覆盖 `INSTALL_REQUIRED -> install -> rerun -> cross-host HANDOFF_READY`，并让 `tests/shared-home/doctor.test.ts` / `tests/cli/lifecycle-cli.test.ts` 读取同一组 acquisition memory、verified downstream manifests、strict gate 事实来断言 `in_progress` 或 `proven`。如果现有 web markdown proof 因 brownfield 兼容需要保留，只允许把它当兼容 coverage；不要把它写成本 phase 的 acceptance bar，也不要新增任何泛化 family-proof 结构。</action>
  <acceptance_criteria>
- `rg -n "proves website QA INSTALL_REQUIRED -> install -> rerun -> cross-host reuse" tests/integration/broker-flow.test.ts` 有 canonical QA proof 测试名。
- `rg -n "doctor --strict|website QA verdict" tests/cli/lifecycle-cli.test.ts tests/shared-home/doctor.test.ts` 有与 QA loop 对齐的 strict proof 断言。
- `npm test -- --run tests/integration/broker-flow.test.ts tests/shared-home/doctor.test.ts tests/cli/lifecycle-cli.test.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/integration/broker-flow.test.ts tests/shared-home/doctor.test.ts tests/cli/lifecycle-cli.test.ts</automated>
  </verify>
  <done>website QA 用同一句请求拿到 install、rerun、cross-host reuse 的闭环证据，doctor/CLI strict 能直接读出这条 loop 当前是 blocked、in_progress 还是 proven。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared-home proof rails -> operator/CI | proof rail 不可读或失真时，如果 doctor 仍然报绿，会误导 operator 与 CI |
| first host success -> second host replay | 如果跨宿主复用证据未被正确记录，同一句 QA 请求会在第二宿主重新发现或误路由 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | T | `src/shared-home/doctor.ts`, `src/bin/skills-broker.ts` | mitigate | 让 unreadable acquisition memory / verified downstream manifests 把 `websiteQaLoop` 置为 `blocked`，并驱动 `doctor --strict` 非零退出。 |
| T-01-03-02 | R | `tests/integration/broker-flow.test.ts` | mitigate | 用同一句 QA 请求串起 INSTALL_REQUIRED、rerun、cross-host reuse，确保 proof 来自真实 routing/evidence，而不是 operator 自行推断 counters。 |
</threat_model>

<verification>
1. 运行 `npm test -- --run tests/shared-home/doctor.test.ts tests/cli/lifecycle-cli.test.ts`
2. 运行 `npm test -- --run tests/integration/broker-flow.test.ts tests/shared-home/doctor.test.ts tests/cli/lifecycle-cli.test.ts`
</verification>

<success_criteria>
- doctor 能直接告诉 operator QA loop 是 `blocked`、`in_progress` 还是 `proven`。
- 同一句 website QA 请求先返回 `INSTALL_REQUIRED`，安装后返回 `HANDOFF_READY`。
- 第二个宿主复用同一条 QA lane，不需要改写请求措辞。
</success_criteria>

<output>
After completion, create `.planning/phases/01-prove-the-qa-hero-loop/01-prove-the-qa-hero-loop-03-SUMMARY.md`
</output>
