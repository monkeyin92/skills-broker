---
phase: 02-generalize-family-proofs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared-home/doctor.ts
  - src/shared-home/format.ts
  - src/bin/skills-broker.ts
  - tests/shared-home/doctor.test.ts
  - tests/cli/lifecycle-cli.test.ts
autonomous: true
requirements:
  - ENTRY-03
  - GROW-01
must_haves:
  truths:
    - "`familyProofs` 是 Phase 2 的 canonical proof surface，`websiteQaLoop` 只是兼容 alias / strict gate 输入。"
    - "`skills-broker doctor` 继续 verdict-first：website QA 在前，web markdown 作为第二条 proven family 跟在后面。"
    - "proof rails unreadable 时，website QA 与 web markdown 都必须 fail-closed，而不是继续输出看起来健康的 operator truth。"
  artifacts:
    - path: "src/shared-home/doctor.ts"
      provides: "familyProofs 聚合、websiteQaLoop compatibility alias、family verdict/phase/nextAction truth"
    - path: "src/shared-home/format.ts"
      provides: "QA-first text output 与 generic family-proof rendering"
    - path: "src/bin/skills-broker.ts"
      provides: "strict doctor gate 继续绑定 websiteQaLoop alias"
    - path: "tests/shared-home/doctor.test.ts"
      provides: "familyProofs JSON/text 与 unreadable proof-rail regression"
    - path: "tests/cli/lifecycle-cli.test.ts"
      provides: "strict gate 对 blocked websiteQaLoop 的 compatibility proof"
  key_links:
    - from: "src/shared-home/doctor.ts"
      to: "src/shared-home/format.ts"
      via: "familyProofs canonical surface feeds QA-first verdict lines plus generic family loop lines"
      pattern: "familyProofs|websiteQaLoop|Web Markdown"
    - from: "src/shared-home/doctor.ts"
      to: "src/bin/skills-broker.ts"
      via: "`websiteQaLoop.verdict` remains the strict-doctor compatibility gate even after proof generalization"
      pattern: "websiteQaLoop|shouldFailStrictDoctorGate"
---

<objective>
把 `doctor` 的 proof surface 真正收口到 `familyProofs`，同时保留 `websiteQaLoop` 作为 Phase 1 已发货 contract 的兼容 alias，并继续维持 QA-first 的 operator truth。

Purpose: 满足 `ENTRY-03`，并按 D-01、D-02、D-03、D-12 把 Phase 1 的单一 QA proof loop 平滑推广成 family-proof surface，而不是重写 strict gate 或冲淡 QA hero lane。
Output: canonical family proof aggregation、QA-first text/json output、strict gate compatibility regressions。
</objective>

<execution_context>
@/Users/monkeyin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/monkeyin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-generalize-family-proofs/02-CONTEXT.md
@.planning/phases/02-generalize-family-proofs/02-PATTERNS.md
@.planning/phases/01-prove-the-qa-hero-loop/01-CONTEXT.md
@.planning/phases/01-prove-the-qa-hero-loop/01-VERIFICATION.md
@docs/superpowers/specs/2026-04-18-semantic-resolver-web-markdown-design.md
@src/shared-home/doctor.ts
@src/shared-home/format.ts
@src/bin/skills-broker.ts
@tests/shared-home/doctor.test.ts
@tests/cli/lifecycle-cli.test.ts

<interfaces>
From src/shared-home/doctor.ts:
```ts
export type DoctorLifecycleResult = {
  familyProofs: Record<DoctorProofFamily, DoctorFamilyProofSummary>;
  websiteQaLoop: DoctorFamilyProofSummary;
  adoptionHealth: AdoptionHealthResult;
};
```

From src/bin/skills-broker.ts:
```ts
type StrictDoctorGateInput = {
  adoptionHealth: { status: "inactive" | "green" | "blocked" };
  websiteQaLoop: { verdict: "blocked" | "in_progress" | "proven" };
};

export function shouldFailStrictDoctorGate(input: StrictDoctorGateInput): boolean;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 收口 canonical family proof surface，同时保留 websiteQaLoop alias</name>
  <files>src/shared-home/doctor.ts, tests/shared-home/doctor.test.ts</files>
  <read_first>
.planning/phases/02-generalize-family-proofs/02-CONTEXT.md
.planning/phases/02-generalize-family-proofs/02-PATTERNS.md
.planning/phases/01-prove-the-qa-hero-loop/01-VERIFICATION.md
docs/superpowers/specs/2026-04-18-semantic-resolver-web-markdown-design.md
src/shared-home/doctor.ts
tests/shared-home/doctor.test.ts
  </read_first>
  <behavior>
    - Test 1: `doctorSharedBrokerHome()` 返回的 `familyProofs.website_qa` 与 `familyProofs.web_content_to_markdown` 都有完整的 `verdict`、`phase`、`proofs`、`nextAction`。
    - Test 2: `websiteQaLoop` 等于 `familyProofs.website_qa` 的 compatibility view，而不是另一套独立推导逻辑。
    - Test 3: acquisition memory 或 verified downstream manifests unreadable 时，两条 family proof 都变成 `blocked` / `proof_unreadable`。
  </behavior>
  <action>按 D-01、D-02 在 `src/shared-home/doctor.ts` 保持 `DOCTOR_FAMILY_CONFIGS` 以 website QA 在前、web markdown 在后的顺序聚合 `familyProofs`，并把 `websiteQaLoop` 明确收口成 `familyProofs.website_qa` 的 compatibility alias。继续让 `installRequiredTraces`、`rerunSuccessfulRoutes`、`reuseRecorded`、`downstreamReplayManifests`、`verifyState`、`crossHostReuseState`、`nextAction` 都从同一套 family evidence 推导，禁止再新增 top-level one-off proof object；proof rails unreadable 时必须 fail-closed，而不是通过 generic counters 继续报绿。</action>
  <acceptance_criteria>
- `rg -n "familyProofs|websiteQaLoop|Web Markdown loop is proven" src/shared-home/doctor.ts tests/shared-home/doctor.test.ts` 有命中。
- `rg -n "emits reusable family proofs for website QA and web markdown|proof_unreadable" tests/shared-home/doctor.test.ts` 有命中。
- `npm test -- --run tests/shared-home/doctor.test.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/shared-home/doctor.test.ts</automated>
  </verify>
  <done>`familyProofs` 成为 canonical proof truth，`websiteQaLoop` 只作为兼容 alias 保留下来，proof unreadable 继续 fail-closed。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 维持 QA-first doctor output 与 strict gate compatibility contract</name>
  <files>src/shared-home/format.ts, src/bin/skills-broker.ts, tests/shared-home/doctor.test.ts, tests/cli/lifecycle-cli.test.ts</files>
  <read_first>
.planning/phases/02-generalize-family-proofs/02-CONTEXT.md
.planning/phases/02-generalize-family-proofs/02-PATTERNS.md
src/shared-home/format.ts
src/bin/skills-broker.ts
tests/shared-home/doctor.test.ts
tests/cli/lifecycle-cli.test.ts
  </read_first>
  <behavior>
    - Test 1: text output 先出现 `Website QA verdict` / `Website QA next action`，然后才进入 routing metrics 和 generic family loops。
    - Test 2: text/json 里同时能看到 `familyProofs.website_qa` 与 `familyProofs.web_content_to_markdown`，并保持 QA-first、web markdown second 的呈现顺序。
    - Test 3: `shouldFailStrictDoctorGate()` 继续只把 `websiteQaLoop.verdict === "blocked"` 当成 strict failure，不要求 operator 改读新字段。
  </behavior>
  <action>按 D-03、D-12 在 `src/shared-home/format.ts` 继续输出 `Website QA verdict:`、`Website QA next action:` 这两行作为 operator-first callout，同时用通用 family loop 渲染 website QA 与 web markdown 的 install/verify/reuse/replay 证据。保持 `src/bin/skills-broker.ts` 的 strict gate 仍然消费 `websiteQaLoop` compatibility alias，不把 strict consumer 强行迁移到新的 field path；但实际 truth 必须来自 Task 1 已收口的 `familyProofs`。更新 `tests/shared-home/doctor.test.ts` 与 `tests/cli/lifecycle-cli.test.ts`，锁住 QA-first ordering、web markdown second-family wording、以及 blocked verdict 仍然触发 strict failure。</action>
  <acceptance_criteria>
- `rg -n "Website QA verdict:|Website QA next action:|Web Markdown loop:" src/shared-home/format.ts tests/shared-home/doctor.test.ts` 有命中。
- `rg -n "shouldFailStrictDoctorGate|websiteQaLoop" src/bin/skills-broker.ts tests/cli/lifecycle-cli.test.ts` 有命中。
- `npm test -- --run tests/shared-home/doctor.test.ts tests/cli/lifecycle-cli.test.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/shared-home/doctor.test.ts tests/cli/lifecycle-cli.test.ts</automated>
  </verify>
  <done>`doctor` 继续先讲 QA hero lane 的 verdict，再讲 family details；strict gate 仍可通过 `websiteQaLoop` 兼容字段工作。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proof rails -> operator / CI | 如果 family proof 与 strict gate 读取的 truth 不一致，operator 会看到互相冲突的健康结论 |
| QA hero lane -> family-proof abstraction | 如果抽象泛化时把 QA 降格成普通 family，Phase 1 刚建立的默认入口心智会回退 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | T | `src/shared-home/doctor.ts` | mitigate | 让 `websiteQaLoop` 明确指向 `familyProofs.website_qa`，避免 canonical truth 与 compatibility alias 漂移成两套状态机。 |
| T-02-01-02 | I | `src/shared-home/format.ts`, `src/bin/skills-broker.ts` | mitigate | 继续用 QA-first verdict line + strict gate regression 锁住 operator 看到的默认入口 truth，不让 Phase 2 的 family 泛化冲淡 QA hero lane。 |
</threat_model>

<verification>
1. 运行 `rg -n "familyProofs|websiteQaLoop|Website QA verdict:|Web Markdown loop:" src/shared-home/doctor.ts src/shared-home/format.ts tests/shared-home/doctor.test.ts`
2. 运行 `npm test -- --run tests/shared-home/doctor.test.ts tests/cli/lifecycle-cli.test.ts`
</verification>

<success_criteria>
- `familyProofs` 成为 Phase 2 的 canonical proof surface。
- `websiteQaLoop` 继续作为 strict gate 兼容 alias 工作。
- `doctor` 对 operator 仍然先讲 website QA，再讲 web markdown 这条第二 proven family。
</success_criteria>

<output>
After completion, create `.planning/phases/02-generalize-family-proofs/02-generalize-family-proofs-01-SUMMARY.md`
</output>
