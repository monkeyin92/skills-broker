---
phase: 03-harden-runtime-and-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared-home/install.ts
  - src/hosts/claude-code/install.ts
  - src/hosts/codex/install.ts
  - tests/e2e/shared-home-smoke.test.ts
  - tests/e2e/claude-code-smoke.test.ts
autonomous: true
requirements:
  - HARD-01
must_haves:
  truths:
    - "Generated runners must prefer runtime-derived or manifest-derived path resolution; raw absolute broker-home paths are not the primary contract in Phase 3."
    - "Awkward path coverage must explicitly include spaces, quotes, `$`, and command-substitution-like characters across shared-home and host-shell install flows."
    - "Thin-host / broker-first product behavior stays unchanged; Phase 3 hardens runtime paths without changing the published hero lane or host boundary."
  artifacts:
    - path: "src/shared-home/install.ts"
      provides: "shared-home runner contract and broker-home resolution seam"
    - path: "src/hosts/claude-code/install.ts"
      provides: "Claude Code runner generation contract"
    - path: "src/hosts/codex/install.ts"
      provides: "Codex runner generation contract"
    - path: "tests/e2e/shared-home-smoke.test.ts"
      provides: "shared-home relocation and awkward-path smoke proof"
    - path: "tests/e2e/claude-code-smoke.test.ts"
      provides: "Claude Code install relocation and awkward-path smoke proof"
  key_links:
    - from: "src/shared-home/install.ts"
      to: "tests/e2e/shared-home-smoke.test.ts"
      via: "shared-home runner should derive `BROKER_HOME` from script location or adjacent layout instead of trusting install-time absolute paths"
      pattern: "SCRIPT_DIR|BROKER_HOME|run-broker"
    - from: "src/hosts/claude-code/install.ts"
      to: "tests/e2e/claude-code-smoke.test.ts"
      via: "Claude Code plugin runner must stay relocatable and safe under awkward filesystem paths"
      pattern: "INSTALL_DIR|run-broker|awkward path"
    - from: "src/hosts/codex/install.ts"
      to: "tests/e2e/shared-home-smoke.test.ts"
      via: "Codex host shell must keep shared-home reuse working after runner hardening"
      pattern: "BROKER_CURRENT_HOST=\"codex\"|bin/run-broker"
---

<objective>
把 shared-home、Claude Code、Codex 三条 runner 路径从“安装时直拼绝对路径”收紧为“运行时推导或 manifest 辅助推导”，并用 awkward path smoke 证明这条 contract 在高风险路径下仍然安全工作。

Purpose: 满足 `HARD-01`，优先降低 quoted path / shell metacharacter / relocation 带来的 runner fragility，而不改变 broker-first product behavior。
Output: runtime-derived runner resolution、awkward-path regression coverage、shared-home / host-shell relocation proof。
</objective>

<execution_context>
@/Users/monkeyin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/monkeyin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-harden-runtime-and-verification/03-CONTEXT.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@src/shared-home/install.ts
@src/shared-home/ownership.ts
@src/hosts/claude-code/install.ts
@src/hosts/codex/install.ts
@tests/e2e/shared-home-smoke.test.ts
@tests/e2e/claude-code-smoke.test.ts

<interfaces>
From src/shared-home/install.ts:
```ts
export async function installSharedBrokerHome(
  options: InstallSharedBrokerHomeOptions
): Promise<InstallSharedBrokerHomeResult>;
```

From src/hosts/claude-code/install.ts:
```ts
export async function installClaudeCodeHostShell(
  options: InstallClaudeCodeHostShellOptions
): Promise<InstallClaudeCodePluginResult>;
```

From src/hosts/codex/install.ts:
```ts
export async function installCodexHostShell(
  options: InstallCodexHostShellOptions
): Promise<InstallCodexHostShellResult>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 把 runner 路径解析改成 runtime-derived / manifest-derived seam</name>
  <files>src/shared-home/install.ts, src/hosts/claude-code/install.ts, src/hosts/codex/install.ts</files>
  <read_first>
.planning/phases/03-harden-runtime-and-verification/03-CONTEXT.md
.planning/codebase/CONCERNS.md
src/shared-home/install.ts
src/shared-home/ownership.ts
src/hosts/claude-code/install.ts
src/hosts/codex/install.ts
  </read_first>
  <behavior>
    - Test 1: moving the shared-home or installed host shell after install still leaves the runner able to locate the broker runtime through relative layout or managed metadata.
    - Test 2: Claude Code and Codex host-shell runners no longer depend on install-time raw `brokerHomeDirectory` interpolation as the primary resolution path.
    - Test 3: existing `--debug` and `BROKER_CURRENT_HOST` contracts remain intact.
  </behavior>
  <action>按 D-01、D-06 在 `src/shared-home/install.ts`、`src/hosts/claude-code/install.ts`、`src/hosts/codex/install.ts` 中把 runner path lookup 收紧成 runtime-derived 或 manifest-derived contract。shared-home runner 继续允许从 `SCRIPT_DIR` / adjacent layout 推导 `BROKER_HOME`；Claude Code / Codex host-shell runner 应从 shell 自身位置、ownership manifest 或固定相对布局定位 shared-home，而不是继续把 install-time `brokerHomeDirectory` 原样拼进 `exec ".../bin/run-broker"`。允许保留最小必要 quoting，但 quoting 只是补充保护，不是主策略。不要改写 host shell 文案、不要改变 thin-host boundary、也不要把此计划扩成安装架构重构。</action>
  <acceptance_criteria>
- `rg -n "SCRIPT_DIR|BROKER_HOME|readManagedShellManifest|brokerHome" src/shared-home/install.ts src/hosts/claude-code/install.ts src/hosts/codex/install.ts` 有命中。
- `rg -n "runtime-derived|manifest-derived|relocatable|awkward path" tests/e2e/shared-home-smoke.test.ts tests/e2e/claude-code-smoke.test.ts` 有对应测试或描述命中。
- `npm test -- --run tests/e2e/shared-home-smoke.test.ts tests/e2e/claude-code-smoke.test.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/e2e/shared-home-smoke.test.ts tests/e2e/claude-code-smoke.test.ts</automated>
  </verify>
  <done>三条 runner path 的主解析逻辑都从运行时位置或 managed metadata 推导，不再把 awkward install path 暴露成 bash 直拼风险。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 用 awkward-path smoke 锁住 spaces / quotes / `$` / command-substitution 风险</name>
  <files>tests/e2e/shared-home-smoke.test.ts, tests/e2e/claude-code-smoke.test.ts</files>
  <read_first>
.planning/phases/03-harden-runtime-and-verification/03-CONTEXT.md
.planning/codebase/CONCERNS.md
.planning/codebase/TESTING.md
tests/e2e/shared-home-smoke.test.ts
tests/e2e/claude-code-smoke.test.ts
  </read_first>
  <behavior>
    - Test 1: install directories containing spaces still allow update / doctor / broker-runner flows to succeed.
    - Test 2: paths containing quotes, `$`, or command-substitution-like characters do not break generated shell execution or accidentally expand.
    - Test 3: relocation coverage from existing smokes stays intact after introducing awkward path fixtures.
  </behavior>
  <action>在 `tests/e2e/shared-home-smoke.test.ts` 与 `tests/e2e/claude-code-smoke.test.ts` 里新增 targeted awkward-path fixtures，至少覆盖 spaces、single/double quote 邻近字符、`$`、以及 command-substitution-like 目录片段。测试应继续使用真实 temp dir + real runner invocation，而不是 mock shell 行为；必要时重命名既有 relocation fixture，让“正常 relocation”与“hostile path”两个场景都保持可读。不要只测 path contains space 这一条低强度 case，也不要把 regression coverage 退回到 string-level unit tests。</action>
  <acceptance_criteria>
- `rg -n "spaces|quotes|\\$|command-substitution|awkward path" tests/e2e/shared-home-smoke.test.ts tests/e2e/claude-code-smoke.test.ts` 有命中。
- `npm test -- --run tests/e2e/shared-home-smoke.test.ts tests/e2e/claude-code-smoke.test.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/e2e/shared-home-smoke.test.ts tests/e2e/claude-code-smoke.test.ts</automated>
  </verify>
  <done>shared-home 与 Claude Code 的安装/运行 smoke 都能证明 awkward path 不会再把 runner path 解析打碎。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| install-time path -> generated shell runner | 原始绝对路径被直拼进 shell source 时，spaces / quotes / `$` / command substitution 会把运行时 contract 变脆 |
| relocated host shell -> shared-home runtime | 安装后移动目录若仍依赖旧绝对路径，会让已安装宿主壳 silently break |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | T | `src/shared-home/install.ts`, `src/hosts/*/install.ts` | mitigate | 用 runtime-derived / manifest-derived path resolution 降低 raw path interpolation 风险，而不是只依赖 quoting。 |
| T-03-01-02 | D | `tests/e2e/shared-home-smoke.test.ts`, `tests/e2e/claude-code-smoke.test.ts` | mitigate | 用真实 temp dir + awkward path smoke 锁住高风险字符，避免安装路径变体在 CI 外 silently 失效。 |
</threat_model>

<verification>
1. 运行 `rg -n "SCRIPT_DIR|BROKER_HOME|spaces|quotes|\\$|command-substitution|awkward path" src/shared-home/install.ts src/hosts/claude-code/install.ts src/hosts/codex/install.ts tests/e2e/shared-home-smoke.test.ts tests/e2e/claude-code-smoke.test.ts`
2. 运行 `npm test -- --run tests/e2e/shared-home-smoke.test.ts tests/e2e/claude-code-smoke.test.ts`
</verification>

<success_criteria>
- generated runners 对 awkward install path 有 runtime-level 防护，而不是单纯赌 shell quoting 不出事。
- shared-home / Claude Code / Codex 的 relocation and reuse contract 在 hostile path 下仍然成立。
- Phase 3 没有改变现有 product wording 或 thin-host boundary。
</success_criteria>

<output>
After completion, create `.planning/phases/03-harden-runtime-and-verification/03-harden-runtime-and-verification-01-SUMMARY.md`
</output>
