---
phase: 03-harden-runtime-and-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/broker/result.ts
  - src/broker/run.ts
  - src/cli.ts
  - src/shared-home/doctor.ts
  - src/shared-home/format.ts
  - tests/broker/run-advisories.test.ts
  - tests/shared-home/doctor.test.ts
autonomous: true
requirements:
  - HARD-02
must_haves:
  truths:
    - "Routing can continue to succeed when advisory persistence misses happen, but the result must surface a structured degraded signal instead of silently swallowing the failure."
    - "Doctor and strict truth remain fail-closed on unreadable proof rails; Phase 3 cannot trade correctness for fewer warnings."
    - "Operator-visible output must distinguish `routing succeeded` from `routing succeeded but proof/reuse state was not fully persisted`."
  artifacts:
    - path: "src/broker/result.ts"
      provides: "typed degraded/advisory result surface"
    - path: "src/broker/run.ts"
      provides: "advisory persistence handling and degraded signal emission"
    - path: "src/cli.ts"
      provides: "broker CLI JSON output boundary"
    - path: "src/shared-home/doctor.ts"
      provides: "proof rail unreadable truth and next-action contract"
    - path: "src/shared-home/format.ts"
      provides: "operator-facing degraded / warning wording"
    - path: "tests/broker/run-advisories.test.ts"
      provides: "first-class regression proof for trace/acquisition/manifest persistence failures"
    - path: "tests/shared-home/doctor.test.ts"
      provides: "fail-closed doctor truth on unreadable proof rails"
  key_links:
    - from: "src/broker/run.ts"
      to: "src/broker/result.ts"
      via: "advisory persistence misses should become typed degraded output without changing the main outcome code"
      pattern: "advisories|degraded"
    - from: "src/cli.ts"
      to: "tests/broker/run-advisories.test.ts"
      via: "CLI JSON surface must preserve degraded signal even when trace output is omitted"
      pattern: "RunBrokerCliOutput|includeTrace"
    - from: "src/shared-home/doctor.ts"
      to: "tests/shared-home/doctor.test.ts"
      via: "proof rails unreadable must remain blocked / proof_unreadable with explicit next action"
      pattern: "proof_unreadable|warnings|nextAction"
---

<objective>
把 advisory persistence failure 从 silent swallow 收紧成 operator 可见的 structured degraded outcome，同时保持 doctor / strict truth 对 unreadable proof rails 的 fail-closed 语义。

Purpose: 满足 `HARD-02`，修补 “routing succeeds but proof/reuse evidence silently disappears” 这一类假绿风险，而不把 advisory writes 变成主路由 hard-fail。
Output: typed degraded result surface、runtime advisory regression tests、doctor fail-closed truth hardening。
</objective>

<execution_context>
@/Users/monkeyin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/monkeyin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-harden-runtime-and-verification/03-CONTEXT.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@src/broker/result.ts
@src/broker/run.ts
@src/broker/trace-store.ts
@src/broker/acquisition-memory.ts
@src/broker/downstream-manifest-source.ts
@src/cli.ts
@src/shared-home/doctor.ts
@src/shared-home/format.ts
@tests/shared-home/doctor.test.ts

<interfaces>
From src/broker/result.ts:
```ts
export type BrokerSuccessResult = {
  ok: true;
  outcome: { code: "HANDOFF_READY"; message: string };
  trace: BrokerRoutingTrace;
};

export type RunBrokerResult = BrokerSuccessResult | BrokerFailureResult | ...;
```

From src/cli.ts:
```ts
export async function runBrokerCli(
  input: RunBrokerCliInput,
  options?: RunBrokerCliOptions
): Promise<RunBrokerCliOutput>;
```

From src/shared-home/doctor.ts:
```ts
export type DoctorLifecycleResult = {
  familyProofs: Record<DoctorProofFamily, DoctorFamilyProofSummary>;
  websiteQaLoop: DoctorFamilyProofSummary;
  warnings: string[];
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 为 advisory persistence miss 增加 typed degraded signal</name>
  <files>src/broker/result.ts, src/broker/run.ts, src/cli.ts, tests/broker/run-advisories.test.ts</files>
  <read_first>
.planning/phases/03-harden-runtime-and-verification/03-CONTEXT.md
.planning/codebase/CONCERNS.md
src/broker/result.ts
src/broker/run.ts
src/broker/trace-store.ts
src/broker/acquisition-memory.ts
src/broker/downstream-manifest-source.ts
src/cli.ts
  </read_first>
  <behavior>
    - Test 1: when routing succeeds but trace persistence fails, the result still returns the original success code while exposing a structured advisory entry.
    - Test 2: acquisition-memory and verified-downstream manifest write failures also surface typed degraded entries instead of disappearing silently.
    - Test 3: `runBrokerCli()` preserves the degraded signal in JSON output regardless of whether `trace` is included.
  </behavior>
  <action>按 D-03、D-04 在 `src/broker/result.ts`、`src/broker/run.ts`、`src/cli.ts` 中增加最小必要的 typed degraded surface，例如 `advisories` / `degraded` entries，明确表达 `routing-trace-write-failed`、`acquisition-memory-write-failed`、`verified-downstream-manifest-write-failed` 之类 advisory persistence miss。保持主 `outcome.code` 不变，不要把这类 miss 一律升级成 routing hard-fail；同时禁止继续在 `run.ts` 中 silent catch 然后丢掉错误上下文。新增 `tests/broker/run-advisories.test.ts`，用可控的 I/O failure seam 证明 HANDOFF_READY / INSTALL_REQUIRED 等主结果可以保留，但 degraded signal 必须可机器读取、可 operator 理解。</action>
  <acceptance_criteria>
- `rg -n "advisories|degraded|trace.*failed|acquisition.*failed|manifest.*failed" src/broker/result.ts src/broker/run.ts src/cli.ts tests/broker/run-advisories.test.ts` 有命中。
- `npm test -- --run tests/broker/run-advisories.test.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/broker/run-advisories.test.ts</automated>
  </verify>
  <done>broker runtime 不再 silent swallow advisory persistence failure；主结果保持稳定，但 degraded signal 明确可见。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 保持 doctor / strict truth 对 unreadable proof rails 的 fail-closed contract</name>
  <files>src/shared-home/doctor.ts, src/shared-home/format.ts, tests/shared-home/doctor.test.ts</files>
  <read_first>
.planning/phases/03-harden-runtime-and-verification/03-CONTEXT.md
.planning/codebase/CONCERNS.md
src/shared-home/doctor.ts
src/shared-home/format.ts
tests/shared-home/doctor.test.ts
  </read_first>
  <behavior>
    - Test 1: unreadable acquisition memory or verified downstream manifests keep `websiteQaLoop` and `familyProofs.website_qa` blocked with `phase=proof_unreadable`.
    - Test 2: text/json output explicitly tells the operator which proof rail became unreadable and what repair action to take.
    - Test 3: doctor wording never implies a green or proven state when proof rails are unreadable or advisory truth is incomplete.
  </behavior>
  <action>按 D-03、D-04 在 `src/shared-home/doctor.ts` 与 `src/shared-home/format.ts` 中继续收紧 proof-rail truth：`acquisition memory unreadable`、`verified downstream manifests unreadable`、以及其他会让 verify/reuse proof 不可信的状态，都必须保持 `blocked` / `proof_unreadable`，并在 `warnings`、`nextAction`、operator-facing text 里说明具体修复方向。更新 `tests/shared-home/doctor.test.ts`，锁住 JSON 和 text output 都不会把 unreadable rails 讲成 “只是少了一点 telemetry”；它们必须是 doctor truth 的 hard blocker。</action>
  <acceptance_criteria>
- `rg -n "proof_unreadable|acquisition-memory: unreadable|downstream-manifests:.*unreadable|nextAction" src/shared-home/doctor.ts src/shared-home/format.ts tests/shared-home/doctor.test.ts` 有命中。
- `npm test -- --run tests/shared-home/doctor.test.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/shared-home/doctor.test.ts</automated>
  </verify>
  <done>doctor 与 strict truth 继续 fail-closed，operator 能明确看到 proof/reuse state 何时丢失且为何不能当成绿灯。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| broker runtime result -> operator | 如果 advisory persistence miss 被 silent swallow，operator 会把“成功路由”误判为“成功且已留下 proof/reuse evidence” |
| proof rails -> strict doctor truth | 如果 proof rails unreadable 还继续报绿，strict gate 和 release confidence 会被假绿污染 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | I | `src/broker/run.ts`, `src/broker/result.ts`, `src/cli.ts` | mitigate | 用 typed degraded/advisory surface 暴露 trace/memory/manifest 写失败，禁止 silent swallow。 |
| T-03-02-02 | T | `src/shared-home/doctor.ts`, `src/shared-home/format.ts` | mitigate | 继续让 unreadable proof rails 进入 `blocked` / `proof_unreadable`，并用 tests 锁住 operator-facing wording。 |
</threat_model>

<verification>
1. 运行 `rg -n "advisories|degraded|proof_unreadable|acquisition-memory: unreadable|downstream-manifests:.*unreadable" src/broker/result.ts src/broker/run.ts src/cli.ts src/shared-home/doctor.ts src/shared-home/format.ts tests/broker/run-advisories.test.ts tests/shared-home/doctor.test.ts`
2. 运行 `npm test -- --run tests/broker/run-advisories.test.ts tests/shared-home/doctor.test.ts`
</verification>

<success_criteria>
- routing 成功但 advisory persistence 失败时，结果会带 structured degraded signal，而不是 silent swallow。
- doctor / strict truth 在 proof rail unreadable 时继续 blocked / fail-closed。
- operator 可以区分 “routing succeeded” 和 “routing succeeded but proof state degraded”。
</success_criteria>

<output>
After completion, create `.planning/phases/03-harden-runtime-and-verification/03-harden-runtime-and-verification-02-SUMMARY.md`
</output>
