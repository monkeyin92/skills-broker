---
phase: 03-harden-runtime-and-verification
plan: 03
type: execute
wave: 2
depends_on:
  - "02"
files_modified:
  - src/shared-home/update.ts
  - src/shared-home/host-surface.ts
  - src/shared-home/peer-surface-audit.ts
  - src/shared-home/format.ts
  - tests/shared-home/update-lifecycle.test.ts
  - tests/cli/lifecycle-cli.test.ts
autonomous: true
requirements:
  - HARD-02
must_haves:
  truths:
    - "The highest-blast-radius skipped rollback/manual-recovery branch must be closed with executable fail-closed behavior, not left as a note."
    - "Repair and rollback failures must remain operator-visible through manual recovery markers, warnings, and lifecycle status; degraded success is not acceptable when rollback closure itself fails."
    - "Clear-manual-recovery remains a guarded operation tied to marker identity and host-surface integrity."
  artifacts:
    - path: "src/shared-home/update.ts"
      provides: "repair / rollback orchestration and overall lifecycle status"
    - path: "src/shared-home/host-surface.ts"
      provides: "peer-surface move and rollback primitives"
    - path: "src/shared-home/peer-surface-audit.ts"
      provides: "manual recovery marker and ledger contracts"
    - path: "src/shared-home/format.ts"
      provides: "operator-facing update output for manual recovery / rollback failure"
    - path: "tests/shared-home/update-lifecycle.test.ts"
      provides: "highest-blast-radius rollback/manual-recovery regression proof"
    - path: "tests/cli/lifecycle-cli.test.ts"
      provides: "lifecycle CLI clear-manual-recovery / failed rollback contract proof"
  key_links:
    - from: "src/shared-home/update.ts"
      to: "tests/shared-home/update-lifecycle.test.ts"
      via: "append-fails + rollback-fails branch must move from skipped debt to executable fail-closed behavior"
      pattern: "rollback failed during repair|manual recovery required"
    - from: "src/shared-home/peer-surface-audit.ts"
      to: "tests/cli/lifecycle-cli.test.ts"
      via: "manual recovery marker identity and clear command remain the source of truth for recovery closure"
      pattern: "markerId|clear-manual-recovery|evidence-ref"
    - from: "src/shared-home/format.ts"
      to: "tests/cli/lifecycle-cli.test.ts"
      via: "operator-facing update output must make rollback/manual recovery blockers unmistakable"
      pattern: "Host .* manual recovery|failurePhase=rollback"
---

<objective>
关闭 shared-home repair / rollback / manual-recovery 的最高 blast-radius skipped branch，并把 rollback failure 的 operator truth 固定成 fail-closed、可恢复、可审计的 contract。

Purpose: 满足 `HARD-02` 中最危险的一组 skipped recovery branches，优先防止 host surface 被半更新后又没有足够 recovery evidence 的状态。
Output: unskipped rollback/manual-recovery regression、clear-manual-recovery guard proof、lifecycle update fail-closed output。
</objective>

<execution_context>
@/Users/monkeyin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/monkeyin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-harden-runtime-and-verification/03-CONTEXT.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@src/shared-home/update.ts
@src/shared-home/host-surface.ts
@src/shared-home/peer-surface-audit.ts
@src/shared-home/format.ts
@tests/shared-home/update-lifecycle.test.ts
@tests/cli/lifecycle-cli.test.ts

<interfaces>
From src/shared-home/update.ts:
```ts
export type UpdateLifecycleResult = {
  command: "update";
  status: "success" | "degraded_success" | "failed";
  hosts: Array<{
    name: BrokerHost;
    status: HostLifecycleStatus;
    manualRecovery?: HostLifecycleManualRecovery;
  }>;
  warnings: string[];
};

export async function updateSharedBrokerHome(
  options: UpdateSharedBrokerHomeOptions
): Promise<UpdateLifecycleResult>;
```

From src/shared-home/peer-surface-audit.ts:
```ts
export function recommendedManualRecoveryClearCommand(...): string;
export async function writePeerSurfaceManualRecoveryMarker(...): Promise<...>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 关闭 append-fails + rollback-fails 的 skipped branch 并保持 fail-closed</name>
  <files>src/shared-home/update.ts, src/shared-home/host-surface.ts, src/shared-home/peer-surface-audit.ts, tests/shared-home/update-lifecycle.test.ts</files>
  <read_first>
.planning/phases/03-harden-runtime-and-verification/03-CONTEXT.md
.planning/codebase/CONCERNS.md
src/shared-home/update.ts
src/shared-home/host-surface.ts
src/shared-home/peer-surface-audit.ts
tests/shared-home/update-lifecycle.test.ts
  </read_first>
  <behavior>
    - Test 1: when peer-surface repair cannot append the ledger and rollback also cannot fully restore host state, lifecycle status becomes `failed`, not `degraded_success`.
    - Test 2: that branch writes a manual recovery marker with rollback failure context and keeps the failed peer list recoverable.
    - Test 3: warnings make the broken rollback explicit instead of only surfacing a generic degraded message.
  </behavior>
  <action>按 D-04、D-05 在 `src/shared-home/update.ts`、`src/shared-home/host-surface.ts`、`src/shared-home/peer-surface-audit.ts` 中补完 append-fails + rollback-fails 的 fail-closed path，并把 `tests/shared-home/update-lifecycle.test.ts` 里当前 skipped 的 “fails closed when repair cannot append the ledger and rollback cannot restore the host surface” 场景真正跑起来。失败时必须留下 manual recovery marker、failed peers、failurePhase / rollbackStatus 等可恢复证据；不要把这类状态继续归为 `degraded_success`，也不要只在 warning 里模糊提一句。</action>
  <acceptance_criteria>
- `rg -n "fails closed when repair cannot append the ledger and rollback cannot restore the host surface|rollback failed during repair|manual recovery required" tests/shared-home/update-lifecycle.test.ts` 有命中。
- `npm test -- --run tests/shared-home/update-lifecycle.test.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/shared-home/update-lifecycle.test.ts</automated>
  </verify>
  <done>最高 blast-radius 的 rollback/manual-recovery branch 从 skipped debt 变成可执行的 fail-closed contract，且 recovery evidence 完整可见。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 对齐 lifecycle CLI 的 manual-recovery / rollback failure truth</name>
  <files>src/shared-home/format.ts, tests/cli/lifecycle-cli.test.ts</files>
  <read_first>
.planning/phases/03-harden-runtime-and-verification/03-CONTEXT.md
src/shared-home/format.ts
tests/cli/lifecycle-cli.test.ts
  </read_first>
  <behavior>
    - Test 1: `skills-broker update` text/json output clearly shows manual recovery marker and rollback failure phase.
    - Test 2: `--clear-manual-recovery` still rejects marker mismatch or dirty host surface even after the new fail-closed branch is enabled.
    - Test 3: lifecycle CLI regression tests keep the recovery closure path explainable to operators.
  </behavior>
  <action>在 `src/shared-home/format.ts` 与 `tests/cli/lifecycle-cli.test.ts` 中对齐 rollback/manual-recovery 的 operator truth。`skills-broker update` 输出必须把 `manual recovery`、`markerId`、`failurePhase=rollback`、以及 recovery clear path 讲清楚；`--clear-manual-recovery` 仍然需要 marker identity、host surface clean、evidence refs 等 guard，不能因为补了 fail-closed branch 就放松关闭条件。不要把 recovery closure logic 混进 unrelated lifecycle flags，也不要弱化 CLI 的 non-zero / blocked semantics。</action>
  <acceptance_criteria>
- `rg -n "manual recovery|failurePhase=rollback|clear manual recovery|markerId" src/shared-home/format.ts tests/cli/lifecycle-cli.test.ts` 有命中。
- `npm test -- --run tests/cli/lifecycle-cli.test.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/cli/lifecycle-cli.test.ts</automated>
  </verify>
  <done>lifecycle CLI 对 rollback/manual recovery blocker 的输出和 guard 条件都与新的 fail-closed branch 保持一致。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| peer-surface mutation -> rollback closure | 如果 repair 失败后 rollback 也失败，但系统没有 fail-closed / recovery evidence，宿主壳会停在半损坏状态 |
| manual recovery marker -> operator action | 如果 clear-manual-recovery 没有 marker/host guard，operator 可能在未真正修复时错误关闭 blocker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | T | `src/shared-home/update.ts`, `src/shared-home/host-surface.ts` | mitigate | 让 append-fails + rollback-fails branch 真正执行并保持 `failed` / manual recovery required，而不是继续 skipped or degraded-success。 |
| T-03-03-02 | R | `src/shared-home/peer-surface-audit.ts`, `src/shared-home/format.ts`, `tests/cli/lifecycle-cli.test.ts` | mitigate | 用 marker identity、clear command、evidence refs 与 CLI regression proof 锁住 recovery closure 审计链。 |
</threat_model>

<verification>
1. 运行 `rg -n "fails closed when repair cannot append the ledger and rollback cannot restore the host surface|rollback failed during repair|manual recovery|failurePhase=rollback|markerId" src/shared-home/update.ts src/shared-home/format.ts tests/shared-home/update-lifecycle.test.ts tests/cli/lifecycle-cli.test.ts`
2. 运行 `npm test -- --run tests/shared-home/update-lifecycle.test.ts tests/cli/lifecycle-cli.test.ts`
</verification>

<success_criteria>
- highest-blast-radius 的 rollback/manual-recovery skipped branch 被关闭并进入可执行测试。
- rollback closure 自身失败时，结果保持 `failed` 并留下 recovery marker / failed peers / operator hints。
- lifecycle CLI 不会允许未验证的 manual recovery closure。
</success_criteria>

<output>
After completion, create `.planning/phases/03-harden-runtime-and-verification/03-harden-runtime-and-verification-03-SUMMARY.md`
</output>
