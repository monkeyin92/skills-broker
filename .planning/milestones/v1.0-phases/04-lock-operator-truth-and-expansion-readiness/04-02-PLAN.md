---
phase: 04-lock-operator-truth-and-expansion-readiness
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/hosts/skill-markdown.ts
  - tests/hosts/host-shell-install.test.ts
  - tests/e2e/shared-home-smoke.test.ts
  - src/core/operator-truth.ts
autonomous: true
requirements:
  - HARD-03
  - GROW-02
must_haves:
  truths:
    - "installed host shell 继续只暴露 coarse broker-first boundary，不把具体 package/skill/workflow 决策泄露给宿主入口。"
    - "host shell copy 与 repo docs 必须复用同一套 operator truth，而不是各自维护 support matrix 或 hierarchy。"
    - "Phase 4 不实现 OpenCode；它只证明当前 host shell contract 足够薄，未来第三宿主能沿同一 contract 接入。"
  artifacts:
    - path: "src/hosts/skill-markdown.ts"
      provides: "generated installed host shell copy and thin-boundary contract"
    - path: "tests/hosts/host-shell-install.test.ts"
      provides: "installed shell wording/order regression"
    - path: "tests/e2e/shared-home-smoke.test.ts"
      provides: "real installed shell smoke coverage"
    - path: "src/core/operator-truth.ts"
      provides: "canonical operator truth reused by host shell generation"
  key_links:
    - from: "src/core/operator-truth.ts"
      to: "src/hosts/skill-markdown.ts"
      via: "installed host shell should reuse canonical support/hierarchy truth instead of hand-maintained copy"
      pattern: "supportedHosts|heroLane|secondProvenFamily"
    - from: "src/hosts/skill-markdown.ts"
      to: "tests/hosts/host-shell-install.test.ts"
      via: "host shell must keep broker-first boundary and not leak downstream candidate choice"
      pattern: "choose only broker_first, handle_normally, or clarify_before_broker|do not pick a package, workflow family, skill, or MCP at the host layer"
    - from: "src/hosts/skill-markdown.ts"
      to: "tests/e2e/shared-home-smoke.test.ts"
      via: "installed shell smoke should keep hero-lane hierarchy and coarse-boundary wording after refactors"
      pattern: "### Hero lane: website QA|### Secondary maintained lanes|Host Contract"
---

<objective>
把 installed host shell 继续锁在 coarse broker-first boundary 上，并让它与 repo-level operator truth 共用一套 canonical contract，从而为 future third host 保留干净接入面。

Purpose: 同时满足 `HARD-03` 的 host-shell parity 和 `GROW-02` 的 thin-host readiness。
Output: contract-backed host shell copy、强化后的 boundary regression、future-host-safe installed-shell proof。
</objective>

<execution_context>
@/Users/monkeyin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/monkeyin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-lock-operator-truth-and-expansion-readiness/04-CONTEXT.md
@.planning/phases/01-prove-the-qa-hero-loop/01-VERIFICATION.md
@.planning/phases/02-generalize-family-proofs/02-VERIFICATION.md
@src/core/operator-truth.ts
@src/hosts/skill-markdown.ts
@tests/hosts/host-shell-install.test.ts
@tests/e2e/shared-home-smoke.test.ts
@docs/superpowers/specs/2026-03-28-broker-auto-router-design.md
@docs/superpowers/specs/2026-03-31-broker-first-capability-scaling-design.md

<interfaces>
From src/hosts/skill-markdown.ts:
```ts
export function buildHostShellSkillMarkdown(
  options: HostShellSkillMarkdownOptions
): string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 让 installed shell copy 复用 canonical operator truth 并继续保持 coarse boundary</name>
  <files>src/hosts/skill-markdown.ts, src/core/operator-truth.ts</files>
  <read_first>
.planning/phases/04-lock-operator-truth-and-expansion-readiness/04-CONTEXT.md
.planning/PROJECT.md
src/core/operator-truth.ts
src/hosts/skill-markdown.ts
docs/superpowers/specs/2026-03-28-broker-auto-router-design.md
docs/superpowers/specs/2026-03-31-broker-first-capability-scaling-design.md
  </read_first>
  <behavior>
    - Test 1: installed shell still teaches only the boundary outcomes and never instructs the host to choose a downstream package/skill/MCP.
    - Test 2: current hero lane / second proven family hierarchy stays intact after the host shell starts consuming canonical truth.
    - Test 3: future host readiness is implied by contract reuse, not by adding OpenCode-specific runtime behavior.
  </behavior>
  <action>按 D-04、D-05 让 `src/hosts/skill-markdown.ts` 复用 Phase 4 建立的 canonical operator truth source，至少在 support/hierarchy wording 上不再独立手写一套 truth。保留并强化 coarse broker-first boundary wording：宿主只能决定 `broker_first` / `handle_normally` / `clarify_before_broker`，不能在入口层选择 package、workflow、skill 或 MCP。不要在这一计划里实现 OpenCode adapter/install，也不要把 host shell 改回按 family 平铺的选择菜单。</action>
  <acceptance_criteria>
- `rg -n "broker_first|handle_normally|clarify_before_broker|do not pick a package, workflow family, skill, or MCP at the host layer" src/hosts/skill-markdown.ts` 有命中。
- `rg -n "supportedHosts|heroLane|secondProvenFamily" src/core/operator-truth.ts src/hosts/skill-markdown.ts` 有命中。
- `npm test -- --run tests/hosts/host-shell-install.test.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/hosts/host-shell-install.test.ts</automated>
  </verify>
  <done>installed shell copy 以 canonical truth 为准，同时继续把宿主边界锁在 coarse broker-first contract 上。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 扩充 smoke / install regression，锁住 future-host-safe wording</name>
  <files>tests/hosts/host-shell-install.test.ts, tests/e2e/shared-home-smoke.test.ts</files>
  <read_first>
.planning/phases/04-lock-operator-truth-and-expansion-readiness/04-CONTEXT.md
src/hosts/skill-markdown.ts
tests/hosts/host-shell-install.test.ts
tests/e2e/shared-home-smoke.test.ts
  </read_first>
  <behavior>
    - Test 1: install tests keep asserting hero lane first, second proven family second, and coarse-boundary wording.
    - Test 2: shared-home smoke keeps proving the installed shell copy seen by a real host matches the same hierarchy and contract.
    - Test 3: wording regressions that would overclaim support or leak downstream candidate choice become red tests.
  </behavior>
  <action>在 `tests/hosts/host-shell-install.test.ts` 与 `tests/e2e/shared-home-smoke.test.ts` 中补强 Phase 4 的 regression bar，锁住三类 truth：website QA hero lane first、web markdown second proven family second、以及 coarse broker-first boundary wording。必要时新增“当前支持矩阵不应 overclaim future host readiness”之类的断言，但不要把 smoke 扩成真正的 OpenCode implementation test，也不要只加低价值 snapshot。</action>
  <acceptance_criteria>
- `rg -n "Hero lane: website QA|Secondary maintained lanes|choose only broker_first, handle_normally, or clarify_before_broker|do not pick a package, workflow family, skill, or MCP at the host layer" tests/hosts/host-shell-install.test.ts tests/e2e/shared-home-smoke.test.ts` 有命中。
- `npm test -- --run tests/hosts/host-shell-install.test.ts tests/e2e/shared-home-smoke.test.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/hosts/host-shell-install.test.ts tests/e2e/shared-home-smoke.test.ts</automated>
  </verify>
  <done>installed shell wording的层级和边界都被真实 install/smoke proof 锁住，不会因为 future host 叙事而退化。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical truth -> installed host shell | 如果 host shell copy 不复用 canonical truth，它会再次变成单独漂移的 operator surface |
| host entry -> broker runtime | 如果 host shell 开始泄露 package/skill/workflow 选择，未来第三宿主会继承错误边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | I | `src/hosts/skill-markdown.ts` | mitigate | 复用 canonical operator truth，并继续把宿主限制在 coarse broker-first boundary。 |
| T-04-02-02 | R | `tests/hosts/host-shell-install.test.ts`, `tests/e2e/shared-home-smoke.test.ts` | mitigate | 用 install/smoke tests 锁住真实 installed shell copy，而不是只靠 repo docs。 |
</threat_model>

<verification>
1. 运行 `rg -n "broker_first|handle_normally|clarify_before_broker|Hero lane: website QA|Secondary maintained lanes" src/hosts/skill-markdown.ts tests/hosts/host-shell-install.test.ts tests/e2e/shared-home-smoke.test.ts`
2. 运行 `npm test -- --run tests/hosts/host-shell-install.test.ts tests/e2e/shared-home-smoke.test.ts`
</verification>

<success_criteria>
- installed host shell 继续只暴露 coarse broker-first boundary。
- host shell copy 的 hierarchy 与 repo docs 使用同一套 canonical truth。
- future third host 可以沿同一 contract 接入，而不是继承一套已经漂移的宿主文案。
</success_criteria>

<output>
After completion, create `.planning/phases/04-lock-operator-truth-and-expansion-readiness/04-lock-operator-truth-and-expansion-readiness-02-SUMMARY.md`
</output>
