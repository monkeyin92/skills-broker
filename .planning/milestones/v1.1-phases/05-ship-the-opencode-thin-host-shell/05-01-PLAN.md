---
phase: 05-ship-the-opencode-thin-host-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/types.ts
  - src/core/envelope.ts
  - src/cli.ts
  - src/shared-home/paths.ts
  - tests/core/envelope.test.ts
  - tests/cli/cli-contract.test.ts
  - tests/shared-home/update-lifecycle.test.ts
autonomous: true
requirements:
  - HOST-01
must_haves:
  truths:
    - "OpenCode 的 canonical runtime host id 必须统一为 `opencode`，不能继续出现 `open-code` 或其他并列 host id。"
    - "published lifecycle CLI 必须新增 `--opencode-dir`，并在 path resolution 中同时兼容 `~/.config/opencode` 与 `~/.opencode`。"
    - "Phase 5 只建立第三宿主 attach path 的 host/CLI contract，不在这一计划里承诺完整 doctor/remove/proof parity。"
  artifacts:
    - path: "src/core/types.ts"
      provides: "canonical `opencode` host identity and support spec"
    - path: "src/core/envelope.ts"
      provides: "envelope host validation for `opencode`"
    - path: "src/cli.ts"
      provides: "CLI currentHost and `--opencode-dir` contract"
    - path: "src/shared-home/paths.ts"
      provides: "default OpenCode root detection and install-directory resolution"
    - path: "tests/core/envelope.test.ts"
      provides: "host id acceptance/rejection proof"
    - path: "tests/cli/cli-contract.test.ts"
      provides: "CLI flag and currentHost regression proof"
    - path: "tests/shared-home/update-lifecycle.test.ts"
      provides: "lifecycle path resolution proof for the new host"
  key_links:
    - from: "src/core/types.ts"
      to: "src/core/envelope.ts"
      via: "the same typed host list must drive envelope validation"
      pattern: "BROKER_HOSTS|isBrokerHost|opencode"
    - from: "src/core/types.ts"
      to: "src/cli.ts"
      via: "CLI currentHost validation and `--host` must accept only canonical host ids"
      pattern: "BROKER_HOSTS|isBrokerHost|--opencode-dir"
    - from: "src/shared-home/paths.ts"
      to: "tests/shared-home/update-lifecycle.test.ts"
      via: "OpenCode root detection must be locked with explicit `.config/opencode` / `.opencode` assertions"
      pattern: "opencodeDirOverride|opencodeInstallDirectory|.config/opencode|.opencode"
---

<objective>
先把 OpenCode 的 canonical host identity、envelope/currentHost validation、CLI flag 和 lifecycle path resolution 收成一套可执行 contract，让 Phase 5 后续的 thin shell attach 与 docs flip 都建立在同一个 `opencode` truth 上。

Purpose: 为 `HOST-01` 建立第三宿主的 runtime/CLI 入口真相，避免再出现 host naming drift。
Output: `opencode` host enum/support spec、`--opencode-dir` CLI contract、双根目录兼容的 lifecycle path resolution、对应 unit/CLI/path tests。
</objective>

<execution_context>
@/Users/monkeyin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/monkeyin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ship-the-opencode-thin-host-shell/05-CONTEXT.md
@src/core/types.ts
@src/core/envelope.ts
@src/cli.ts
@src/shared-home/paths.ts
@tests/core/envelope.test.ts
@tests/cli/cli-contract.test.ts
@tests/shared-home/update-lifecycle.test.ts
@docs/superpowers/specs/2026-04-22-third-host-thin-shell-readiness.md

<interfaces>
Current host truth:
```ts
export const BROKER_HOSTS = [
  "claude-code",
  "codex"
] as const;
```

Expected additions:
```ts
type BrokerHost = "claude-code" | "codex" | "opencode";

type ResolveLifecyclePathsInput = {
  homeDirectory?: string;
  brokerHomeOverride?: string;
  claudeDirOverride?: string;
  codexDirOverride?: string;
  opencodeDirOverride?: string;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 统一 `opencode` host truth、envelope validation 与 CLI flag</name>
  <files>src/core/types.ts, src/core/envelope.ts, src/cli.ts, tests/core/envelope.test.ts, tests/cli/cli-contract.test.ts</files>
  <read_first>
.planning/phases/05-ship-the-opencode-thin-host-shell/05-CONTEXT.md
src/core/types.ts
src/core/envelope.ts
src/cli.ts
tests/core/envelope.test.ts
tests/cli/cli-contract.test.ts
  </read_first>
  <behavior>
    - Test 1: broker envelope host validation accepts `opencode` and still rejects `open-code` or other non-canonical aliases.
    - Test 2: CLI currentHost validation and `--host` handling accept `opencode` without weakening conflict checks.
    - Test 3: lifecycle CLI parses `--opencode-dir` exactly like `--claude-dir` / `--codex-dir`.
  </behavior>
  <action>在 `src/core/types.ts` 里把 `BROKER_HOSTS` 扩成 `["claude-code", "codex", "opencode"]`，新增 `BROKER_HOST_SUPPORT.opencode`，其已知 shell 条目固定为 `["SKILL.md", "bin", ".skills-broker.json"]`。在 `src/core/envelope.ts` 和 `src/cli.ts` 中，把所有 host/currentHost 校验与报错文案更新为只接受 `claude-code|codex|opencode` 这三种值，并在 lifecycle CLI 解析里新增 `--opencode-dir` flag；不要接受 `open-code` 或大小写变体作为兼容别名，也不要为 OpenCode 增加新的 lifecycle command 名称。</action>
  <acceptance_criteria>
- `rg -n "\"opencode\"|--opencode-dir|Expected broker envelope.host to be one of claude-code, codex, opencode|Expected broker currentHost to be one of claude-code, codex, opencode" src/core/types.ts src/core/envelope.ts src/cli.ts tests/core/envelope.test.ts tests/cli/cli-contract.test.ts` 有命中。
- `npm test -- --run tests/core/envelope.test.ts tests/cli/cli-contract.test.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/core/envelope.test.ts tests/cli/cli-contract.test.ts</automated>
  </verify>
  <done>`opencode` 成为唯一 canonical 第三宿主 id，CLI/envelope 不再对 host naming 含糊其辞。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 增加 OpenCode 双根目录 detection 与 install path resolution</name>
  <files>src/shared-home/paths.ts, tests/shared-home/update-lifecycle.test.ts</files>
  <read_first>
.planning/phases/05-ship-the-opencode-thin-host-shell/05-CONTEXT.md
src/shared-home/paths.ts
tests/shared-home/update-lifecycle.test.ts
  </read_first>
  <behavior>
    - Test 1: lifecycle path resolution supports an explicit `opencodeDirOverride`.
    - Test 2: default target detection treats `~/.config/opencode` and `~/.opencode` as valid OpenCode roots.
    - Test 3: the resolved OpenCode shell install path is `<detected-root>/skills/skills-broker`, not a new OpenCode-only runtime root.
  </behavior>
  <action>在 `src/shared-home/paths.ts` 中扩展 `ResolveLifecyclePathsInput` / `ResolveLifecyclePathsResult` / `DetectLifecycleHostTargetsResult`，新增 `opencodeDirOverride`、`opencodeInstallDirectory` 和 `opencode` target。把 OpenCode 的默认 root detection 设计成同时检查 `~/.config/opencode` 与 `~/.opencode` 两个根目录，并把 install directory 固定解析到 `${detectedRoot}/skills/skills-broker`；若提供 `--opencode-dir` override，则直接使用 override 结果。同步在 `tests/shared-home/update-lifecycle.test.ts` 增加显式断言，覆盖上述双根目录、override 与 “root 缺失时提示 `--opencode-dir`” 的 reason string；不要为 OpenCode 引入第二套 broker home 默认值。</action>
  <acceptance_criteria>
- `rg -n "opencodeDirOverride|opencodeInstallDirectory|\\.config\", \"opencode\"|\\.opencode|--opencode-dir" src/shared-home/paths.ts tests/shared-home/update-lifecycle.test.ts` 有命中。
- `npm test -- --run tests/shared-home/update-lifecycle.test.ts -t "shared-home lifecycle paths"` 通过。
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/shared-home/update-lifecycle.test.ts -t "shared-home lifecycle paths"</automated>
  </verify>
  <done>shared-home path resolution 已经知道 OpenCode 在哪、该往哪装壳，并且这些值被测试锁死。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical host id -> CLI/envelope/runtime | 如果 `opencode` 没有统一成单一 host id，更新 path、tests、docs 会继续各说各话 |
| OpenCode root detection -> install target | 如果根目录与 install dir 不明确，第三宿主会很快演变成手工指定路径的脆弱集成 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | I | `src/core/types.ts`, `src/core/envelope.ts`, `src/cli.ts` | mitigate | 用单一 `opencode` host id 和 CLI/envelope tests 消除 host naming drift。 |
| T-05-01-02 | T | `src/shared-home/paths.ts` | mitigate | 明确双根目录 detection 与固定 install subpath，防止第三宿主 attach 落到随意路径。 |
</threat_model>

<verification>
1. 运行 `rg -n "\"opencode\"|--opencode-dir|opencodeInstallDirectory|\\.config\", \"opencode\"|\\.opencode" src/core/types.ts src/core/envelope.ts src/cli.ts src/shared-home/paths.ts tests/core/envelope.test.ts tests/cli/cli-contract.test.ts tests/shared-home/update-lifecycle.test.ts`
2. 运行 `npm test -- --run tests/core/envelope.test.ts tests/cli/cli-contract.test.ts`
3. 运行 `npm test -- --run tests/shared-home/update-lifecycle.test.ts -t "shared-home lifecycle paths"`
</verification>

<success_criteria>
- `opencode` 成为 Phase 5 唯一 canonical 第三宿主 id。
- lifecycle CLI 具备 `--opencode-dir`，且 path resolution 同时兼容 `~/.config/opencode` 与 `~/.opencode`。
- OpenCode 的 host/CLI contract 被 unit/CLI/path tests fail closed。
</success_criteria>

<output>
After completion, create `.planning/phases/05-ship-the-opencode-thin-host-shell/05-ship-the-opencode-thin-host-shell-01-SUMMARY.md`
</output>
