---
phase: 05-ship-the-opencode-thin-host-shell
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/hosts/opencode/install.ts
  - src/hosts/opencode/adapter.ts
  - src/shared-home/update.ts
  - src/shared-home/remove.ts
  - src/shared-home/doctor.ts
  - tests/hosts/host-shell-install.test.ts
  - tests/shared-home/update-lifecycle.test.ts
  - tests/shared-home/doctor.test.ts
autonomous: true
requirements:
  - HOST-01
  - HOST-02
must_haves:
  truths:
    - "OpenCode 必须通过同一条 `npx skills-broker update` attach 到 shared broker home，而不是新增 OpenCode-only install path。"
    - "OpenCode host shell 必须是 Codex-style thin shell：`SKILL.md` + runner + managed manifest，且 `invocationMode` 固定为 `explicit`。"
    - "doctor/remove 在本计划里只做最小一致性 plumbing，不能提前分叉出 OpenCode-only proof/reuse surface。"
  artifacts:
    - path: "src/hosts/opencode/install.ts"
      provides: "OpenCode thin host shell installer"
    - path: "src/hosts/opencode/adapter.ts"
      provides: "OpenCode adapter for installed-shell execution"
    - path: "src/shared-home/update.ts"
      provides: "published update path that can install/repair OpenCode shell"
    - path: "src/shared-home/remove.ts"
      provides: "host detach plumbing aware of OpenCode"
    - path: "src/shared-home/doctor.ts"
      provides: "minimal host inventory truth for OpenCode without overclaiming parity"
    - path: "tests/hosts/host-shell-install.test.ts"
      provides: "installed OpenCode shell boundary/manifest regression"
    - path: "tests/shared-home/update-lifecycle.test.ts"
      provides: "published update path regression for third-host attach"
    - path: "tests/shared-home/doctor.test.ts"
      provides: "minimal doctor truth regression for OpenCode host state"
  key_links:
    - from: "src/hosts/opencode/install.ts"
      to: "tests/hosts/host-shell-install.test.ts"
      via: "the new shell must reuse the same generated coarse-boundary copy and managed manifest contract"
      pattern: "host: \"opencode\"|invocationMode: \"explicit\"|BROKER_CURRENT_HOST=\"opencode\""
    - from: "src/hosts/opencode/install.ts"
      to: "src/shared-home/update.ts"
      via: "published update path should attach OpenCode by calling the new installer, not by inlining custom shell writes"
      pattern: "installOpenCodeHostShell|BROKER_HOSTS"
    - from: "src/shared-home/doctor.ts"
      to: "tests/shared-home/doctor.test.ts"
      via: "doctor should report OpenCode as detected/not_detected/managed without claiming Phase 6 parity is done"
      pattern: "name: \"opencode\"|managedHosts|--opencode-dir"
---

<objective>
把 OpenCode 真正接进 Phase 5 的 published host-shell path：新增 Codex-style thin shell installer/adapter，并把 `update` / `remove` / `doctor` 至少推进到能诚实识别第三宿主 attach 状态的程度，而不把 Phase 6 的完整 parity 一起塞进来。

Purpose: 满足 `HOST-01` 与 `HOST-02` 的最小 shipping bar，让第三宿主壳真实可安装、可运行、可被 lifecycle surface 识别。
Output: `src/hosts/opencode/*`、published update path 的 OpenCode attach、minimal doctor/remove consistency、对应 host/lifecycle tests。
</objective>

<execution_context>
@/Users/monkeyin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/monkeyin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ship-the-opencode-thin-host-shell/05-CONTEXT.md
@src/hosts/codex/install.ts
@src/hosts/codex/adapter.ts
@src/shared-home/update.ts
@src/shared-home/remove.ts
@src/shared-home/doctor.ts
@src/shared-home/ownership.ts
@tests/hosts/host-shell-install.test.ts
@tests/shared-home/update-lifecycle.test.ts
@tests/shared-home/doctor.test.ts

<interfaces>
Use Codex as the reference shape:
```ts
export async function installCodexHostShell(
  options: InstallCodexHostShellOptions
): Promise<InstallCodexHostShellResult>;

export async function runCodexAdapter(
  input: NormalizeRequestInput,
  options: RunCodexAdapterOptions
): Promise<RunBrokerResult>;
```

Expected new interfaces:
```ts
export async function installOpenCodeHostShell(...): Promise<...>;
export async function runOpenCodeAdapter(...): Promise<RunBrokerResult>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新增 Codex-style OpenCode thin shell installer 与 adapter</name>
  <files>src/hosts/opencode/install.ts, src/hosts/opencode/adapter.ts, tests/hosts/host-shell-install.test.ts</files>
  <read_first>
.planning/phases/05-ship-the-opencode-thin-host-shell/05-CONTEXT.md
src/hosts/codex/install.ts
src/hosts/codex/adapter.ts
src/hosts/skill-markdown.ts
src/shared-home/ownership.ts
tests/hosts/host-shell-install.test.ts
  </read_first>
  <behavior>
    - Test 1: OpenCode shell installer writes `SKILL.md`, `bin/run-broker`, and `.skills-broker.json` exactly like the thin-shell hosts do today.
    - Test 2: the generated shell passes `host: "opencode"` and `invocationMode: "explicit"` into the existing host shell markdown contract.
    - Test 3: the runner resolves `brokerHome` from the managed manifest and execs shared `bin/run-broker` with `BROKER_CURRENT_HOST="opencode"`.
  </behavior>
  <action>新增 `src/hosts/opencode/install.ts` 与 `src/hosts/opencode/adapter.ts`，整体形态对齐 `src/hosts/codex/*`：installer 负责写出 `SKILL.md`、`bin/run-broker`、`.skills-broker.json` 三件套，并通过现有 `buildHostShellSkillMarkdown()` 生成 coarse broker-first copy；runner 中的环境变量和 exec 路径要固定为 `BROKER_CURRENT_HOST="opencode"`、读取 `.skills-broker.json` 里的 `brokerHome` 后执行 `${brokerHome}/bin/run-broker`；adapter 要像 Codex 一样只断言 `SKILL.md` 与 runner 存在，然后回传 broker JSON 结果。同步更新 `tests/hosts/host-shell-install.test.ts`，让它对 OpenCode shell 也断言 managed manifest、`invocationMode:"explicit"`、support/boundary wording 与“不要在入口层选 package/workflow/skill/MCP”文案；不要新建 OpenCode plugin 包装或独立 shell markdown 源。</action>
  <acceptance_criteria>
- `rg -n "installOpenCodeHostShell|runOpenCodeAdapter|BROKER_CURRENT_HOST=\\\"opencode\\\"|host: \\\"opencode\\\"|invocationMode: \\\"explicit\\\"" src/hosts/opencode/install.ts src/hosts/opencode/adapter.ts tests/hosts/host-shell-install.test.ts` 有命中。
- `npm test -- --run tests/hosts/host-shell-install.test.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/hosts/host-shell-install.test.ts</automated>
  </verify>
  <done>OpenCode 已经拥有一个真实、薄、复用 shared-home 的 host shell 安装面。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 把 OpenCode 接进 published update/remove/doctor path，但只做最小诚实 plumbing</name>
  <files>src/shared-home/update.ts, src/shared-home/remove.ts, src/shared-home/doctor.ts, tests/shared-home/update-lifecycle.test.ts, tests/shared-home/doctor.test.ts</files>
  <read_first>
.planning/phases/05-ship-the-opencode-thin-host-shell/05-CONTEXT.md
src/shared-home/update.ts
src/shared-home/remove.ts
src/shared-home/doctor.ts
src/shared-home/paths.ts
src/hosts/opencode/install.ts
tests/shared-home/update-lifecycle.test.ts
tests/shared-home/doctor.test.ts
  </read_first>
  <behavior>
    - Test 1: `updateSharedBrokerHome()` can install or repair the OpenCode shell through the same BROKER_HOSTS loop and `--opencode-dir` override path.
    - Test 2: `removeSharedBrokerHome()` and `doctorSharedBrokerHome()` can enumerate OpenCode host state without inventing OpenCode-only lifecycle semantics.
    - Test 3: doctor may report OpenCode as detected/not_detected/managed, but must not claim full familyProofs/proof-reuse parity beyond Phase 5 scope.
  </behavior>
  <action>在 `src/shared-home/update.ts` 中引入 `installOpenCodeHostShell`，把 host lifecycle option type 扩到 `opencodeInstallDirectory?: string`，并让 update 的 host loop、manual recovery targeting、planned-install output 都能覆盖 OpenCode。同步在 `src/shared-home/remove.ts`、`src/shared-home/doctor.ts` 中加入对应的 OpenCode install dir/host entry plumbing，使 `remove` 与 `doctor` 至少能识别 `.skills-broker.json` ownership、`--opencode-dir` override、managedHosts/hosts 列表中的 `opencode` 条目，以及“默认 root 未检测到时提示 `--opencode-dir`”的 reason string。测试中只锁“OpenCode 真实进入 published host inventory”，不要在本任务里新增 OpenCode-only `familyProofs`、proof/reuse counters、manual-recovery special casing 或新的 lifecycle verbs。</action>
  <acceptance_criteria>
- `rg -n "installOpenCodeHostShell|opencodeInstallDirectory|--opencode-dir|name: \\\"opencode\\\"|managedHosts" src/shared-home/update.ts src/shared-home/remove.ts src/shared-home/doctor.ts tests/shared-home/update-lifecycle.test.ts tests/shared-home/doctor.test.ts` 有命中。
- `npm test -- --run tests/shared-home/update-lifecycle.test.ts tests/shared-home/doctor.test.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/shared-home/update-lifecycle.test.ts tests/shared-home/doctor.test.ts</automated>
  </verify>
  <done>published lifecycle 已经知道如何 attach/识别/移除 OpenCode 壳，但没有越界把 Phase 6 parity 一起伪造出来。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published update path -> host shell attach | 如果 OpenCode 不能通过现有 `update` attach，它就仍然只是 deferred 叙事，不是 shipping 宿主 |
| minimal lifecycle visibility -> operator truth | 如果 doctor/remove 完全不知道 OpenCode，repo 会再次出现“支持矩阵已翻面但 lifecycle 不认识它”的断层 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | I | `src/hosts/opencode/*` | mitigate | 复用 Codex-style thin shell 和 managed manifest，禁止为 OpenCode 发明第二套壳层 contract。 |
| T-05-02-02 | R | `src/shared-home/update.ts`, `src/shared-home/doctor.ts`, `src/shared-home/remove.ts` | mitigate | 让 published lifecycle 至少诚实识别 OpenCode host state，但不越界伪造 Phase 6 parity。 |
</threat_model>

<verification>
1. 运行 `rg -n "installOpenCodeHostShell|runOpenCodeAdapter|BROKER_CURRENT_HOST=\\\"opencode\\\"|opencodeInstallDirectory|--opencode-dir|name: \\\"opencode\\\"" src/hosts/opencode/install.ts src/hosts/opencode/adapter.ts src/shared-home/update.ts src/shared-home/remove.ts src/shared-home/doctor.ts tests/hosts/host-shell-install.test.ts tests/shared-home/update-lifecycle.test.ts tests/shared-home/doctor.test.ts`
2. 运行 `npm test -- --run tests/hosts/host-shell-install.test.ts`
3. 运行 `npm test -- --run tests/shared-home/update-lifecycle.test.ts tests/shared-home/doctor.test.ts`
</verification>

<success_criteria>
- OpenCode 可以通过现有 `npx skills-broker update` attach 到 shared broker home。
- OpenCode shell 以 Codex-style thin shell 形态 shipping，且 `invocationMode` 固定为 `explicit`。
- lifecycle surface 至少诚实地知道 OpenCode 这个 host，但没有提前 overclaim full parity。
</success_criteria>

<output>
After completion, create `.planning/phases/05-ship-the-opencode-thin-host-shell/05-ship-the-opencode-thin-host-shell-02-SUMMARY.md`
</output>
