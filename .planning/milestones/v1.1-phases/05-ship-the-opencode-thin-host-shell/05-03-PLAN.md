---
phase: 05-ship-the-opencode-thin-host-shell
plan: 03
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - tests/e2e/shared-home-smoke.test.ts
  - tests/e2e/host-auto-routing-smoke.test.ts
autonomous: true
requirements:
  - HOST-01
  - HOST-02
must_haves:
  truths:
    - "三宿主必须继续复用同一个 shared broker home，而不是给 OpenCode 单独跑一套 runtime。"
    - "OpenCode installed shell 仍然只走 coarse broker-first boundary，`invocationMode` 保持 `explicit`。"
    - "Phase 5 的 no-regression 证明必须覆盖现有 Claude Code / Codex，而不是只新增 OpenCode happy path。"
  artifacts:
    - path: "tests/e2e/shared-home-smoke.test.ts"
      provides: "three-host shared-home attach and no-regression smoke"
    - path: "tests/e2e/host-auto-routing-smoke.test.ts"
      provides: "installed-shell routing/decline contract smoke for OpenCode"
  key_links:
    - from: "src/hosts/opencode/adapter.ts"
      to: "tests/e2e/shared-home-smoke.test.ts"
      via: "the new adapter should prove OpenCode can attach to the same broker home as the existing hosts"
      pattern: "runOpenCodeAdapter|currentHost"
    - from: "src/hosts/skill-markdown.ts"
      to: "tests/e2e/host-auto-routing-smoke.test.ts"
      via: "installed-shell routing outcomes must remain broker-first and explicit on OpenCode too"
      pattern: "invocationMode|INSTALL_REQUIRED|HANDOFF_READY|UNSUPPORTED_REQUEST"
---

<objective>
用真实 smoke proof 把 Phase 5 的核心承诺锁住：OpenCode 不是“新写一套壳就算完”，而是能和 Claude Code / Codex 一起挂到同一个 shared broker home 上，同时保持相同的 installed-shell routing/decline contract。

Purpose: 为 `HOST-01` / `HOST-02` 提供第三宿主的 end-to-end attach/no-regression 证据。
Output: three-host shared-home smoke、OpenCode installed-shell routing smoke、以及对现有双宿主不回归的证明。
</objective>

<execution_context>
@/Users/monkeyin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/monkeyin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ship-the-opencode-thin-host-shell/05-CONTEXT.md
@src/hosts/opencode/adapter.ts
@src/hosts/skill-markdown.ts
@tests/e2e/shared-home-smoke.test.ts
@tests/e2e/host-auto-routing-smoke.test.ts
@tests/e2e/workflow-host-smoke.test.ts

<interfaces>
Smoke should exercise the same envelope shape already used by existing hosts:
```ts
{
  requestText: "turn this webpage into markdown: https://example.com/article",
  host: "opencode",
  invocationMode: "explicit",
  urls: ["https://example.com/article"]
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 把 shared-home smoke 扩成三宿主 attach/no-regression proof</name>
  <files>tests/e2e/shared-home-smoke.test.ts</files>
  <read_first>
.planning/phases/05-ship-the-opencode-thin-host-shell/05-CONTEXT.md
tests/e2e/shared-home-smoke.test.ts
src/hosts/opencode/adapter.ts
src/shared-home/update.ts
src/shared-home/doctor.ts
  </read_first>
  <behavior>
    - Test 1: one shared broker home can install Claude Code, Codex, and OpenCode shells at the same time.
    - Test 2: OpenCode adapter returns broker results with `currentHost: "opencode"` while existing hosts still work unchanged.
    - Test 3: doctor/update smoke still reflects the existing hosts correctly after the third host is attached.
  </behavior>
  <action>在 `tests/e2e/shared-home-smoke.test.ts` 中把当前双宿主 smoke 扩成三宿主场景：同一个 temp `brokerHomeDirectory` 下安装 Claude Code、Codex、OpenCode 三个 shell；通过 `runOpenCodeAdapter()` 发出至少一条 `web markdown` 类 broker-first 请求，断言返回结果里的 `handoff.context.currentHost === "opencode"`，且 `doctor`/`update` 的 managed host truth 能看到第三宿主 attach 后的状态。保留并继续执行现有 Claude Code / Codex 断言，确保这不是“OpenCode 绿了，老宿主回归了”的伪通过；不要在这里引入 Phase 6 才需要的 proof/reuse 指标扩写。</action>
  <acceptance_criteria>
- `rg -n "runOpenCodeAdapter|host: \\\"opencode\\\"|invocationMode: \\\"explicit\\\"|currentHost\\).*opencode|managedHosts" tests/e2e/shared-home-smoke.test.ts` 有命中。
- `npm test -- --run tests/e2e/shared-home-smoke.test.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/e2e/shared-home-smoke.test.ts</automated>
  </verify>
  <done>shared-home smoke 已经证明 OpenCode 是挂在同一套 broker home 上的真实第三宿主，而不是一个旁路演示壳。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 扩充 installed-shell routing smoke，锁住 OpenCode 的 coarse boundary</name>
  <files>tests/e2e/host-auto-routing-smoke.test.ts</files>
  <read_first>
.planning/phases/05-ship-the-opencode-thin-host-shell/05-CONTEXT.md
tests/e2e/host-auto-routing-smoke.test.ts
src/hosts/skill-markdown.ts
src/hosts/opencode/adapter.ts
  </read_first>
  <behavior>
    - Test 1: OpenCode installed shell continues to return broker-owned routed outcomes such as `HANDOFF_READY` and `INSTALL_REQUIRED`.
    - Test 2: OpenCode keeps `invocationMode: "explicit"` and does not bypass into a host-native tool path.
    - Test 3: existing Claude Code / Codex routed and declined outcomes stay green in the same suite.
  </behavior>
  <action>在 `tests/e2e/host-auto-routing-smoke.test.ts` 中加入 OpenCode shell 的 installed-routing smoke，复用现有 routed/declined cases，例如 `web markdown` 的 `HANDOFF_READY`、capability discovery/install 的 `INSTALL_REQUIRED` 或 `NO_CANDIDATE`。测试里要显式传入 `host: "opencode"` 和 `invocationMode: "explicit"`，并断言 OpenCode 仍走 broker-owned outcome codes，而不是跳到 host-native fallback。保留现有 Claude Code / Codex case，一次性证明三宿主共享同一 coarse boundary contract；不要把本任务扩成 workflow phase 的 deep proof。</action>
  <acceptance_criteria>
- `rg -n "host: \\\"opencode\\\"|invocationMode: \\\"explicit\\\"|HANDOFF_READY|INSTALL_REQUIRED|NO_CANDIDATE|UNSUPPORTED_REQUEST" tests/e2e/host-auto-routing-smoke.test.ts` 有命中。
- `npm test -- --run tests/e2e/host-auto-routing-smoke.test.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/e2e/host-auto-routing-smoke.test.ts</automated>
  </verify>
  <done>OpenCode 的 installed-shell routing/decline contract 已经被 smoke 锁住，现有宿主也同时被回归验证。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared broker home -> three host shells | 如果 smoke 只验证 OpenCode 单独可跑，无法证明没有偷偷分叉第三套 runtime |
| installed shell -> broker-owned outcomes | 如果 OpenCode 路由结果和现有宿主不一致，thin-shell 边界会在 shipping 当天就漂移 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | R | `tests/e2e/shared-home-smoke.test.ts` | mitigate | 用同一 temp broker home 同时挂三宿主，证明第三宿主不是旁路 runtime。 |
| T-05-03-02 | I | `tests/e2e/host-auto-routing-smoke.test.ts` | mitigate | 把 OpenCode 纳入已有 routed/declined smoke，锁住 coarse broker-first contract。 |
</threat_model>

<verification>
1. 运行 `rg -n "runOpenCodeAdapter|host: \\\"opencode\\\"|invocationMode: \\\"explicit\\\"|HANDOFF_READY|INSTALL_REQUIRED|NO_CANDIDATE|currentHost" tests/e2e/shared-home-smoke.test.ts tests/e2e/host-auto-routing-smoke.test.ts`
2. 运行 `npm test -- --run tests/e2e/shared-home-smoke.test.ts`
3. 运行 `npm test -- --run tests/e2e/host-auto-routing-smoke.test.ts`
</verification>

<success_criteria>
- 三个宿主都能挂到同一个 shared broker home 上。
- OpenCode 的 installed-shell routed/declined outcomes 与现有宿主保持同一 coarse contract。
- 现有 Claude Code / Codex smoke 在第三宿主落地后继续保持绿色。
</success_criteria>

<output>
After completion, create `.planning/phases/05-ship-the-opencode-thin-host-shell/05-ship-the-opencode-thin-host-shell-03-SUMMARY.md`
</output>
