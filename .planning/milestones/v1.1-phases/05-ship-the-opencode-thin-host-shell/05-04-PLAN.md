---
phase: 05-ship-the-opencode-thin-host-shell
plan: 04
type: execute
wave: 4
depends_on:
  - "02"
  - "03"
files_modified:
  - src/core/operator-truth.ts
  - tests/shared-home/operator-truth-parity.test.ts
  - README.md
  - README.zh-CN.md
  - TODOS.md
  - STATUS.md
  - package.json
autonomous: true
requirements:
  - HOST-01
must_haves:
  truths:
    - "只要 OpenCode 的真实 attach/shipping path 已存在，support matrix 就必须翻面为 supported，而不是继续写 deferred。"
    - "truth flip 不能把 Phase 6 的 lifecycle/proof parity 偷渡成已完成，必须保留明确的后续 caveat。"
    - "published metadata、README 双语、TODOS、STATUS 和 installed shell copy 都要围绕同一份 operator truth 说话。"
  artifacts:
    - path: "src/core/operator-truth.ts"
      provides: "post-Phase-5 canonical operator truth contract"
    - path: "tests/shared-home/operator-truth-parity.test.ts"
      provides: "cross-surface parity proof for the new support matrix and Phase 6 caveat"
    - path: "README.md"
      provides: "English supported-host truth after OpenCode shipping"
    - path: "README.zh-CN.md"
      provides: "Chinese supported-host truth after OpenCode shipping"
    - path: "TODOS.md"
      provides: "backlog truth that moves OpenCode from deferred host to shipped host, while keeping parity work in Phase 6"
    - path: "STATUS.md"
      provides: "canonical repo status updated for the shipping third host"
    - path: "package.json"
      provides: "published package metadata aligned with the three-host truth"
  key_links:
    - from: "src/core/operator-truth.ts"
      to: "tests/shared-home/operator-truth-parity.test.ts"
      via: "the parity suite should read the new supported-host list and explicit Phase 6 caveat from one typed contract"
      pattern: "supportedHosts|deferredHosts|lifecycleCommands|OpenCode"
    - from: "src/core/operator-truth.ts"
      to: "README.md"
      via: "English docs and installed shell copy must move from deferred OpenCode wording to supported-host wording"
      pattern: "Supported now: Claude Code, Codex, OpenCode"
    - from: "src/core/operator-truth.ts"
      to: "README.zh-CN.md"
      via: "Chinese docs must mirror the same support matrix and Phase 6 caveat"
      pattern: "现在支持：Claude Code、Codex、OpenCode"
    - from: "src/core/operator-truth.ts"
      to: "STATUS.md"
      via: "repo status summary must stop claiming OpenCode is deferred once Phase 5 shipping lands"
      pattern: "OpenCode"
---

<objective>
在第三宿主 attach 真实存在之后，把 repo/operator-facing truth 从“OpenCode deferred”翻成“OpenCode supported”，同时明确把完整 lifecycle/proof parity 留在 Phase 6，而不是继续让实现与叙事错位。

Purpose: 完成 `HOST-01` 的 truth flip，让 support matrix 和 published metadata 与真实 shipping 状态一致。
Output: updated operator truth contract、parity tests、README 双语/TODOS/STATUS/package metadata 的三宿主 truth。
</objective>

<execution_context>
@/Users/monkeyin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/monkeyin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ship-the-opencode-thin-host-shell/05-CONTEXT.md
@src/core/operator-truth.ts
@tests/shared-home/operator-truth-parity.test.ts
@README.md
@README.zh-CN.md
@TODOS.md
@STATUS.md
@package.json

<interfaces>
Current truth:
```ts
supportedHosts: ["Claude Code", "Codex"];
deferredHosts: ["OpenCode"];
```

Target truth:
```ts
supportedHosts: ["Claude Code", "Codex", "OpenCode"];
deferredHosts: [];
```

Target caveat (exact idea, wording may be helper-backed):
```md
OpenCode thin host shell is shipping in Phase 5; lifecycle / proof parity continues in Phase 6.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 翻转 canonical operator truth，并把 Phase 6 caveat 收成 typed contract</name>
  <files>src/core/operator-truth.ts, tests/shared-home/operator-truth-parity.test.ts, package.json</files>
  <read_first>
.planning/phases/05-ship-the-opencode-thin-host-shell/05-CONTEXT.md
src/core/operator-truth.ts
tests/shared-home/operator-truth-parity.test.ts
package.json
  </read_first>
  <behavior>
    - Test 1: `supportedHosts` now includes OpenCode and `deferredHosts` no longer uses OpenCode as the deferred third host.
    - Test 2: parity tests assert an explicit Phase 6 caveat instead of the old deferred-host line.
    - Test 3: package metadata stops saying the product works only across Claude Code and Codex.
  </behavior>
  <action>在 `src/core/operator-truth.ts` 中把 canonical support matrix 改成 `supportedHosts: ["Claude Code", "Codex", "OpenCode"]`，并把 `deferredHosts` 清空；新增一条明确的 post-Phase-5 caveat helper，用于表达 “OpenCode thin host shell is shipping in Phase 5; lifecycle / proof parity continues in Phase 6.” 以及等价中文文案。同步更新 `tests/shared-home/operator-truth-parity.test.ts`，让它读取新的 support matrix 和 caveat，而不是继续要求 `Deferred but planned: OpenCode thin host shell`。同时更新 `package.json` 的 `description` 与 `keywords`，把 published metadata 从 “across Claude Code and Codex” 翻到三宿主 truth；不要把 OpenCode 写成已经完成 Phase 6 parity。</action>
  <acceptance_criteria>
- `rg -n "Claude Code\", \"Codex\", \"OpenCode\"|deferredHosts: \\[\\]|Phase 6|OpenCode thin host shell is shipping|现在支持：Claude Code、Codex、OpenCode|\"opencode\"" src/core/operator-truth.ts tests/shared-home/operator-truth-parity.test.ts package.json` 有命中。
- `npm test -- --run tests/shared-home/operator-truth-parity.test.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/shared-home/operator-truth-parity.test.ts</automated>
  </verify>
  <done>canonical operator truth 和 published metadata 已经反映 OpenCode shipping，同时没有偷报 Phase 6 完成。</done>
</task>

<task type="auto">
  <name>Task 2: 同步 README / README.zh-CN / TODOS / STATUS 到三宿主 truth</name>
  <files>README.md, README.zh-CN.md, TODOS.md, STATUS.md</files>
  <read_first>
.planning/phases/05-ship-the-opencode-thin-host-shell/05-CONTEXT.md
src/core/operator-truth.ts
tests/shared-home/operator-truth-parity.test.ts
README.md
README.zh-CN.md
TODOS.md
STATUS.md
  </read_first>
  <action>按 Task 1 的新 contract 更新 `README.md`、`README.zh-CN.md`、`TODOS.md`、`STATUS.md`，把旧的 OpenCode deferred wording 全部翻到“三宿主已支持”的 truth 上，但必须紧跟一个明确 caveat：OpenCode 在 Phase 5 拿到的是 thin host shell shipping，完整 lifecycle / proof parity 继续由 Phase 6 交付。`STATUS.md` 的 canonical JSON block 和 human summary 都要同步；`TODOS.md` 的 OpenCode backlog 项要从 “Add OpenCode as the third thin host shell” 转成 Phase 6 parity 或后续 host-templating backlog，而不是继续把已 shipping 的 host 写成待实现。不要只改英文 README，也不要让中文 README/STATUS/TODOS 继续保留旧 deferred note。</action>
  <acceptance_criteria>
- `rg -n "Supported now: Claude Code, Codex, OpenCode|现在支持：Claude Code、Codex、OpenCode|Phase 6|lifecycle / proof parity|OpenCode" README.md README.zh-CN.md TODOS.md STATUS.md` 有命中。
- `npm test -- --run tests/shared-home/operator-truth-parity.test.ts tests/shared-home/status.test.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/shared-home/operator-truth-parity.test.ts tests/shared-home/status.test.ts</automated>
  </verify>
  <done>repo docs/status/backlog 已经和真实的第三宿主 shipping 状态对齐，不再停在 deferred 叙事。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| real shipping attach -> public support matrix | 如果 OpenCode 已可安装但 docs/status 仍写 deferred，repo truth 会再次落后于实现 |
| Phase 5 support flip -> Phase 6 parity | 如果 support flip 不带 caveat，团队会误以为 doctor/remove/proof parity 已经完成 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | I | `src/core/operator-truth.ts`, `README*.md`, `STATUS.md`, `TODOS.md` | mitigate | 用单一 operator truth contract 和 parity tests 驱动 support flip。 |
| T-05-04-02 | R | `tests/shared-home/operator-truth-parity.test.ts` | mitigate | 明确检查三宿主 support matrix 与 Phase 6 caveat，防止把后续 parity 提前写成 done。 |
</threat_model>

<verification>
1. 运行 `rg -n "Claude Code, Codex, OpenCode|现在支持：Claude Code、Codex、OpenCode|Phase 6|lifecycle / proof parity|OpenCode" src/core/operator-truth.ts README.md README.zh-CN.md TODOS.md STATUS.md package.json tests/shared-home/operator-truth-parity.test.ts`
2. 运行 `npm test -- --run tests/shared-home/operator-truth-parity.test.ts`
3. 运行 `npm test -- --run tests/shared-home/operator-truth-parity.test.ts tests/shared-home/status.test.ts`
</verification>

<success_criteria>
- support matrix 已经从 “OpenCode deferred” 翻成三宿主 supported。
- Phase 6 caveat 被写进 operator truth，而不是只存在口头记忆里。
- README 双语、TODOS、STATUS、package metadata 与 canonical contract 一致。
</success_criteria>

<output>
After completion, create `.planning/phases/05-ship-the-opencode-thin-host-shell/05-ship-the-opencode-thin-host-shell-04-SUMMARY.md`
</output>
