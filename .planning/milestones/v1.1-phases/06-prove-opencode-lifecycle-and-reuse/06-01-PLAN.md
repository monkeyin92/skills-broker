---
phase: 06-prove-opencode-lifecycle-and-reuse
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared-home/remove.ts
  - tests/shared-home/remove.test.ts
  - tests/cli/lifecycle-cli.test.ts
  - tests/e2e/shared-home-smoke.test.ts
autonomous: true
requirements:
  - HOST-03
must_haves:
  truths:
    - "OpenCode 必须拿到与现有宿主同级的 published remove/update/doctor lifecycle path，而不是继续只活在 attach smoke 里。"
    - "remove parity 需要覆盖 managed remove、preserve、conflict truth，不允许为 OpenCode 保留隐式未测空洞。"
    - "本计划优先锁生命周期面，不在这里翻 docs truth；Phase 6 caveat 只在完整 parity 成立后统一移除。"
  artifacts:
    - path: "src/shared-home/remove.ts"
      provides: "OpenCode-aware remove lifecycle parity"
    - path: "tests/shared-home/remove.test.ts"
      provides: "OpenCode remove/preserve/conflict regression proof"
    - path: "tests/cli/lifecycle-cli.test.ts"
      provides: "published lifecycle CLI proof for `--opencode-dir` on remove/doctor"
    - path: "tests/e2e/shared-home-smoke.test.ts"
      provides: "shared-home lifecycle smoke that still holds after remove parity changes"
  key_links:
    - from: "src/shared-home/remove.ts"
      to: "tests/shared-home/remove.test.ts"
      via: "OpenCode remove parity must be locked with the same managed/conflict semantics as other hosts"
      pattern: "opencodeInstallDirectory|name: \"opencode\"|status: \"removed\""
    - from: "src/shared-home/remove.ts"
      to: "tests/cli/lifecycle-cli.test.ts"
      via: "published CLI should route `--opencode-dir` into remove/doctor without introducing a new command surface"
      pattern: "--opencode-dir|command).toBe(\"remove\")|command).toBe(\"doctor\")"
---

<objective>
把 OpenCode 补到 published lifecycle surface 的第一层 parity：`remove` / `doctor` / CLI option routing 都要和现有宿主保持同级可用、可测、可解释。

Purpose: 关闭 Phase 5 “OpenCode 已 attach 但 lifecycle 还没 fully symmetric” 的最明显缺口。
Output: OpenCode remove regression、CLI lifecycle regression、no-regression shared-home smoke。
</objective>

<execution_context>
@/Users/monkeyin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/monkeyin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-prove-opencode-lifecycle-and-reuse/06-CONTEXT.md
@src/shared-home/remove.ts
@tests/shared-home/remove.test.ts
@tests/cli/lifecycle-cli.test.ts
@tests/e2e/shared-home-smoke.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 锁住 OpenCode remove parity</name>
  <files>src/shared-home/remove.ts, tests/shared-home/remove.test.ts</files>
  <read_first>
.planning/phases/06-prove-opencode-lifecycle-and-reuse/06-CONTEXT.md
src/shared-home/remove.ts
tests/shared-home/remove.test.ts
  </read_first>
  <behavior>
    - Test 1: remove default path can remove a managed OpenCode shell while preserving shared broker home.
    - Test 2: `--reset-acquisition-memory` preserves a managed OpenCode shell just like other hosts.
    - Test 3: conflicting OpenCode directories still return `skipped_conflict` with a stable reason.
  </behavior>
  <action>补齐 `tests/shared-home/remove.test.ts` 的 OpenCode coverage，必要时微调 `src/shared-home/remove.ts` 以确保 OpenCode installDirectory resolution、managed manifest handling、preserve/remove/conflict semantics 与 Claude Code/Codex 完全同级。不要给 OpenCode 发明单独 result status，也不要更改 shared-home purge / acquisition-memory semantics。</action>
  <acceptance_criteria>
- `rg -n "opencodeInstallDirectory|name: \\\"opencode\\\"|status: \\\"removed\\\"|status: \\\"preserved\\\"|status: \\\"skipped_conflict\\\"" src/shared-home/remove.ts tests/shared-home/remove.test.ts` 有命中。
- `node ./node_modules/vitest/vitest.mjs --run tests/shared-home/remove.test.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs --run tests/shared-home/remove.test.ts</automated>
  </verify>
  <done>OpenCode 不再缺席 published remove parity。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 锁住 CLI lifecycle parity 与 no-regression smoke</name>
  <files>tests/cli/lifecycle-cli.test.ts, tests/e2e/shared-home-smoke.test.ts</files>
  <read_first>
.planning/phases/06-prove-opencode-lifecycle-and-reuse/06-CONTEXT.md
tests/cli/lifecycle-cli.test.ts
tests/e2e/shared-home-smoke.test.ts
src/bin/skills-broker.ts
  </read_first>
  <behavior>
    - Test 1: lifecycle CLI accepts `--opencode-dir` on doctor/remove paths with the same parsing contract as update.
    - Test 2: shared-home smoke remains green after lifecycle parity changes.
  </behavior>
  <action>在 `tests/cli/lifecycle-cli.test.ts` 增加 `--opencode-dir` 对 doctor/remove 的解析与 strict-path behavior proof；必要时只做最小 CLI plumbing 修正。同步重跑并必要时更新 `tests/e2e/shared-home-smoke.test.ts` 的 no-regression assertions，确保 OpenCode lifecycle parity 改动不会破坏现有三宿主 shared-home smoke。</action>
  <acceptance_criteria>
- `rg -n "--opencode-dir|opencode" tests/cli/lifecycle-cli.test.ts tests/e2e/shared-home-smoke.test.ts` 有命中。
- `node ./node_modules/vitest/vitest.mjs --run tests/cli/lifecycle-cli.test.ts tests/e2e/shared-home-smoke.test.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs --run tests/cli/lifecycle-cli.test.ts tests/e2e/shared-home-smoke.test.ts</automated>
  </verify>
  <done>published lifecycle CLI 对 OpenCode 已无路径级空洞，shared-home smoke 仍然保持绿色。</done>
</task>

</tasks>

<verification>
1. 运行 `rg -n "opencode|--opencode-dir" src/shared-home/remove.ts tests/shared-home/remove.test.ts tests/cli/lifecycle-cli.test.ts tests/e2e/shared-home-smoke.test.ts`
2. 运行 `node ./node_modules/vitest/vitest.mjs --run tests/shared-home/remove.test.ts`
3. 运行 `node ./node_modules/vitest/vitest.mjs --run tests/cli/lifecycle-cli.test.ts tests/e2e/shared-home-smoke.test.ts`
</verification>

<output>
After completion, create `.planning/phases/06-prove-opencode-lifecycle-and-reuse/06-prove-opencode-lifecycle-and-reuse-01-SUMMARY.md`
</output>
