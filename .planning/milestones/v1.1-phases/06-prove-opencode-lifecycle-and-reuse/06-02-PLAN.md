---
phase: 06-prove-opencode-lifecycle-and-reuse
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/shared-home/doctor.ts
  - src/shared-home/format.ts
  - tests/shared-home/doctor.test.ts
  - tests/e2e/shared-home-smoke.test.ts
  - tests/e2e/status-doctor-git.test.ts
autonomous: true
requirements:
  - HOST-03
must_haves:
  truths:
    - "OpenCode parity 必须体现在 shared proof/reuse state 上，而不只是 host inventory。"
    - "doctor 需要能够用同一份 proof rail 讲清楚 OpenCode 参与的 reuse evidence，但不分叉出 OpenCode-only counters。"
    - "至少一条跨宿主证据要证明 OpenCode 与现有宿主共享 verified downstream manifests 或 acquisition memory。"
  artifacts:
    - path: "src/shared-home/doctor.ts"
      provides: "OpenCode lifecycle/proof parity on the shared doctor surface"
    - path: "src/shared-home/format.ts"
      provides: "text output parity for any newly surfaced shared proof"
    - path: "tests/shared-home/doctor.test.ts"
      provides: "OpenCode doctor parity regression"
    - path: "tests/e2e/shared-home-smoke.test.ts"
      provides: "OpenCode cross-host reuse evidence against shared broker home"
    - path: "tests/e2e/status-doctor-git.test.ts"
      provides: "strict doctor no-regression after parity changes"
  key_links:
    - from: "src/shared-home/doctor.ts"
      to: "tests/shared-home/doctor.test.ts"
      via: "doctor parity must be proven at the shared JSON/text surface, not via private helper behavior"
      pattern: "managedHosts|verifiedDownstreamManifests|familyProofs|opencode"
    - from: "tests/e2e/shared-home-smoke.test.ts"
      to: "src/shared-home/doctor.ts"
      via: "OpenCode should become a real contributor or consumer of shared proof/reuse state"
      pattern: "cacheHit|cachedCandidateId|doctor --json|opencode"
---

<objective>
把 OpenCode 从 “doctor 认识这个 host” 推进到 “doctor 能通过 shared proof/reuse rails 证明这个 host 和现有宿主真的共享 evidence”。

Purpose: 满足 `HOST-03` 中最关键的 proof/reuse parity 条件。
Output: OpenCode doctor parity、cross-host proof/reuse evidence、strict doctor no-regression。
</objective>

<execution_context>
@/Users/monkeyin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/monkeyin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-prove-opencode-lifecycle-and-reuse/06-CONTEXT.md
@src/shared-home/doctor.ts
@src/shared-home/format.ts
@tests/shared-home/doctor.test.ts
@tests/e2e/shared-home-smoke.test.ts
@tests/e2e/status-doctor-git.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 让 OpenCode 进入 shared doctor proof / reuse truth</name>
  <files>src/shared-home/doctor.ts, src/shared-home/format.ts, tests/shared-home/doctor.test.ts</files>
  <read_first>
.planning/phases/06-prove-opencode-lifecycle-and-reuse/06-CONTEXT.md
src/shared-home/doctor.ts
src/shared-home/format.ts
tests/shared-home/doctor.test.ts
  </read_first>
  <behavior>
    - Test 1: doctor JSON/text can show OpenCode as part of shared proof/reuse state without inventing host-local counters.
    - Test 2: OpenCode participation in verified downstream manifests is visible through the same shared fields used by existing hosts.
    - Test 3: adoptionHealth / strict doctor semantics remain shared and stable.
  </behavior>
  <action>补齐 `tests/shared-home/doctor.test.ts` 的 OpenCode parity coverage，必要时调整 `src/shared-home/doctor.ts` 与 `src/shared-home/format.ts`，让 OpenCode 参与 shared `verifiedDownstreamManifests` / `familyProofs` / `adoptionHealth` truth 的呈现达到与现有宿主同级清晰度。实现时只允许沿用共享 field；不要增加 `opencodeProofs` 之类的新 surface。</action>
  <acceptance_criteria>
- `rg -n "opencode|verifiedDownstreamManifests|familyProofs|adoptionHealth" src/shared-home/doctor.ts src/shared-home/format.ts tests/shared-home/doctor.test.ts` 有命中。
- `node ./node_modules/vitest/vitest.mjs --run tests/shared-home/doctor.test.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs --run tests/shared-home/doctor.test.ts</automated>
  </verify>
  <done>doctor surface 已能用 shared proof rails 诚实呈现 OpenCode parity。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 补一条 OpenCode cross-host proof / strict-doctor no-regression 证据</name>
  <files>tests/e2e/shared-home-smoke.test.ts, tests/e2e/status-doctor-git.test.ts</files>
  <read_first>
.planning/phases/06-prove-opencode-lifecycle-and-reuse/06-CONTEXT.md
tests/e2e/shared-home-smoke.test.ts
tests/e2e/status-doctor-git.test.ts
  </read_first>
  <behavior>
    - Test 1: at least one e2e proof shows OpenCode sharing reuse evidence with Claude Code or Codex through the same shared broker home.
    - Test 2: strict doctor status flow stays green after OpenCode parity changes.
  </behavior>
  <action>在 `tests/e2e/shared-home-smoke.test.ts` 增强一条 OpenCode 与现有宿主共享 proof/reuse evidence 的断言，优先沿用已有 `website QA` 或 `web markdown` canonical loop。同步重跑 `tests/e2e/status-doctor-git.test.ts`，若 parity 改动影响 strict doctor / status surface，就补最小 no-regression 修正。</action>
  <acceptance_criteria>
- `rg -n "opencode|cacheHit|cachedCandidateId|doctor --json|managedHosts" tests/e2e/shared-home-smoke.test.ts tests/e2e/status-doctor-git.test.ts` 有命中。
- `node ./node_modules/vitest/vitest.mjs --run tests/e2e/shared-home-smoke.test.ts tests/e2e/status-doctor-git.test.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs --run tests/e2e/shared-home-smoke.test.ts tests/e2e/status-doctor-git.test.ts</automated>
  </verify>
  <done>Phase 6 拿到一条 shared proof/reuse e2e 证据，而不是只停在 unit-level parity。</done>
</task>

</tasks>

<verification>
1. 运行 `rg -n "opencode|verifiedDownstreamManifests|familyProofs|adoptionHealth|cacheHit|cachedCandidateId" src/shared-home/doctor.ts src/shared-home/format.ts tests/shared-home/doctor.test.ts tests/e2e/shared-home-smoke.test.ts tests/e2e/status-doctor-git.test.ts`
2. 运行 `node ./node_modules/vitest/vitest.mjs --run tests/shared-home/doctor.test.ts`
3. 运行 `node ./node_modules/vitest/vitest.mjs --run tests/e2e/shared-home-smoke.test.ts tests/e2e/status-doctor-git.test.ts`
</verification>

<output>
After completion, create `.planning/phases/06-prove-opencode-lifecycle-and-reuse/06-prove-opencode-lifecycle-and-reuse-02-SUMMARY.md`
</output>
