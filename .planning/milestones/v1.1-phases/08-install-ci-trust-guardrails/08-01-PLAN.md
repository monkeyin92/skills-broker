---
phase: 08-install-ci-trust-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dev/ci-trust.ts
  - scripts/ci-trust-report.mjs
  - package.json
  - .github/workflows/ci.yml
  - tests/dev/ci-trust.test.ts
autonomous: true
requirements:
  - QUAL-01
must_haves:
  truths:
    - "CI 必须输出一份 blind-spot / coverage budget report，明确列出支持宿主、maintained family、proven family、workflow 与关键 proof surface 的 coverage layer。"
    - "新增宿主、新增 family、新增 workflow 时，如果没有对应 proof layer，CI report 必须 fail closed。"
    - "`verify:local` 继续只回答本地环境健康与 build/test readiness，不和 CI trust gate 混成一套。"
  artifacts:
    - path: "src/dev/ci-trust.ts"
      provides: "dynamic inventory-driven blind-spot report and CLI"
    - path: "tests/dev/ci-trust.test.ts"
      provides: "fail-closed coverage budget tests and CI wiring assertions"
    - path: ".github/workflows/ci.yml"
      provides: "explicit CI trust-report execution"
  key_links:
    - from: "src/dev/ci-trust.ts"
      to: "src/core/types.ts"
      via: "supported host inventory should come from typed broker host truth"
      pattern: "BROKER_HOSTS"
    - from: "src/dev/ci-trust.ts"
      to: "src/core/maintained-broker-first.ts"
      via: "maintained family inventory should come from canonical maintained contract"
      pattern: "maintainedFamilies"
    - from: "src/dev/ci-trust.ts"
      to: "src/sources/host-skill-catalog.ts"
      via: "proven family and workflow inventory should come from managed host catalog"
      pattern: "proofFamily|workflow"
---

<objective>
把 blind-spot / coverage budget 变成 repo-native、动态 inventory 驱动的 CI 输出，并让它在新增高风险 surface 缺 proof 时 fail closed。

Purpose: 满足 `QUAL-01`，让 CI 能明确指出 runtime / proof surface 的覆盖缺口。
Output: CI trust report CLI、focused tests、CI workflow 接线。
</objective>

<execution_context>
@/Users/monkeyin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/monkeyin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-install-ci-trust-guardrails/08-CONTEXT.md
@src/dev/local-verification.ts
@src/core/types.ts
@src/core/maintained-broker-first.ts
@src/sources/host-skill-catalog.ts
@package.json
@.github/workflows/ci.yml
@tests/dev/local-verification.test.ts
@tests/shared-home/status.test.ts
@tests/cli/lifecycle-cli.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 生成动态 blind-spot / coverage budget report</name>
  <files>src/dev/ci-trust.ts, scripts/ci-trust-report.mjs, tests/dev/ci-trust.test.ts</files>
  <read_first>
.planning/phases/08-install-ci-trust-guardrails/08-CONTEXT.md
src/dev/local-verification.ts
src/core/types.ts
src/core/maintained-broker-first.ts
src/sources/host-skill-catalog.ts
tests/dev/local-verification.test.ts
  </read_first>
  <behavior>
    - Test 1: current repo can生成一份 no-blind-spot 的 trust report。
    - Test 2: 当 inventory 中出现新的 workflow / family / host，但 proof layers 没跟上时，report 会 fail closed。
    - Test 3: report 文本要明确列出 surface、risk、required layers 与缺口。
  </behavior>
  <action>新增 `src/dev/ci-trust.ts`，动态读取支持宿主、maintained families、proven families、workflow recipes，并基于 proof-layer budget 生成 blind-spot report。通过 `scripts/ci-trust-report.mjs` 暴露成 npm script。补 `tests/dev/ci-trust.test.ts`，锁住 current repo green path 与 fail-closed blind-spot 行为。</action>
  <acceptance_criteria>
- `node --import ./scripts/register-ts-node.mjs ./scripts/ci-trust-report.mjs` 成功输出 report。
- `node ./node_modules/vitest/vitest.mjs --run tests/dev/ci-trust.test.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs --run tests/dev/ci-trust.test.ts</automated>
  </verify>
  <done>CI blind-spot report 已经可以基于当前 repo truth 自动发现 coverage 缺口。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 把 blind-spot report 接进 CI 而不污染 verify:local</name>
  <files>package.json, .github/workflows/ci.yml, tests/dev/ci-trust.test.ts</files>
  <read_first>
.planning/phases/08-install-ci-trust-guardrails/08-CONTEXT.md
package.json
.github/workflows/ci.yml
tests/dev/ci-trust.test.ts
  </read_first>
  <behavior>
    - Test 1: CI workflow 明确运行 blind-spot report。
    - Test 2: local verify 仍然只跑本地预检 + build/test，不被改成第二套 CI trust orchestration。
    - Test 3: workflow wiring 如果被删掉，repo-native tests 会失败。
  </behavior>
  <action>在 `package.json` 增加 focused CI trust script，并把 `.github/workflows/ci.yml` 接成显式的 trust gate。通过 `tests/dev/ci-trust.test.ts` 断言 workflow 仍然保留这个 command，同时保持 `verify:local` scripts 语义不变。</action>
  <acceptance_criteria>
- `rg -n "ci:blind-spot|verify:local" package.json .github/workflows/ci.yml tests/dev/ci-trust.test.ts` 有命中。
- blind-spot gate 不要求修改 `verify:local` 的 build/test 契约。
  </acceptance_criteria>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs --run tests/dev/ci-trust.test.ts</automated>
  </verify>
  <done>blind-spot report 已经成为显式 CI gate，同时 local verify 继续保持本地职责。</done>
</task>

</tasks>

<verification>
1. 运行 `node --import ./scripts/register-ts-node.mjs ./scripts/ci-trust-report.mjs`
2. 运行 `node ./node_modules/vitest/vitest.mjs --run tests/dev/ci-trust.test.ts`
3. 运行 `rg -n "ci:blind-spot|blind-spot|coverage budget|verify:local" src/dev/ci-trust.ts scripts/ci-trust-report.mjs package.json .github/workflows/ci.yml tests/dev/ci-trust.test.ts`
</verification>

<output>
After completion, create `.planning/phases/08-install-ci-trust-guardrails/08-install-ci-trust-guardrails-01-SUMMARY.md`
</output>
