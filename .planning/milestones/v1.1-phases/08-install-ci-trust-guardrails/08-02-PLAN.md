---
phase: 08-install-ci-trust-guardrails
plan: 02
type: execute
wave: 1
depends_on:
  - 08-01
files_modified:
  - package.json
  - .github/workflows/ci.yml
  - README.md
  - README.zh-CN.md
  - STATUS.md
  - TODOS.md
  - tests/dev/ci-trust.test.ts
autonomous: true
requirements:
  - QUAL-02
must_haves:
  truths:
    - "README、README.zh-CN、STATUS、TODOS 与 installed shell truth 的 parity 检查必须作为显式 CI gate 运行，而不是埋在泛化测试里。"
    - "CI gate 继续复用 canonical operator truth contract 和现有 parity tests，不引入第二套 narrative source of truth。"
    - "repo 文档必须清楚区分 `verify:local` 与 CI trust guards 的职责。"
  artifacts:
    - path: "package.json"
      provides: "focused narrative parity npm script"
    - path: ".github/workflows/ci.yml"
      provides: "explicit narrative parity gate wiring"
    - path: "tests/dev/ci-trust.test.ts"
      provides: "workflow command retention assertion"
    - path: "README.md"
      provides: "english explanation of local verify vs CI trust guardrails"
    - path: "README.zh-CN.md"
      provides: "chinese explanation of local verify vs CI trust guardrails"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "tests/shared-home/operator-truth-parity.test.ts"
      via: "CI should run focused narrative parity instead of relying on accidental full-suite coverage"
      pattern: "operator-truth-parity"
    - from: ".github/workflows/ci.yml"
      to: "tests/hosts/host-shell-install.test.ts"
      via: "installed-shell truth should stay in the same parity gate"
      pattern: "host-shell-install"
---

<objective>
把 narrative parity 变成显式 CI gate，并把 local verify 与 CI trust guardrail 的分工写清楚。

Purpose: 满足 `QUAL-02`，让支持矩阵、hero lane / proven family hierarchy 与 lifecycle commands 的 drift 在 CI 中 fail closed。
Output: focused parity CI script、workflow wiring、repo docs truth 更新。
</objective>

<execution_context>
@/Users/monkeyin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/monkeyin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-install-ci-trust-guardrails/08-CONTEXT.md
@package.json
@.github/workflows/ci.yml
@tests/shared-home/operator-truth-parity.test.ts
@tests/hosts/host-shell-install.test.ts
@README.md
@README.zh-CN.md
@STATUS.md
@TODOS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建 focused narrative parity CI gate</name>
  <files>package.json, .github/workflows/ci.yml, tests/dev/ci-trust.test.ts</files>
  <read_first>
.planning/phases/08-install-ci-trust-guardrails/08-CONTEXT.md
package.json
.github/workflows/ci.yml
tests/shared-home/operator-truth-parity.test.ts
tests/hosts/host-shell-install.test.ts
tests/dev/ci-trust.test.ts
  </read_first>
  <behavior>
    - Test 1: CI 明确运行 narrative parity focused suite。
    - Test 2: suite 至少覆盖 README、README.zh-CN、STATUS、TODOS 与 installed shell truth。
    - Test 3: workflow wiring 漂移时，repo tests 能 fail closed。
  </behavior>
  <action>在 `package.json` 添加 focused parity script，运行 `tests/shared-home/operator-truth-parity.test.ts` 与 `tests/hosts/host-shell-install.test.ts`。把它接进 `.github/workflows/ci.yml` 的 trust gate，并由 `tests/dev/ci-trust.test.ts` 锁住 command 仍存在。</action>
  <acceptance_criteria>
- `npm run test:ci:narrative-parity` 可运行。
- `.github/workflows/ci.yml` 明确出现 `npm run test:ci:narrative-parity`。
  </acceptance_criteria>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs --run tests/dev/ci-trust.test.ts tests/shared-home/operator-truth-parity.test.ts tests/hosts/host-shell-install.test.ts</automated>
  </verify>
  <done>narrative parity 已成为 focused、显式的 CI truth gate。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 更新 repo truth，写清 local verify vs CI trust guardrails 分工</name>
  <files>README.md, README.zh-CN.md, STATUS.md, TODOS.md</files>
  <read_first>
.planning/phases/08-install-ci-trust-guardrails/08-CONTEXT.md
README.md
README.zh-CN.md
STATUS.md
TODOS.md
  </read_first>
  <behavior>
    - 文档明确说明 `verify:local` 是本地环境与 build/test readiness，不是 repo-truth gate。
    - 文档明确说明 CI trust guards 会跑 blind-spot report、narrative parity，以及 strict doctor/status truth。
    - STATUS/TODOS 从“准备补 CI guardrails”前推到“guardrails 已接入”。
  </behavior>
  <action>更新 README 双语、STATUS、TODOS，把当前产品阶段从“补 CI guardrails”前推成“已有 CI trust guardrails”，并补一小段说明本地 verify 与 CI trust gate 的职责边界。</action>
  <acceptance_criteria>
- `rg -n "ci:blind-spot|narrative-parity|verify:local|CI trust" README.md README.zh-CN.md STATUS.md TODOS.md` 有命中。
  </acceptance_criteria>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs --run tests/shared-home/operator-truth-parity.test.ts tests/hosts/host-shell-install.test.ts</automated>
  </verify>
  <done>repo docs 已经把 CI trust gate 的存在与职责边界说清楚。</done>
</task>

</tasks>

<verification>
1. 运行 `node ./node_modules/vitest/vitest.mjs --run tests/dev/ci-trust.test.ts tests/shared-home/operator-truth-parity.test.ts tests/hosts/host-shell-install.test.ts`
2. 运行 `npm run test:ci:narrative-parity`
3. 运行 `rg -n "ci:blind-spot|test:ci:narrative-parity|verify:local|Current product phase|当前产品阶段" package.json .github/workflows/ci.yml README.md README.zh-CN.md STATUS.md TODOS.md`
</verification>

<output>
After completion, create `.planning/phases/08-install-ci-trust-guardrails/08-install-ci-trust-guardrails-02-SUMMARY.md`
</output>
