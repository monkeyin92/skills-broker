---
phase: 09-lock-release-gate-verdicts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dev/release-truth.ts
  - scripts/release-truth.mjs
  - package.json
  - tests/dev/release-truth.test.ts
autonomous: true
requirements:
  - REL-01
  - REL-03
must_haves:
  truths:
    - "release gate 必须复用现有三条 trust rail，而不是新增 release-only evaluator。"
    - "`verify:local` 继续保持本地预检，不属于 ship/publish gate。"
  artifacts:
    - path: "src/dev/release-truth.ts"
      provides: "repo-owned release gate orchestration and verdict model"
    - path: "scripts/release-truth.mjs"
      provides: "repo-local CLI entrypoint for release gate"
    - path: "package.json"
      provides: "npm script surface for release gate"
    - path: "tests/dev/release-truth.test.ts"
      provides: "release gate verdict coverage"
---

<objective>
把现有 CI trust rails 收口成一条 repo-owned release gate，供 ship/publish 直接消费。

Purpose: 满足 `REL-01` 与 `REL-03`，把 blind-spot、narrative parity、strict doctor 统一成 release verdict，同时保持 `verify:local` 的职责边界不变。
Output: release gate orchestration、repo-local CLI/script、verdict tests。
</objective>

<context>
@.planning/milestones/v1.2-phases/09-lock-release-gate-verdicts/09-CONTEXT.md
@src/dev/ci-trust.ts
@src/bin/skills-broker.ts
@package.json
@tests/dev/ci-trust.test.ts
@tests/e2e/status-doctor-git.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 release gate orchestration</name>
  <files>src/dev/release-truth.ts, scripts/release-truth.mjs, package.json, tests/dev/release-truth.test.ts</files>
  <behavior>
    - Test 1: release gate 可以组合 blind-spot、narrative parity、strict doctor 三条 rail。
    - Test 2: 当任何一条 rail 失败时，verdict fail closed 并指出 failing rail。
    - Test 3: `verify:local` script 不被纳入 release gate surface。
  </behavior>
  <action>新增 repo-local release gate CLI，输出 text/json verdict，并通过 npm script 暴露给 ship/publish flow 复用。</action>
  <acceptance_criteria>
- `npm run release:gate -- --json` 能输出结构化 verdict。
- `tests/dev/release-truth.test.ts` 覆盖 green / failing rail / verify-local boundary。
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. 运行 `node --import ./scripts/register-ts-node.mjs ./scripts/release-truth.mjs gate --json`
2. 运行 `node ./node_modules/vitest/vitest.mjs --run tests/dev/release-truth.test.ts`
</verification>

<output>
After completion, create `.planning/milestones/v1.2-phases/09-lock-release-gate-verdicts/09-lock-release-gate-verdicts-01-SUMMARY.md`
</output>
