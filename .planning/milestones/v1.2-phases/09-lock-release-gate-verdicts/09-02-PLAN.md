---
phase: 09-lock-release-gate-verdicts
plan: 02
type: execute
wave: 1
depends_on:
  - 09-01
files_modified:
  - src/dev/release-truth.ts
  - tests/dev/release-truth.test.ts
  - README.md
  - README.zh-CN.md
autonomous: true
requirements:
  - REL-02
must_haves:
  truths:
    - "machine-readable verdict 必须包含 failing rail、shipping ref、remote freshness。"
    - "release gate 文案要清楚区分 repo-owned release truth 与 published lifecycle commands。"
  artifacts:
    - path: "src/dev/release-truth.ts"
      provides: "shipping-ref and remote-freshness diagnostics in verdict"
    - path: "README.md"
      provides: "release gate operator wording"
    - path: "README.zh-CN.md"
      provides: "release gate operator wording (zh-CN)"
---

<objective>
让 release gate 的输出对 automation 和 maintainer 都足够可读：失败 rail、shipping ref、remote freshness 一次说清。

Purpose: 满足 `REL-02`，避免 maintainer 还要回看 workflow log 才知道问题出在哪。
Output: enriched release verdict, diagnostics tests, repo docs wording.
</objective>

<context>
@.planning/milestones/v1.2-phases/09-lock-release-gate-verdicts/09-CONTEXT.md
@src/shared-home/status.ts
@tests/shared-home/status.test.ts
@tests/e2e/status-doctor-git.test.ts
@README.md
@README.zh-CN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 丰富 release verdict 诊断信息</name>
  <files>src/dev/release-truth.ts, tests/dev/release-truth.test.ts</files>
  <behavior>
    - Test 1: verdict 在 strict doctor 成功时暴露 evaluated shipping ref 与 remote freshness。
    - Test 2: strict doctor 失败时仍保留 enough diagnostics 供 maintainer 定位。
  </behavior>
  <action>把 shipping ref、remote freshness、strict issue summary 写入 release verdict，并补测试锁住 JSON shape。</action>
  <acceptance_criteria>
- release verdict JSON 包含 `shippingRef` 与 `remoteFreshness`。
- failing rail summary 能直接指出 strict doctor / narrative parity / blind-spot 哪一条失败。
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 更新 repo docs 对 release gate 的表述</name>
  <files>README.md, README.zh-CN.md</files>
  <behavior>
    - 文档明确 release gate 是 repo-owned flow，不扩大 published lifecycle commands。
  </behavior>
  <action>在 README 中补充 release gate 的 repo-local usage 与职责边界。</action>
  <acceptance_criteria>
- 中英 README 都提到 release gate 与 `verify:local` 的区别。
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. 运行 `node --import ./scripts/register-ts-node.mjs ./scripts/release-truth.mjs gate --json`
2. 运行 `node ./node_modules/vitest/vitest.mjs --run tests/dev/release-truth.test.ts`
3. 运行 `rg -n "release:gate|shippingRef|remoteFreshness" README.md README.zh-CN.md src/dev/release-truth.ts tests/dev/release-truth.test.ts`
</verification>

<output>
After completion, create `.planning/milestones/v1.2-phases/09-lock-release-gate-verdicts/09-lock-release-gate-verdicts-02-SUMMARY.md`
</output>
