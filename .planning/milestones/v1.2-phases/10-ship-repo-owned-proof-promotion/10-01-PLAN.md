---
phase: 10-ship-repo-owned-proof-promotion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dev/release-truth.ts
  - package.json
  - tests/dev/release-truth.test.ts
autonomous: true
requirements:
  - PROOF-01
must_haves:
  truths:
    - "promotion 只能升级 declared=shipped_local 且 evaluated=shipped_remote 的 canonical status item。"
    - "promotion flow 继续保持 repo-owned，不改 published lifecycle CLI 对外 contract。"
  artifacts:
    - path: "src/dev/release-truth.ts"
      provides: "repo-owned proof promotion command and result model"
    - path: "package.json"
      provides: "npm script surface for release:promote"
    - path: "tests/dev/release-truth.test.ts"
      provides: "eligible-promotion rewrite coverage"
---

<objective>
新增显式的 `release:promote` flow，让 maintainer 可以把 canonical `STATUS.md` 中 truly eligible 的 `shipped_local` 项升级成 `shipped_remote`。

Purpose: 满足 `PROOF-01`，结束人工补 shipped-remote truth 的流程。
Output: promotion command、repo-local npm script、successful rewrite tests。
</objective>

<context>
@.planning/milestones/v1.2-phases/10-ship-repo-owned-proof-promotion/10-CONTEXT.md
@src/dev/release-truth.ts
@src/shared-home/status.ts
@STATUS.md
@tests/shared-home/status.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 eligible-only proof promotion</name>
  <files>src/dev/release-truth.ts, package.json, tests/dev/release-truth.test.ts</files>
  <behavior>
    - Test 1: `release:promote` 只提升 declared=shipped_local 且 evaluated=shipped_remote 的 item。
    - Test 2: 成功 promotion 后 canonical `STATUS.md` block 被写回为 `shipped_remote`。
  </behavior>
  <action>在 repo-local release truth CLI 中新增 `promote` 子命令，并通过 `package.json` 暴露 `release:promote`。</action>
  <acceptance_criteria>
- `node --import ./scripts/register-ts-node.mjs ./scripts/release-truth.mjs promote --json` 能输出结构化结果。
- `tests/dev/release-truth.test.ts` 覆盖 successful promotion rewrite。
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. 运行 `node ./node_modules/vitest/vitest.mjs --run tests/dev/release-truth.test.ts`
2. 运行 `node --import ./scripts/register-ts-node.mjs ./scripts/release-truth.mjs promote --json`
</verification>

<output>
After completion, create `.planning/milestones/v1.2-phases/10-ship-repo-owned-proof-promotion/10-ship-repo-owned-proof-promotion-01-SUMMARY.md`
</output>
