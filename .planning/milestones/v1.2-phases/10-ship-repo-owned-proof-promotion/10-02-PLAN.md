---
phase: 10-ship-repo-owned-proof-promotion
plan: 02
type: execute
wave: 1
depends_on:
  - 10-01
files_modified:
  - src/dev/release-truth.ts
  - tests/dev/release-truth.test.ts
autonomous: true
requirements:
  - PROOF-02
must_haves:
  truths:
    - "ship ref unresolved、remote refresh failed、proof invalid、non-promotion mismatch 都必须阻止 promotion。"
    - "blocking 情况下 `STATUS.md` 不能被部分写回。"
  artifacts:
    - path: "src/dev/release-truth.ts"
      provides: "fail-closed blocking issue classification for proof promotion"
    - path: "tests/dev/release-truth.test.ts"
      provides: "refresh failure and blocking mismatch coverage"
---

<objective>
把 proof promotion 做成 fail-closed contract，而不是 best-effort 文档修补。

Purpose: 满足 `PROOF-02`，确保 unresolved ship ref、refresh failure、proof failure、non-promotion mismatch 都会阻断写回。
Output: blocking issue classification、no-write-on-failure tests。
</objective>

<context>
@.planning/milestones/v1.2-phases/10-ship-repo-owned-proof-promotion/10-CONTEXT.md
@src/shared-home/status.ts
@tests/shared-home/status.test.ts
@tests/e2e/status-doctor-git.test.ts
@src/dev/release-truth.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 锁住 promotion 的 fail-closed 边界</name>
  <files>src/dev/release-truth.ts, tests/dev/release-truth.test.ts</files>
  <behavior>
    - Test 1: remote refresh 失败时 promotion 返回 blocking issue 且不写文件。
    - Test 2: non-promotion mismatch 存在时 promotion 返回 blocking issue 且不写文件。
  </behavior>
  <action>基于 `evaluateStatusBoard()` 的 issue taxonomy 分类 blocking issue，并保证所有 blocking case 都 short-circuit 写回。</action>
  <acceptance_criteria>
- promotion JSON 输出包含 blocking issues。
- blocking 情况下 `STATUS.md` 保持原样。
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. 运行 `node ./node_modules/vitest/vitest.mjs --run tests/dev/release-truth.test.ts`
2. 运行 `node --import ./scripts/register-ts-node.mjs ./scripts/release-truth.mjs promote --json`
</verification>

<output>
After completion, create `.planning/milestones/v1.2-phases/10-ship-repo-owned-proof-promotion/10-ship-repo-owned-proof-promotion-02-SUMMARY.md`
</output>
