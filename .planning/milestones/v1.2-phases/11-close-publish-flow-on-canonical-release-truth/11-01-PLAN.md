---
phase: 11-close-publish-flow-on-canonical-release-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/publish-npm.yml
  - tests/dev/release-truth.test.ts
autonomous: true
requirements:
  - SHIP-01
must_haves:
  truths:
    - "publish workflow 必须直接复用 `release:gate` / `release:promote`，而不是引入第二套 release-only evaluator。"
    - "publish 成功后 canonical `STATUS.md` 要能通过 automation promotion 回写。"
  artifacts:
    - path: ".github/workflows/publish-npm.yml"
      provides: "canonical release truth wired into publish automation"
    - path: "tests/dev/release-truth.test.ts"
      provides: "publish workflow wiring assert"
---

<objective>
让 npm publish flow 直接建立在 canonical release truth 之上，而不是只跑 build/test/publish 三步。

Purpose: 满足 `SHIP-01`，把 publish automation 接到 Phase 9/10 的 repo-owned truth 上。
Output: publish workflow gate/promotion/closeout wiring and workflow assert.
</objective>

<context>
@.planning/milestones/v1.2-phases/11-close-publish-flow-on-canonical-release-truth/11-CONTEXT.md
@.github/workflows/publish-npm.yml
@src/dev/release-truth.ts
@tests/dev/release-truth.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 把 canonical release truth 接到 publish workflow</name>
  <files>.github/workflows/publish-npm.yml, tests/dev/release-truth.test.ts</files>
  <behavior>
    - Test 1: publish workflow 在 publish 前运行 `release:gate`。
    - Test 2: publish workflow 在 publish 后运行 `release:promote` 并回推 `STATUS.md`。
    - Test 3: publish workflow 最后再次运行 canonical release gate 做 closeout。
  </behavior>
  <action>更新 publish automation 以复用 `release:gate` / `release:promote`，并用 repo-native test 锁住 wiring。</action>
  <acceptance_criteria>
- `publish-npm.yml` 直接调用 repo-owned release truth commands。
- `tests/dev/release-truth.test.ts` 断言 workflow 包含 pre-publish gate、promotion 与 closeout。
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. 运行 `node ./node_modules/vitest/vitest.mjs --run tests/dev/release-truth.test.ts`
2. 运行 `rg -n "release:gate|release:promote|git push origin" .github/workflows/publish-npm.yml`
</verification>

<output>
After completion, create `.planning/milestones/v1.2-phases/11-close-publish-flow-on-canonical-release-truth/11-close-publish-flow-on-canonical-release-truth-01-SUMMARY.md`
</output>
