---
phase: 12-harden-website-qa-broker-first-hit-rate
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01-PLAN.md
files_modified:
  - src/broker/trace.ts
  - src/shared-home/doctor.ts
  - src/shared-home/format.ts
  - tests/broker/trace.test.ts
  - tests/shared-home/doctor.test.ts
autonomous: true
requirements:
  - ROUTE-03
must_haves:
  truths:
    - "routing evidence 必须复用现有 trace/doctor rail，而不是发明平行 telemetry。"
    - "route evidence 先服务 `website QA` hero lane，不提前泛化成 maintained-family 通用 schema。"
  artifacts:
    - path: "src/broker/trace.ts"
      provides: "trace-level hero-lane request attribution"
    - path: "src/shared-home/doctor.ts"
      provides: "repo-owned website QA routing evidence summary"
    - path: "src/shared-home/format.ts"
      provides: "operator-visible website QA routing summary text output"
---

<objective>
把 `website QA` 的 hit / misroute / fallback / host-skip 证据直接 surfaced 到 repo-owned trace + doctor surface，避免 maintainer 手工读 jsonl。

Purpose: 满足 `ROUTE-03`，让默认入口的路由可信度可以被 repo-native 证据直接审计。
Output: trace hero-lane attribution、doctor `website QA` routing summary、对应测试覆盖。
</objective>

<context>
@.planning/milestones/v1.3-phases/12-harden-website-qa-broker-first-hit-rate/12-CONTEXT.md
@src/broker/trace.ts
@src/shared-home/doctor.ts
@src/shared-home/format.ts
@tests/broker/trace.test.ts
@tests/shared-home/doctor.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 给 routing trace 增加 hero-lane attribution</name>
  <files>src/broker/trace.ts, tests/broker/trace.test.ts</files>
  <behavior>
    - Test 1: direct QA、QA install-help、web/social markdown 请求在 trace schema 里能区分各自 proof family。
    - Test 2: 旧 trace 仍能被读取，不因为新增字段破坏 summarize contract。
  </behavior>
  <action>在 trace 生成阶段补充请求所属 hero/proof lane 信息，并保持旧 trace 兼容。</action>
  <acceptance_criteria>
- 新 trace 字段能为 doctor 提供 `website QA` 路由归因。
- `summarizeBrokerRoutingTraces()` 现有 surface / contract 汇总不回归。
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在 doctor 输出里增加 website QA routing evidence</name>
  <files>src/shared-home/doctor.ts, src/shared-home/format.ts, tests/shared-home/doctor.test.ts</files>
  <behavior>
    - Test 1: doctor JSON 直接输出 `website QA` observed / hit / misroute / fallback / host-skip 汇总。
    - Test 2: text output 直接显示 `website QA` routing summary，不需要 operator 自己解析 raw trace file。
    - Test 3: 当没有 `website QA` trace 时，summary 仍给出 fail-closed 的空状态。
  </behavior>
  <action>复用 persisted routing traces 产出 `website QA` 专用 route evidence，并把它放进 doctor text/json surface。</action>
  <acceptance_criteria>
- `doctor --json` 包含可机器读取的 `website QA` route evidence。
- `formatLifecycleResult()` 的 text 输出会把 `website QA` routing summary 讲清楚。
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. 运行 `node ./node_modules/vitest/vitest.mjs --run tests/broker/trace.test.ts tests/shared-home/doctor.test.ts`
2. 如需端到端确认，再运行 `node ./node_modules/vitest/vitest.mjs --run tests/e2e/host-auto-routing-smoke.test.ts`
</verification>

<output>
After completion, create `.planning/milestones/v1.3-phases/12-harden-website-qa-broker-first-hit-rate/12-harden-website-qa-broker-first-hit-rate-02-SUMMARY.md`
</output>
