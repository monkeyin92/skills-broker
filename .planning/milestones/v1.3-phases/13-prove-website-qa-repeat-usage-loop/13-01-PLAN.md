---
phase: 13-prove-website-qa-repeat-usage-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/broker-flow.test.ts
  - tests/e2e/shared-home-smoke.test.ts
  - tests/cli/lifecycle-cli.test.ts
autonomous: true
requirements:
  - REUSE-01
  - REUSE-02
must_haves:
  truths:
    - "website QA canonical proof 必须覆盖三宿主 shared-home surface，而不是只停在单宿主 smoke。"
    - "proof loop 不能只证明 install 之后成功一次；至少还要有 repeat usage evidence。"
    - "宿主仍然只做 coarse broker-first boundary，proof 不得回退到 host-side winner selection。"
  artifacts:
    - path: "tests/integration/broker-flow.test.ts"
      provides: "repo-owned website QA install / verify / reuse proof loop"
    - path: "tests/e2e/shared-home-smoke.test.ts"
      provides: "published shared-home / doctor surface parity on the shipped host shells"
    - path: "tests/cli/lifecycle-cli.test.ts"
      provides: "CLI doctor JSON contract for website QA proof loop"
---

<objective>
把 `website QA` 的 canonical proof 从“install 后成功一次”推进到“install 后还能重复成功，并跨宿主复用”。

Purpose: 满足 `REUSE-01` 与 `REUSE-02`，证明 QA-first 不是一次性 demo，而是可重复的 shared-home loop。
Output: strengthened integration / CLI proof coverage for the three-host website QA loop.
</objective>

<context>
@.planning/milestones/v1.3-phases/13-prove-website-qa-repeat-usage-loop/13-CONTEXT.md
@src/broker/acquisition-memory.ts
@tests/integration/broker-flow.test.ts
@tests/e2e/shared-home-smoke.test.ts
@tests/cli/lifecycle-cli.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 把 canonical website QA proof 扩到 repeat usage</name>
  <files>tests/integration/broker-flow.test.ts</files>
  <behavior>
    - Test 1: website QA 先走 `INSTALL_REQUIRED -> install -> rerun`。
    - Test 2: proof loop 继续覆盖至少一个跨宿主复用路径，而不是停在第一次 verified handoff。
    - Test 3: acquisition memory 会记录成功路由次数、first reuse 与 verified hosts。
  </behavior>
  <action>扩展 website QA integration proof，让 repo 能证明 install 之后还有 repeat usage / cross-host reuse evidence。</action>
  <acceptance_criteria>
- website QA integration test 覆盖 canonical install / verify / reuse loop。
- acquisition memory assert 明确验证 `successfulRoutes`、`firstReuseAt` 与 `verifiedHosts`。
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 让 published CLI / shared-home smoke 继续消费同一份 proof contract</name>
  <files>tests/e2e/shared-home-smoke.test.ts, tests/cli/lifecycle-cli.test.ts</files>
  <behavior>
    - Test 1: `doctor --json` 继续输出 website QA loop contract，而不是只剩 text-only 证据。
    - Test 2: published/shared-home smoke 仍证明三宿主共享同一份 doctor / acquisition memory truth。
  </behavior>
  <action>同步 CLI / smoke asserts，锁住 website QA proof loop 的 JSON contract 与 shared-home parity。</action>
  <acceptance_criteria>
- CLI test 不会因为 website QA proof shape 更新而回归。
- shared-home smoke 继续能消费 acquisition memory / family proof truth。
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. 运行 `node ./node_modules/vitest/vitest.mjs --run tests/integration/broker-flow.test.ts`
2. 运行 `node ./node_modules/vitest/vitest.mjs --run tests/e2e/shared-home-smoke.test.ts tests/cli/lifecycle-cli.test.ts`
</verification>

<output>
After completion, create `.planning/milestones/v1.3-phases/13-prove-website-qa-repeat-usage-loop/13-prove-website-qa-repeat-usage-loop-01-SUMMARY.md`
</output>
