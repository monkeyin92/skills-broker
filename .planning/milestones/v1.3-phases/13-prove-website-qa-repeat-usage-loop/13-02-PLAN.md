---
phase: 13-prove-website-qa-repeat-usage-loop
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01-PLAN.md
files_modified:
  - src/shared-home/doctor.ts
  - src/shared-home/format.ts
  - tests/shared-home/doctor.test.ts
  - tests/cli/lifecycle-cli.test.ts
autonomous: true
requirements:
  - REUSE-03
must_haves:
  truths:
    - "repeat usage 与 cross-host reuse 必须在 doctor surface 上明确分开，不能再互相借字段。"
    - "proof surface 继续复用 acquisition memory 与 verified downstream manifests，而不是引入平行 proof store。"
    - "doctor 必须指出 website QA loop 下一步缺的是什么 proof。"
  artifacts:
    - path: "src/shared-home/doctor.ts"
      provides: "aligned repeat-usage / cross-host reuse proof states for website QA"
    - path: "src/shared-home/format.ts"
      provides: "operator-visible doctor wording for repeat usage vs cross-host reuse"
    - path: "tests/shared-home/doctor.test.ts"
      provides: "repo-owned proof-surface regression coverage"
---

<objective>
把 `doctor`、acquisition memory 与 verified downstream manifests 对齐成同一份 website QA repeat-usage proof story。

Purpose: 满足 `REUSE-03`，让 maintainer 能直接看出 verify、repeat usage、cross-host reuse 各自状态和下一步缺口。
Output: aligned doctor JSON/text proof surface plus regression coverage.
</objective>

<context>
@.planning/milestones/v1.3-phases/13-prove-website-qa-repeat-usage-loop/13-CONTEXT.md
@src/shared-home/doctor.ts
@src/shared-home/format.ts
@src/broker/acquisition-memory.ts
@tests/shared-home/doctor.test.ts
@tests/cli/lifecycle-cli.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 doctor proof surface 上拆开 repeat usage 与 cross-host reuse</name>
  <files>src/shared-home/doctor.ts, tests/shared-home/doctor.test.ts</files>
  <behavior>
    - Test 1: repeat usage 和 cross-host reuse 有独立的 state / proof flags / recorded counters。
    - Test 2: 当 repeat usage 已 confirmed 但仍未跨宿主时，doctor 仍会提示 cross-host reuse，而不是误报 proven。
    - Test 3: unreadable rails 仍然 fail closed，并在 proof flags 上保持一致。
  </behavior>
  <action>更新 doctor proof summary，让 phase、proofs、state 与 nextAction 对齐 repeat usage / cross-host reuse 的真实语义。</action>
  <acceptance_criteria>
- `DoctorFamilyProofSummary` 同时表达 repeat usage 与 cross-host reuse。
- `doctor` 在不同 proof 缺口下能给出正确的 phase / nextAction。
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 锁定 doctor text / CLI JSON 的 operator contract</name>
  <files>src/shared-home/format.ts, tests/shared-home/doctor.test.ts, tests/cli/lifecycle-cli.test.ts</files>
  <behavior>
    - Test 1: text output 明确区分 verify、repeat usage、cross-host reuse。
    - Test 2: CLI `doctor --json` 继续输出机器可读的 website QA proof loop contract。
    - Test 3: acquisition proof wording 不再把 repeat usage 和 cross-host reuse 混成一个字段故事。
  </behavior>
  <action>补齐 format/CLI asserts，让 operator-facing doctor wording 与 JSON proof shape 一起 fail closed。</action>
  <acceptance_criteria>
- `formatLifecycleResult()` 会明确输出 repeat-usage proof 与 cross-host reuse proof。
- CLI / unit tests 对新增 proof 字段和文案保持稳定。
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. 运行 `node ./node_modules/vitest/vitest.mjs --run tests/shared-home/doctor.test.ts tests/cli/lifecycle-cli.test.ts`
2. 运行 `node ./node_modules/typescript/bin/tsc -p tsconfig.build.json`
</verification>

<output>
After completion, create `.planning/milestones/v1.3-phases/13-prove-website-qa-repeat-usage-loop/13-prove-website-qa-repeat-usage-loop-02-SUMMARY.md`
</output>
