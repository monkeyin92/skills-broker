---
phase: 14-lock-qa-first-operator-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/operator-truth.ts
  - README.md
  - README.zh-CN.md
  - STATUS.md
  - TODOS.md
  - src/hosts/skill-markdown.ts
autonomous: true
requirements:
  - TRUTH-01
  - TRUTH-02
must_haves:
  truths:
    - "`website QA` 继续是单一 default-entry hero lane；web markdown 仍是 second proven family，social markdown 仍是 next proven family。"
    - "宿主只解释 coarse broker-first boundary，不能在入口处挑具体 QA winner。"
    - "operator-facing wording 要能反映当前 doctor/runtime truth，而不是停在旧 milestone 的故事里。"
  artifacts:
    - path: "src/core/operator-truth.ts"
      provides: "canonical QA-first operator-truth phrasing"
    - path: "README.md"
      provides: "english QA-first story with current proof/boundary wording"
    - path: "README.zh-CN.md"
      provides: "chinese QA-first story with current proof/boundary wording"
    - path: "STATUS.md"
      provides: "repo-native current-state summary for Phase 12/13/14 sequencing"
    - path: "TODOS.md"
      provides: "current milestone focus and remaining operator-truth work"
---

<objective>
把 README、README.zh-CN、generated host shell、`STATUS.md` 与 `TODOS.md` 对齐成同一份 QA-first operator story。

Purpose: 满足 `TRUTH-01` 与 `TRUTH-02`，让 QA-first hero lane、family hierarchy 与 coarse broker-first boundary 继续讲一份真话。
Output: aligned bilingual docs, status/backlog surfaces, and canonical operator-truth phrasing.
</objective>

<context>
@.planning/milestones/v1.3-phases/14-lock-qa-first-operator-truth/14-CONTEXT.md
@src/core/operator-truth.ts
@src/hosts/skill-markdown.ts
@README.md
@README.zh-CN.md
@STATUS.md
@TODOS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 对齐 QA-first hero lane、proof story 与 coarse boundary wording</name>
  <files>src/core/operator-truth.ts, README.md, README.zh-CN.md, src/hosts/skill-markdown.ts</files>
  <behavior>
    - Test 1: README、README.zh-CN 与 generated host shell 都明确 website QA 是唯一 default-entry hero lane。
    - Test 2: operator-facing wording 解释宿主只决定 `broker_first` / `handle_normally` / `clarify_before_broker`，broker 仍选 concrete QA winner。
    - Test 3: README 双语会反映 `doctor` 当前的 routing evidence 与 repeat-usage / cross-host reuse proof story。
  </behavior>
  <action>抽 canonical QA-first phrasing 并回写到 README 双语与 generated host shell copy。</action>
  <acceptance_criteria>
- QA-first hierarchy 在英文、中文、host shell 上一致。
- coarse boundary explanation 在这些 surface 上不漂移。
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 把当前 milestone truth 写回 STATUS 与 TODOS</name>
  <files>STATUS.md, TODOS.md</files>
  <behavior>
    - Test 1: `TODOS.md` 当前焦点不再停在 v1.2 release truth。
    - Test 2: `STATUS.md` human summary / canonical board 可以读出 Phase 12/13 的 QA-first routing + repeat-usage truth。
  </behavior>
  <action>更新 repo-native status/backlog surfaces，让它们讲当前 milestone 剩余工作和已经 landed 的 QA-first runtime truth。</action>
  <acceptance_criteria>
- `TODOS.md` 反映 v1.3 Phase 14 的当前 focus。
- `STATUS.md` 不再缺失 Phase 12/13 的 QA-first state。
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. 运行 `node ./node_modules/vitest/vitest.mjs --run tests/shared-home/operator-truth-parity.test.ts tests/hosts/host-shell-install.test.ts`
2. 运行 `node ./node_modules/vitest/vitest.mjs --run tests/e2e/shared-home-smoke.test.ts`
</verification>

<output>
After completion, create `.planning/milestones/v1.3-phases/14-lock-qa-first-operator-truth/14-lock-qa-first-operator-truth-01-SUMMARY.md`
</output>
