---
phase: 14-lock-qa-first-operator-truth
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01-PLAN.md
files_modified:
  - tests/shared-home/operator-truth-parity.test.ts
  - tests/hosts/host-shell-install.test.ts
  - tests/e2e/shared-home-smoke.test.ts
  - src/dev/ci-trust.ts
autonomous: true
requirements:
  - TRUTH-03
must_haves:
  truths:
    - "QA-first wording 与 coarse boundary truth 的漂移必须被 repo-native guardrails 抓住。"
    - "guardrails 继续复用现有 parity / CI trust rail，不引入平行 checker。"
    - "host shell / README / README.zh-CN / STATUS / TODOS 的 current truth 要能一起 fail closed。"
  artifacts:
    - path: "tests/shared-home/operator-truth-parity.test.ts"
      provides: "cross-surface QA-first wording parity coverage"
    - path: "tests/e2e/shared-home-smoke.test.ts"
      provides: "published host-shell copy parity on the shared-home surface"
    - path: "src/dev/ci-trust.ts"
      provides: "CI snippet guardrails for the current operator truth"
---

<objective>
把 QA-first operator story 收成 fail-closed 的 repo-native guardrail，而不是继续靠人工 review。

Purpose: 满足 `TRUTH-03`，让 bilingual/operator truth 与 proof wording 的漂移会在 parity tests 或 CI trust rail 中直接爆红。
Output: updated parity/host-shell/CI guardrails for the Phase 14 wording contract.
</objective>

<context>
@.planning/milestones/v1.3-phases/14-lock-qa-first-operator-truth/14-CONTEXT.md
@tests/shared-home/operator-truth-parity.test.ts
@tests/hosts/host-shell-install.test.ts
@tests/e2e/shared-home-smoke.test.ts
@src/dev/ci-trust.ts
@src/core/operator-truth.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 parity tests 去锁 QA-first 当前故事</name>
  <files>tests/shared-home/operator-truth-parity.test.ts, tests/hosts/host-shell-install.test.ts, tests/e2e/shared-home-smoke.test.ts</files>
  <behavior>
    - Test 1: README、README.zh-CN、TODOS、STATUS 必须包含新的 coarse-boundary 解释与 QA-first proof wording。
    - Test 2: generated host shell copy 继续保持相同 hierarchy / boundary truth。
    - Test 3: shared-home smoke 证明 published shell copy 仍复用同一份 operator truth。
  </behavior>
  <action>更新 parity 与 host-shell assertions，把 current operator story 变成 repo-owned test contract。</action>
  <acceptance_criteria>
- docs/status/todos drift 会在 parity suite 爆红。
- host-shell copy drift 会在 host-shell / smoke tests 爆红。
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 更新 CI trust snippets 以覆盖新的 operator truth</name>
  <files>src/dev/ci-trust.ts</files>
  <behavior>
    - Test 1: narrative parity CI rail 要求 docs/status/todos 都包含新的 QA-first canonical snippets。
    - Test 2: CI trust 不会继续只验证支持矩阵和 lifecycle commands，而忽略 Phase 12/13 landed truth。
  </behavior>
  <action>扩展 CI trust doc snippets，把 coarse boundary 与 QA-first proof surface 纳入现有 narrative guardrail。</action>
  <acceptance_criteria>
- `ci:blind-spot` / `test:ci:narrative-parity` 继续复用原有 rail 但能抓 Phase 14 drift。
- 没有新增平行 checker。
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. 运行 `node ./node_modules/vitest/vitest.mjs --run tests/shared-home/operator-truth-parity.test.ts tests/hosts/host-shell-install.test.ts tests/e2e/shared-home-smoke.test.ts`
2. 运行 `node ./node_modules/typescript/bin/tsc -p tsconfig.build.json`
</verification>

<output>
After completion, create `.planning/milestones/v1.3-phases/14-lock-qa-first-operator-truth/14-lock-qa-first-operator-truth-02-SUMMARY.md`
</output>
