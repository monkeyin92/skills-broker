---
phase: 15-quantify-website-qa-adoption-signals
plan: 01
type: execute
autonomous: true
requirements:
  - ADOPT-01
  - ADOPT-02
files_modified:
  - src/shared-home/doctor.ts
  - tests/shared-home/doctor.test.ts
---

# Plan 15-01

## Objective

定义 `websiteQaAdoption` packet，把 recent routing / freshness / per-host coverage 从现有 proof rails 汇总成 repo-owned JSON contract。

## Tasks

1. 在 `doctor.ts` 增加 `DoctorWebsiteQaAdoptionSignal` 类型与聚合逻辑。
2. 从 routing traces 提取历史/近期 timestamps 与 per-host coverage。
3. 从 acquisition memory 与 verified downstream manifests 提取 recent/latest verify/reuse/replay timestamps。
4. 在 `tests/shared-home/doctor.test.ts` 锁定 active/stale/missing JSON contract。

## Verification

- `node ./node_modules/typescript/bin/tsc -p tsconfig.build.json --noEmit`
- `node ./node_modules/vitest/vitest.mjs --run tests/shared-home/doctor.test.ts`

---
*Archived with milestone v1.4*
