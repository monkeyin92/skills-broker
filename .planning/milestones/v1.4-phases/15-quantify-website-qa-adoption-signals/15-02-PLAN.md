---
phase: 15-quantify-website-qa-adoption-signals
plan: 02
type: execute
autonomous: true
requirements:
  - ADOPT-01
  - ADOPT-02
files_modified:
  - src/shared-home/format.ts
  - tests/shared-home/doctor.test.ts
---

# Plan 15-02

## Objective

把 `websiteQaAdoption` surfaced 到 `doctor` 文本输出，确保 maintainer 不用手工读 raw traces 就能看到 current signal 与 refresh action。

## Tasks

1. 在 `format.ts` 增加 adoption packet summary / latest / host breakdown / next-action lines。
2. 保持 `websiteQaLoop`、`websiteQaRouting` 顺序与旧 contract 兼容。
3. 在 doctor 文本断言里加入 adoption packet wording 与排序。

## Verification

- `node ./node_modules/vitest/vitest.mjs --run tests/shared-home/doctor.test.ts`

---
*Archived with milestone v1.4*
