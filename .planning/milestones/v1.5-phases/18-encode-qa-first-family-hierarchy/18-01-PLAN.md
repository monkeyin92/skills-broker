---
phase: 18-encode-qa-first-family-hierarchy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/operator-truth.ts
  - src/shared-home/format.ts
  - README.md
  - README.zh-CN.md
  - STATUS.md
  - TODOS.md
  - src/hosts/skill-markdown.ts
autonomous: true
requirements:
  - LOOP-01
  - LOOP-02
  - LOOP-03
must_haves:
  truths:
    - "`website QA` 继续是唯一第一步，`web markdown` 与 `social markdown` 是第二、第三条 proven loop。"
    - "operator-facing surface 只能给 family-level next-loop guidance，不能让宿主或文案偷选具体 downstream winner。"
    - "`doctor` / docs / host shell 应该让 operator 一眼看懂 post-QA next loop，而不是自己去拼层级。"
  artifacts:
    - path: "src/core/operator-truth.ts"
      provides: "canonical QA-first family hierarchy phrasing"
    - path: "src/shared-home/format.ts"
      provides: "doctor text surface for QA-first family-loop guidance"
    - path: "README.md"
      provides: "english QA-first family-loop story"
    - path: "README.zh-CN.md"
      provides: "chinese QA-first family-loop story"
    - path: "src/hosts/skill-markdown.ts"
      provides: "generated host shell hierarchy and next-loop guidance"
    - path: "STATUS.md"
      provides: "repo-native current-state summary for v1.5 hierarchy work"
    - path: "TODOS.md"
      provides: "backlog/current-focus wording aligned to the family-loop story"
---

<objective>
把 QA-first family hierarchy 收成一份 canonical operator story，并让 `doctor`、README 双语、generated host shell、`STATUS.md` 与 `TODOS.md` 共享这份 story。

Purpose: 满足 `LOOP-01`、`LOOP-02`、`LOOP-03`，让 operator 不靠多份文档或 traces 也能看懂 “先 website QA，再 web markdown，再 social markdown”。
Output: aligned canonical wording, doctor text guidance, and repo docs/status surfaces.
</objective>

<context>
@.planning/milestones/v1.5-phases/18-encode-qa-first-family-hierarchy/18-CONTEXT.md
@src/core/operator-truth.ts
@src/shared-home/format.ts
@src/hosts/skill-markdown.ts
@README.md
@README.zh-CN.md
@STATUS.md
@TODOS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 提升 QA-first hierarchy 与 next-loop guidance 为 canonical wording</name>
  <files>src/core/operator-truth.ts, README.md, README.zh-CN.md, src/hosts/skill-markdown.ts</files>
  <behavior>
    - Test 1: README、README.zh-CN 与 generated host shell 都明确 `website QA` 是唯一第一步，`web markdown`、`social markdown` 是第二、第三条 proven loop。
    - Test 2: next-loop guidance 只停留在 family level，不让宿主或文案选具体 downstream winner。
    - Test 3: operator 能从这些 surfaces 直接看懂 post-QA 下一条建议 loop。
  </behavior>
  <action>抽取或扩展 canonical hierarchy phrasing，并同步回写到双语 README 与 generated host shell copy。</action>
  <acceptance_criteria>
- hierarchy wording 在英文、中文、host shell 上一致。
- “next loop” guidance 不会突破 coarse broker-first boundary。
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 把 hierarchy guidance 接进 doctor / status / backlog surfaces</name>
  <files>src/shared-home/format.ts, STATUS.md, TODOS.md</files>
  <behavior>
    - Test 1: `doctor` text surface 能直接表达 QA-first family hierarchy 或 post-QA next-loop guidance。
    - Test 2: `STATUS.md` 与 `TODOS.md` 会反映当前 v1.5 hierarchy work，而不是只停留在 v1.4 adoption packet story。
  </behavior>
  <action>给 `doctor` 文本输出加入 hierarchy guidance，并更新 repo-native status/backlog surfaces 让它们讲当前 milestone 的 family-loop story。</action>
  <acceptance_criteria>
- operator 无需读多份 docs 或 traces 也能从 repo-owned surfaces 读到下一条 loop。
- `STATUS.md` / `TODOS.md` 与 milestone v1.5 当前 focus 对齐。
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. 运行 `node ./node_modules/vitest/vitest.mjs --run tests/shared-home/operator-truth-parity.test.ts tests/hosts/host-shell-install.test.ts tests/shared-home/doctor.test.ts tests/cli/lifecycle-cli.test.ts`
2. 运行 `node ./node_modules/vitest/vitest.mjs --run tests/e2e/shared-home-smoke.test.ts`
</verification>

<output>
After completion, create `.planning/milestones/v1.5-phases/18-encode-qa-first-family-hierarchy/18-encode-qa-first-family-hierarchy-01-SUMMARY.md`
</output>
