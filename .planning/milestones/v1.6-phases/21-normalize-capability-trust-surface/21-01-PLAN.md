---
phase: 21-normalize-capability-trust-surface
plan: 01
type: execute
wave: 1
files_modified:
  - src/broker/capability-trust.ts
  - src/broker/explain.ts
  - src/sources/mcp-registry.ts
  - src/broker/downstream-manifest-source.ts
  - src/dev/capability-trust-check.ts
  - scripts/capability-trust-check.mjs
  - package.json
  - tests/broker/capability-trust.test.ts
  - tests/sources/mcp-registry.test.ts
autonomous: true
requirements:
  - CAP-01
  - CAP-02
  - CAP-03
must_haves:
  truths:
    - Candidate provenance labels distinguish installed local skills, verified downstream manifests, MCP registry advisory candidates, and broker-owned workflows.
    - Registry/downstream metadata degradation is deterministic and fail-closed.
    - Advisory registry candidates cannot outrank verified installed winners without proof.
---

<objective>
Normalize capability trust metadata and add a repo-owned check for registry/downstream manifest trust.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Add reusable capability trust summaries</name>
  <files>src/broker/capability-trust.ts, src/broker/explain.ts</files>
  <acceptance_criteria>
- Explanation output includes provenance, eligibility, install state, install requirement, and trust reasons.
- Provenance labels cover installed local skill, verified downstream manifest, MCP registry advisory, broker-owned workflow, acquisition memory, and unknown candidates.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Propagate registry and downstream metadata</name>
  <files>src/sources/mcp-registry.ts, src/broker/downstream-manifest-source.ts</files>
  <acceptance_criteria>
- MCP registry candidates carry `discoverySource: mcp_registry` plus validated registry metadata.
- Verified downstream manifest replay candidates carry verified host and verified manifest timestamp.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Add repo-owned capability trust check</name>
  <files>src/dev/capability-trust-check.ts, scripts/capability-trust-check.mjs, package.json, tests/broker/capability-trust.test.ts</files>
  <acceptance_criteria>
- `npm run ci:capability-trust` passes on the repo seed catalogs.
- Tests fail on missing registry metadata, stale downstream manifests, or advisory-over-verified precedence regressions.
  </acceptance_criteria>
</task>

</tasks>
