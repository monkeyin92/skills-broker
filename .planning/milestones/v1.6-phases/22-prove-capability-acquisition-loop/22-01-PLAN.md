---
phase: 22-prove-capability-acquisition-loop
plan: 01
type: execute
wave: 1
files_modified:
  - src/broker/capability-growth.ts
  - src/broker/acquisition-memory.ts
  - src/broker/result.ts
  - src/broker/run.ts
  - src/shared-home/doctor.ts
  - src/shared-home/format.ts
  - tests/broker/acquisition-memory.test.ts
  - tests/integration/broker-flow.test.ts
  - tests/shared-home/doctor.test.ts
autonomous: true
requirements:
  - ACQ-01
  - ACQ-02
  - ACQ-03
must_haves:
  truths:
    - Broker results expose an auditable capability growth stage and next action.
    - Shared-home acquisition memory separates verification success, repeat usage, cross-host reuse, degraded, and failed outcomes.
    - Doctor surfaces generalized capability acquisition outcomes without regressing website QA proof.
---

<objective>
Prove the capability acquisition loop across result payloads, memory, and doctor surfaces.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Add capability growth payloads</name>
  <files>src/broker/capability-growth.ts, src/broker/result.ts, src/broker/run.ts</files>
  <acceptance_criteria>
- `INSTALL_REQUIRED` results expose stage `install_required` and next action `install`.
- successful handoffs expose `verified_handoff` on first success and `reused_capability` on later success.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Extend acquisition memory outcomes</name>
  <files>src/broker/acquisition-memory.ts, tests/broker/acquisition-memory.test.ts</files>
  <acceptance_criteria>
- Memory entries include explicit outcome summaries.
- Older entries without `outcomes` are read and normalized rather than discarded.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Expose doctor acquisition next actions</name>
  <files>src/shared-home/doctor.ts, src/shared-home/format.ts, tests/shared-home/doctor.test.ts</files>
  <acceptance_criteria>
- Doctor JSON/text reports verification successes, repeat usage, degraded/failed counts, and next action.
- Website QA-specific acquisition proof remains intact.
  </acceptance_criteria>
</task>

</tasks>
