---
status: passed
milestone: v1.7 Demand-Guided Capability Growth Health
audited: 2026-04-24
---

# Milestone 1.7 Audit

## Verdict

**PASSED.** v1.7 achieved its definition of done: demand-guided capability growth health is now derived from existing proof rails, surfaced through `doctor`, and locked into docs, installed host shell wording, canonical `STATUS.md`, and CI/parity rails without weakening the coarse broker-first boundary.

## Scope

- Phase 24: Surface Demand-Backed Growth Backlog
- Phase 25: Prove Capability Health And Readiness
- Phase 26: Lock Demand-Guided Operator Truth

## Requirements Coverage

| Requirement | Phase | Status | Evidence |
|-------------|-------|--------|----------|
| DEMAND-01 | 24 | satisfied | `src/broker/capability-demand.ts`; `tests/broker/capability-demand.test.ts` |
| DEMAND-02 | 24 | satisfied | deterministic opportunity states: proven/speculative/blocked/stale/ready/satisfied |
| DEMAND-03 | 24 | satisfied | focused tests cover blocked, stale, ready, and deduped evidence |
| HEALTH-01 | 25 | satisfied | `DoctorLifecycleResult.capabilityGrowthHealth` and doctor text output |
| HEALTH-02 | 25 | satisfied | health summary consumes routing traces and acquisition memory; no new telemetry store |
| HEALTH-03 | 25 | satisfied | doctor tests prove blocked and stale states while existing QA/family-loop tests remain green |
| TRUST-01 | 26 | satisfied | README, README.zh-CN, STATUS, TODOS, and host shell wording share one canonical line |
| TRUST-02 | 26 | satisfied | operator truth parity, host shell, and CI trust specs require demand-guided wording and `capabilityGrowthHealth` |
| TRUST-03 | 26 | satisfied | `release:gate` passes after strict doctor host-surface remediation; no release mechanics changed |

## Cross-Phase Integration

- Phase 24 demand aggregation feeds Phase 25 doctor health directly through `summarizeCapabilityDemand()`.
- Phase 25 `capabilityGrowthHealth` is consumed by Phase 26 operator truth and CI trust rails.
- Host shell remains coarse: generated shell wording still limits host decisions to `broker_first` / `handle_normally` / `clarify_before_broker` and does not select concrete winners.
- Existing QA-first adoption and family-loop packets remain intact and tested.

## E2E / Operator Flow Audit

Operator can now run `skills-broker doctor` and see:

1. Demand-backed capability opportunities.
2. Whether each opportunity is blocked, stale, ready for promotion, or already satisfied locally.
3. Broker-owned next actions such as verify, refresh metadata, promote, or prefer verified winner.
4. The same story repeated in docs, STATUS, TODOs, generated host shell wording, and CI trust checks.

## Automated Validation Reviewed

- `npm test -- --run` — passed, 50 files / 416 tests.
- `npm run ci:blind-spot` — passed, 14/14 surfaces green.
- `npm run release:gate` — passed after repairing local host-visible peer skill placement.
- `npm run build` — passed.

## Critical Gaps

None.

## Tech Debt And Non-Blocking Notes

- Two lifecycle CLI integration tests needed higher explicit timeouts because full-suite execution can exceed 30 seconds on this machine while invoking ts-node child processes.
- Local shared-home strict doctor was blocked by host-visible `baoyu-url-to-markdown` peer skills; the built-in repair migrated Claude Code automatically, and the differing Codex host-visible copy was moved to a timestamped shared-home backup before strict doctor went green.

## Final Assessment

v1.7 closes the immediate post-v1.6 bottleneck. Capability growth is no longer just a trusted candidate/acquisition story; it now has demand, freshness, blocked action, promotion-readiness, and satisfied-winner health surfaced through repo-owned truth rails.
