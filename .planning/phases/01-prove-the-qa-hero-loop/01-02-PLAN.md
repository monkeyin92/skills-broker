---
phase: 01-prove-the-qa-hero-loop
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/broker/query-compiler.ts
  - tests/core/request-normalization.test.ts
  - tests/e2e/phase1-website-qa-eval.test.ts
  - tests/fixtures/phase1-website-qa-eval.json
autonomous: true
requirements:
  - ROUTE-01
must_haves:
  truths:
    - "只有显式 website QA 意图加网站目标才会进入 QA hero lane。"
    - "模糊页面/链接请求不会为了提高命中率被误吞进 QA hero lane。"
    - "QA miss 会稳定落在 host_selection、broker_normalization、retrieval、prepare 之一。"
  artifacts:
    - path: "src/broker/query-compiler.ts"
      provides: "precision-first QA query synthesis"
    - path: "tests/core/request-normalization.test.ts"
      provides: "QA positive/negative normalization proof"
    - path: "tests/e2e/phase1-website-qa-eval.test.ts"
      provides: "Phase 1 QA miss-layer eval harness"
    - path: "tests/fixtures/phase1-website-qa-eval.json"
      provides: "QA miss-layer fixture matrix"
  key_links:
    - from: "src/broker/query-compiler.ts"
      to: "tests/core/request-normalization.test.ts"
      via: "explicit QA synthesis and negative-boundary assertions"
      pattern: "quality_assurance|qa_report|AMBIGUOUS_REQUEST|UNSUPPORTED_REQUEST"
    - from: "tests/fixtures/phase1-website-qa-eval.json"
      to: "tests/e2e/phase1-website-qa-eval.test.ts"
      via: "host_selection / broker_normalization / retrieval / prepare coverage"
      pattern: "host_selection|broker_normalization|retrieval|prepare"
---

<objective>
把 website QA 的语义边界收紧到 precision-first：只让明确 QA 意图的请求进入 QA lane，并用 eval harness 证明所有 miss 都能被归因。

Purpose: 按 D-06、D-07、D-08 落实 ROUTE-01 的前半段，确保“未安装赢家时返回 INSTALL_REQUIRED”建立在正确的 QA 归一化之上，而不是靠扩大 hero lane 语义边界。
Output: QA compiler heuristics、unit regression、Phase 1 miss-layer eval fixture 与 harness。
</objective>

<execution_context>
@/Users/monkeyin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/monkeyin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-prove-the-qa-hero-loop/01-CONTEXT.md
@.planning/phases/01-prove-the-qa-hero-loop/01-PATTERNS.md
@src/broker/query-compiler.ts
@tests/core/request-normalization.test.ts
@tests/e2e/phase1-website-qa-eval.test.ts
@tests/fixtures/phase1-website-qa-eval.json
@docs/superpowers/plans/2026-04-17-semantic-coarse-boundary-hardening-implementation.md

<interfaces>
From src/broker/query-compiler.ts:
```ts
function buildWebsiteQaCapabilityQuery(
  input: BrokerEnvelope,
  url: string | undefined
): CapabilityQuery;

export function compileEnvelopeRequest(
  input: BrokerEnvelope
): CompileEnvelopeRequestResult;
```

Current QA synthesis contract:
```ts
jobFamilies: ["quality_assurance"];
targets: [{ type: "website", value: url }];
artifacts: ["qa_report"];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 收紧 QA compiler 语义边界</name>
  <files>src/broker/query-compiler.ts, tests/core/request-normalization.test.ts</files>
  <read_first>
.planning/phases/01-prove-the-qa-hero-loop/01-CONTEXT.md
src/broker/query-compiler.ts
tests/core/request-normalization.test.ts
.planning/phases/01-prove-the-qa-hero-loop/01-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: 明确 QA 请求加网站目标会合成 `jobFamilies: ["quality_assurance"]`、`targets: [{type:"website"}]`、`artifacts: ["qa_report"]`。
    - Test 2: “check this page / 看下这个页面 / convert this page to pdf” 这类 QA-neighbor 请求不会被编进 QA lane。
    - Test 3: markdown、requirements、investigation 现有 lane 不因 QA 边界调整而回归。
  </behavior>
  <action>按 D-06 与 D-08 在 `src/broker/query-compiler.ts` 保持 precision-first：只有显式 QA 意图 + 网站目标的请求才能走 `buildWebsiteQaCapabilityQuery`，并继续排除 markdown 与非 markdown conversion 目标。先在 `tests/core/request-normalization.test.ts` 写清正例与反例，再最小修改 compiler 以通过测试。不要为了抬高 hero lane 命中率新增模糊 page/link alias，也不要把 capability discovery 或普通页面理解吞进 QA lane。</action>
  <acceptance_criteria>
- `rg -n "quality_assurance|qa_report" src/broker/query-compiler.ts tests/core/request-normalization.test.ts` 能看到 QA synthesis contract。
- `rg -n "check this page|看下这个页面|check this website" tests/core/request-normalization.test.ts` 能看到 QA negative cases。
- `npm test -- --run tests/core/request-normalization.test.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/core/request-normalization.test.ts</automated>
  </verify>
  <done>query compiler 只把明确 QA 意图请求编进 quality_assurance，且 unit tests 覆盖正例、负例与已有相邻 lane 不回归。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 锁住 QA miss-layer eval 证明面</name>
  <files>tests/e2e/phase1-website-qa-eval.test.ts, tests/fixtures/phase1-website-qa-eval.json</files>
  <read_first>
.planning/phases/01-prove-the-qa-hero-loop/01-CONTEXT.md
tests/e2e/phase1-website-qa-eval.test.ts
tests/fixtures/phase1-website-qa-eval.json
src/broker/query-compiler.ts
tests/core/request-normalization.test.ts
  </read_first>
  <behavior>
    - Test 1: eval fixture 至少覆盖 `host_selection`、`broker_normalization`、`retrieval`、`prepare` 四层。
    - Test 2: retrieval miss 仍然是显式 QA wording 下的 `INSTALL_REQUIRED` / retrieval proof，而不是误分类。
    - Test 3: eval harness 对每个 case 都校验 `resultCode` 与 `missLayer`，并收集全量四层覆盖。
  </behavior>
  <action>按 D-06、D-07、D-08 扩展 `tests/fixtures/phase1-website-qa-eval.json` 与 `tests/e2e/phase1-website-qa-eval.test.ts`，确保 Phase 1 QA eval fixture 继续只关注 website QA hero lane，并把 host_selection、broker_normalization、retrieval、prepare 四层 miss 都显式建模出来。retrieval case 必须体现“同一类 QA wording 在赢家未安装时进入 retrieval / INSTALL_REQUIRED 证据”，而不是 generic no-candidate。不要往这个 harness 里混入 web markdown 或 family 泛化案例。</action>
  <acceptance_criteria>
- `rg -n "\"host_selection\"|\"broker_normalization\"|\"retrieval\"|\"prepare\"" tests/fixtures/phase1-website-qa-eval.json` 返回四层 miss。
- `rg -n "attributes every maintained website QA miss to a Phase 1 routing layer" tests/e2e/phase1-website-qa-eval.test.ts` 保持 phase-specific harness 名称。
- `npm test -- --run tests/e2e/phase1-website-qa-eval.test.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/e2e/phase1-website-qa-eval.test.ts</automated>
  </verify>
  <done>Phase 1 eval harness 能把 QA miss 稳定归因到四个 routing layer，并保持 QA hero lane 的 precision-first proof。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw user request -> broker compiler | 模糊请求若被误判进 QA lane，会触发错误安装/复用路径 |
| eval fixture -> regression suite | 漏掉某个 miss layer 会让 QA hero loop 的失败原因不可观测 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | T | `src/broker/query-compiler.ts` | mitigate | 用 explicit QA intent + website target 双条件和负例单测锁住 precision-first 边界，避免把模糊 page/link 请求篡改成 QA lane。 |
| T-01-02-02 | R | `tests/e2e/phase1-website-qa-eval.test.ts` | mitigate | 用固定四层 miss fixture 和断言保证每次回归都能归因，不允许 QA fail path 无法解释。 |
</threat_model>

<verification>
1. 运行 `npm test -- --run tests/core/request-normalization.test.ts`
2. 运行 `npm test -- --run tests/e2e/phase1-website-qa-eval.test.ts`
</verification>

<success_criteria>
- 显式 QA 请求仍会被合成为 `quality_assurance` + `qa_report`。
- 模糊页面/链接请求不会被误送到 QA hero lane。
- Phase 1 eval harness 保持四层 miss 归因覆盖。
</success_criteria>

<output>
After completion, create `.planning/phases/01-prove-the-qa-hero-loop/01-prove-the-qa-hero-loop-02-SUMMARY.md`
</output>
