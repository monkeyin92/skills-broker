---
phase: 02-generalize-family-proofs
plan: 03
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - tests/integration/broker-flow.test.ts
  - src/hosts/skill-markdown.ts
  - tests/hosts/host-shell-install.test.ts
  - README.md
  - README.zh-CN.md
  - TODOS.md
  - STATUS.md
autonomous: true
requirements:
  - ENTRY-03
  - ROUTE-04
  - GROW-01
must_haves:
  truths:
    - "Phase 2 的产品叙事必须是 one hero lane, two proven loops: website QA 仍然是唯一 first move，web markdown 是第二条 proven family。"
    - "web markdown 必须有和 QA 类似的 INSTALL_REQUIRED -> rerun -> cross-host reuse canonical proof，不靠文案声称。"
    - "repo docs、installed shell copy、backlog/status truth 只能把 web markdown 讲成 second proven family，不能和 website QA 并列争夺默认入口。"
  artifacts:
    - path: "tests/integration/broker-flow.test.ts"
      provides: "web markdown canonical proof loop and semantic trace evidence"
    - path: "src/hosts/skill-markdown.ts"
      provides: "installed shell wording hierarchy: hero lane first, secondary maintained lanes after"
    - path: "tests/hosts/host-shell-install.test.ts"
      provides: "installed shell wording/order regression"
    - path: "README.md"
      provides: "English operator-facing second-proven-family narrative"
    - path: "README.zh-CN.md"
      provides: "Chinese operator-facing second-proven-family narrative"
    - path: "TODOS.md"
      provides: "backlog truth updated after Phase 2 growth item ships"
    - path: "STATUS.md"
      provides: "repo-native proof board mirrors shipped Phase 2 truth"
  key_links:
    - from: "tests/integration/broker-flow.test.ts"
      to: "README.md"
      via: "operator-visible web markdown proof claims must be backed by canonical INSTALL_REQUIRED -> rerun -> cross-host reuse evidence"
      pattern: "web markdown INSTALL_REQUIRED -> install -> rerun -> cross-host reuse|second proven family"
    - from: "src/hosts/skill-markdown.ts"
      to: "tests/hosts/host-shell-install.test.ts"
      via: "installed shell copy keeps website QA hero lane first and web markdown secondary"
      pattern: "### Hero lane: website QA|### Secondary maintained lanes"
    - from: "README.md"
      to: "README.zh-CN.md"
      via: "same hierarchy and lifecycle wording in both languages"
      pattern: "second proven family|第二条已经证明"
---

<objective>
把 web markdown 作为第二条 proven family 真正“发布”为 operator truth，同时维持 website QA 是唯一 first move 的产品层级。

Purpose: 在 Plan 01 / 02 完成 canonical proof surface 与 semantic boundary 之后，用 integration proof、installed shell copy、README / STATUS / TODOS parity 一起满足 `ENTRY-03`、`ROUTE-04`、`GROW-01` 的产品化落点。
Output: canonical web markdown proof loop regression、installed shell wording parity、repo-native second-proven-family truth。
</objective>

<execution_context>
@/Users/monkeyin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/monkeyin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-generalize-family-proofs/02-CONTEXT.md
@.planning/phases/02-generalize-family-proofs/02-PATTERNS.md
@.planning/phases/01-prove-the-qa-hero-loop/01-VERIFICATION.md
@tests/integration/broker-flow.test.ts
@src/hosts/skill-markdown.ts
@tests/hosts/host-shell-install.test.ts
@README.md
@README.zh-CN.md
@TODOS.md
@STATUS.md

<interfaces>
From src/hosts/skill-markdown.ts:
```ts
export function buildHostShellSkillMarkdown(
  options: HostShellSkillMarkdownOptions
): string;
```

From tests/integration/broker-flow.test.ts:
```ts
it("proves web markdown INSTALL_REQUIRED -> install -> rerun -> cross-host reuse", async () => {
  ...
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 把 web markdown canonical proof loop 锁成第二条 proven family done-bar</name>
  <files>tests/integration/broker-flow.test.ts</files>
  <read_first>
.planning/phases/02-generalize-family-proofs/02-CONTEXT.md
.planning/phases/02-generalize-family-proofs/02-PATTERNS.md
.planning/phases/01-prove-the-qa-hero-loop/01-VERIFICATION.md
tests/integration/broker-flow.test.ts
src/broker/run.ts
src/shared-home/doctor.ts
  </read_first>
  <behavior>
    - Test 1: 同一句 web markdown 请求先得到 `INSTALL_REQUIRED`，安装后得到 `HANDOFF_READY`。
    - Test 2: 同一句请求从另一个 host 重跑时仍是 `HANDOFF_READY`，并记录 cross-host reuse。
    - Test 3: persisted traces 里保留 `semanticMatchReason` / `semanticMatchCandidateId` / `semanticMatchProofFamily`，证明这是 second proven family 的 canonical path，而不是 accidental winner selection。
  </behavior>
  <action>按 D-04、D-05、D-09 在 `tests/integration/broker-flow.test.ts` 把 `proves web markdown INSTALL_REQUIRED -> install -> rerun -> cross-host reuse` 作为 Phase 2 的 canonical proof test 继续补强：固定同一句 request、固定相同 package / leaf identity、固定 `semanticMatchReason: "direct_route"` 与 `semanticMatchProofFamily: "web_content_to_markdown"` 的 trace evidence。必要时补足对 acquisition memory、verified hosts、persisted traces 的断言，但不要把 social-post markdown、generic fetch/extract 或更多 family 混入这条 proof bar。</action>
  <acceptance_criteria>
- `rg -n "proves web markdown INSTALL_REQUIRED -> install -> rerun -> cross-host reuse" tests/integration/broker-flow.test.ts` 有命中。
- `rg -n "semanticMatchReason: \\\"direct_route\\\"|semanticMatchCandidateId: \\\"web-content-to-markdown\\\"|semanticMatchProofFamily: \\\"web_content_to_markdown\\\"" tests/integration/broker-flow.test.ts` 有命中。
- `npm test -- --run tests/integration/broker-flow.test.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/integration/broker-flow.test.ts</automated>
  </verify>
  <done>web markdown 拥有和 QA 相同形状的 canonical proof loop，可被 operator 和后续 phase 直接引用为第二条 proven family 证据。</done>
</task>

<task type="auto">
  <name>Task 2: 镜像 one hero lane, two proven loops 的 operator truth</name>
  <files>src/hosts/skill-markdown.ts, tests/hosts/host-shell-install.test.ts, README.md, README.zh-CN.md, TODOS.md, STATUS.md</files>
  <read_first>
.planning/phases/02-generalize-family-proofs/02-CONTEXT.md
.planning/phases/02-generalize-family-proofs/02-PATTERNS.md
src/hosts/skill-markdown.ts
tests/hosts/host-shell-install.test.ts
README.md
README.zh-CN.md
TODOS.md
STATUS.md
  </read_first>
  <action>按 D-11、D-12 统一 repo docs、installed shell copy、status/backlog truth 的层级表述。`src/hosts/skill-markdown.ts` 和 `tests/hosts/host-shell-install.test.ts` 保持 `### Hero lane: website QA` 在前、`### Secondary maintained lanes` 在后，并在 secondary lane / examples 中允许 web markdown 作为第二条 proven family 出现，但不能把它写成并列 first move。`README.md` 与 `README.zh-CN.md` 保持现有 QA-first Quick Start 不变，同时明确写出 “the second proven family is **web markdown**” / “第二条已经证明 install / verify / reuse 的 family 是 **web markdown**”。`TODOS.md` 需要停止把 “Generalize maintained-family semantic contract after QA default-entry proof is hard” 继续当未完成 backlog 挂着；`STATUS.md` 需要新增与 Phase 2 shipped truth 对应的人类摘要和 status item，说明 family proofs + web markdown second-proven-loop 已经有真实证据，而不是纯文案宣称。</action>
  <acceptance_criteria>
- `rg -n "second proven family is \\*\\*web markdown\\*\\*|第二条已经证明 install / verify / reuse 的 family 是 \\*\\*web markdown\\*\\*" README.md README.zh-CN.md` 有命中。
- `rg -n "### Hero lane: website QA|### Secondary maintained lanes|web markdown" src/hosts/skill-markdown.ts tests/hosts/host-shell-install.test.ts` 有命中。
- `rg -n "Generalize maintained-family semantic contract|second proven family|web markdown" TODOS.md STATUS.md` 有命中。
- `npm test -- --run tests/hosts/host-shell-install.test.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/hosts/host-shell-install.test.ts</automated>
  </verify>
  <done>repo docs、installed shell、status board、backlog truth 全部讲同一个故事：website QA 是唯一 hero lane，web markdown 是第二条 proven family。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| integration proof -> operator docs | 如果 README / STATUS 宣称 second proven family，却没有 canonical integration proof，对外叙事会变成 overclaim |
| installed shell copy -> repo docs | 如果宿主壳和 README 对 default-entry hierarchy 说法不同，用户会被引导到不同 first move |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | R | `tests/integration/broker-flow.test.ts` | mitigate | 用同一句 web markdown request 锁 install_required -> rerun -> cross-host reuse 与 semantic trace evidence，防止 second-family claim 没有真实 proof。 |
| T-02-03-02 | I | `src/hosts/skill-markdown.ts`, `README*.md`, `TODOS.md`, `STATUS.md` | mitigate | 用 host-shell regression 与双语/多文档 wording 同步，防止 web markdown 被写成和 website QA 并列的 first move。 |
</threat_model>

<verification>
1. 运行 `npm test -- --run tests/integration/broker-flow.test.ts tests/hosts/host-shell-install.test.ts`
2. 运行 `rg -n "second proven family|第二条已经证明|### Hero lane: website QA|### Secondary maintained lanes|Generalize maintained-family semantic contract" README.md README.zh-CN.md src/hosts/skill-markdown.ts tests/hosts/host-shell-install.test.ts TODOS.md STATUS.md`
</verification>

<success_criteria>
- web markdown 的 canonical proof loop 被 integration tests 明确证明。
- repo docs、installed shell、STATUS、TODOS 都讲 “one hero lane, two proven loops”。
- website QA 仍然是唯一 first move，web markdown 只作为第二条 proven family 进入 operator truth。
</success_criteria>

<output>
After completion, create `.planning/phases/02-generalize-family-proofs/02-generalize-family-proofs-03-SUMMARY.md`
</output>
