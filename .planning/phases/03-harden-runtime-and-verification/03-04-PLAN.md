---
phase: 03-harden-runtime-and-verification
plan: 04
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
  - "03"
files_modified:
  - package.json
  - scripts/verify-local.mjs
  - src/dev/local-verification.ts
  - tests/dev/local-verification.test.ts
  - tests/helpers/npm.ts
  - README.md
  - README.zh-CN.md
autonomous: true
requirements:
  - HARD-04
must_haves:
  truths:
    - "Contributor verification must have a deterministic preflight path before the main suite starts; broken dependencies cannot keep failing as opaque Rollup/Vitest crashes."
    - "The canonical local verification baseline matches CI: Node 22 + `npm ci` + `npm run build` + `npm test`."
    - "Repair guidance must be repo-owned and reproducible, not an ad hoc `npm_execpath` shim or tribal knowledge."
  artifacts:
    - path: "package.json"
      provides: "canonical local verification entrypoint"
    - path: "scripts/verify-local.mjs"
      provides: "repo-owned preflight / repair guidance wrapper"
    - path: "src/dev/local-verification.ts"
      provides: "typed dependency-health and repair-step logic"
    - path: "tests/dev/local-verification.test.ts"
      provides: "deterministic preflight and repair-hint regression proof"
    - path: "tests/helpers/npm.ts"
      provides: "test/runtime npm CLI path resolution compatibility"
    - path: "README.md"
      provides: "English contributor verification guidance aligned with CI"
    - path: "README.zh-CN.md"
      provides: "Chinese contributor verification guidance aligned with CI"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "package.json"
      via: "local verification entrypoint must mirror Node 22 + npm ci + build + test baseline"
      pattern: "node-version: 22|npm ci|npm run build|npm test"
    - from: "scripts/verify-local.mjs"
      to: "src/dev/local-verification.ts"
      via: "CLI wrapper should expose preflight-only and repair-hint modes on top of typed health logic"
      pattern: "check-only|repair"
    - from: "src/dev/local-verification.ts"
      to: "tests/dev/local-verification.test.ts"
      via: "Rollup/Vitest optional dependency failure must produce deterministic repair steps"
      pattern: "rollup|vitest|npm ci|Node 22"
---

<objective>
为本地贡献者提供一个 deterministic preflight + repair path，让验证链路先检查 Node/npm/Rollup/Vitest 健康，再决定是否运行 `build` / `test`，并把修复指引与 CI baseline 对齐。

Purpose: 满足 `HARD-04`，降低 “本地环境先在 Rollup optional native dependency 上炸掉，连测试都起不来” 的协作摩擦。
Output: repo-owned local verification preflight、repair guidance、README 双语验证说明、对应 regression tests。
</objective>

<execution_context>
@/Users/monkeyin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/monkeyin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-harden-runtime-and-verification/03-CONTEXT.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
@package.json
@.github/workflows/ci.yml
@tests/helpers/npm.ts
@README.md
@README.zh-CN.md

<interfaces>
From tests/helpers/npm.ts:
```ts
export function resolveNpmCliPath(nodePath = process.execPath): string;
export function execNpm(args: string[], options?: ExecFileOptions);
```

Expected new interface:
```ts
export type LocalVerificationHealth = {
  nodeVersionOk: boolean;
  npmCliResolved: boolean;
  rollupNativeHealthy: boolean;
  vitestEntryHealthy: boolean;
  repairSteps: string[];
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 增加 repo-owned local verification preflight 与 repair guidance</name>
  <files>package.json, scripts/verify-local.mjs, src/dev/local-verification.ts, tests/dev/local-verification.test.ts</files>
  <read_first>
.planning/phases/03-harden-runtime-and-verification/03-CONTEXT.md
.planning/codebase/CONCERNS.md
.planning/codebase/TESTING.md
package.json
.github/workflows/ci.yml
  </read_first>
  <behavior>
    - Test 1: a healthy environment reports Node 22 / npm CLI / Rollup native / Vitest entry readiness before running build and test.
    - Test 2: a broken Rollup optional dependency state returns deterministic repair steps instead of a generic stack trace.
    - Test 3: contributors can run a preflight-only mode that diagnoses environment issues without starting the full suite.
  </behavior>
  <action>按 D-07、D-08 新增一个 repo-owned local verification path。把 health logic 放在 `src/dev/local-verification.ts`，并由 `scripts/verify-local.mjs` 暴露给开发者；`package.json` 里增加如 `verify:local` 的 canonical entrypoint。预检至少覆盖 Node major version 是否为 22、npm CLI 是否可解析、Vitest entry 是否可访问、Rollup optional native dependency 是否可加载，以及在失败时给出 deterministic repair steps（例如 clean reinstall / `npm ci` / rerun）。不要把这条能力塞进 shipped lifecycle CLI，也不要继续依赖临时 `npm_execpath` shim 或手工口口相传的修复方式。</action>
  <acceptance_criteria>
- `rg -n "\"verify:local\"|verify-local|check-only|Node 22|npm ci|rollup|vitest" package.json scripts/verify-local.mjs src/dev/local-verification.ts tests/dev/local-verification.test.ts` 有命中。
- `npm test -- --run tests/dev/local-verification.test.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/dev/local-verification.test.ts</automated>
  </verify>
  <done>仓库拥有自己的 local verification preflight 与 repair guidance，贡献者在 suite 启动前就能知道如何修复环境。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 对齐 npm path helper 与双语 contributor guidance</name>
  <files>tests/helpers/npm.ts, README.md, README.zh-CN.md</files>
  <read_first>
.planning/phases/03-harden-runtime-and-verification/03-CONTEXT.md
.planning/codebase/TESTING.md
tests/helpers/npm.ts
README.md
README.zh-CN.md
  </read_first>
  <behavior>
    - Test 1: test helpers continue to resolve npm CLI deterministically even when `npm` is not on PATH.
    - Test 2: README and README.zh-CN both document the same canonical local verification path and repair guidance.
    - Test 3: the docs explicitly point contributors to the new preflight entrypoint instead of assuming trial-and-error.
  </behavior>
  <action>把 `tests/helpers/npm.ts` 调整为与新的 repo-owned verification logic 对齐，确保测试和脚本对 npm CLI path 的推导不再分叉。同步更新 `README.md` 与 `README.zh-CN.md` 的 contributor / verification 部分，明确写出 Node 22 + `npm ci` + `npm run build` + `npm test` 的 canonical baseline，并指向新的 preflight/repair entrypoint。不要在双语文档里写出不同 repair steps，也不要继续把本地验证健康隐含在某几条零散命令里。</action>
  <acceptance_criteria>
- `rg -n "Node 22|npm ci|npm run build|npm test|verify:local|verify-local" README.md README.zh-CN.md tests/helpers/npm.ts` 有命中。
- `npm test -- --run tests/dev/local-verification.test.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/dev/local-verification.test.ts</automated>
  </verify>
  <done>测试 helper、repo-owned preflight、README 双语 guidance 都围绕同一条 CI-aligned verification baseline 工作。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor shell -> local verification entrypoint | 如果本地验证没有 deterministic preflight，贡献者会在测试启动前就被 opaque dependency failures 卡住 |
| CI baseline -> README guidance | 如果 README 与 CI baseline 不一致，开发者修环境会越来越依赖口口相传而不是 repo truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | D | `scripts/verify-local.mjs`, `src/dev/local-verification.ts` | mitigate | 在 suite 启动前检查 Node/npm/Rollup/Vitest 健康，并输出 deterministic repair steps。 |
| T-03-04-02 | I | `package.json`, `README.md`, `README.zh-CN.md`, `tests/helpers/npm.ts` | mitigate | 用 repo-owned verification entrypoint 和双语 guidance 对齐 CI baseline，禁止 ad hoc shim / tribal knowledge 漂移。 |
</threat_model>

<verification>
1. 运行 `rg -n "\"verify:local\"|verify-local|Node 22|npm ci|npm run build|npm test|rollup|vitest" package.json scripts/verify-local.mjs src/dev/local-verification.ts tests/dev/local-verification.test.ts README.md README.zh-CN.md tests/helpers/npm.ts`
2. 运行 `npm test -- --run tests/dev/local-verification.test.ts`
</verification>

<success_criteria>
- 本地验证链路先做 deterministic health check，再运行 build/test。
- Rollup/Vitest optional dependency failure 会给出 repo-owned repair guidance，而不是 opaque stack trace。
- README 双语与 CI baseline 对齐，并指向统一的 verification entrypoint。
</success_criteria>

<output>
After completion, create `.planning/phases/03-harden-runtime-and-verification/03-harden-runtime-and-verification-04-SUMMARY.md`
</output>
