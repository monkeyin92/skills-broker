---
phase: 04-lock-operator-truth-and-expansion-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/operator-truth.ts
  - tests/shared-home/operator-truth-parity.test.ts
  - README.md
  - README.zh-CN.md
  - TODOS.md
  - STATUS.md
autonomous: true
requirements:
  - HARD-03
must_haves:
  truths:
    - "README.md、README.zh-CN.md、TODOS.md、STATUS.md 必须围绕同一套 operator truth 工作，而不是靠人工同步。"
    - "当前支持矩阵继续只写 Claude Code 和 Codex；OpenCode 只能以 deferred-but-planned 的身份出现。"
    - "published lifecycle wording 必须显式收口到 `npx skills-broker update`、`npx skills-broker doctor`、`npx skills-broker remove`。"
  artifacts:
    - path: "src/core/operator-truth.ts"
      provides: "repo-owned canonical operator truth contract"
    - path: "tests/shared-home/operator-truth-parity.test.ts"
      provides: "cross-surface parity proof for README / README.zh-CN / TODOS / STATUS"
    - path: "README.md"
      provides: "English operator-facing truth surface"
    - path: "README.zh-CN.md"
      provides: "Chinese operator-facing truth surface"
    - path: "TODOS.md"
      provides: "backlog truth for deferred OpenCode and shipped packets"
    - path: "STATUS.md"
      provides: "repo-native shipped truth contract"
  key_links:
    - from: "src/core/operator-truth.ts"
      to: "tests/shared-home/operator-truth-parity.test.ts"
      via: "one canonical contract should drive cross-surface parity assertions"
      pattern: "supportedHosts|deferredHosts|heroLane|secondProvenFamily|lifecycleCommands"
    - from: "src/core/operator-truth.ts"
      to: "README.md"
      via: "English docs must mirror the same support matrix, hero lane, second proven family, and lifecycle command wording"
      pattern: "Claude Code|Codex|OpenCode|website QA|web markdown|npx skills-broker update"
    - from: "src/core/operator-truth.ts"
      to: "README.zh-CN.md"
      via: "Chinese docs must mirror the same support matrix, hero lane, second proven family, and lifecycle command wording"
      pattern: "Claude Code|Codex|OpenCode|website QA|web markdown|npx skills-broker update"
---

<objective>
把 repo 内最重要的 operator-facing truth surface 收口成一个可测试的 canonical contract，并用 parity tests 把 README / README.zh-CN / TODOS / STATUS 的 support matrix、default-entry hierarchy 与 lifecycle wording 锁成同一件事。

Purpose: 满足 `HARD-03` 的“多文档 truth 不允许漂移”核心要求。
Output: repo-owned operator truth contract、cross-surface parity tests、对齐后的 repo docs/status/backlog wording。
</objective>

<execution_context>
@/Users/monkeyin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/monkeyin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-lock-operator-truth-and-expansion-readiness/04-CONTEXT.md
@.planning/phases/03-harden-runtime-and-verification/03-VERIFICATION.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@README.md
@README.zh-CN.md
@TODOS.md
@STATUS.md
@tests/shared-home/status.test.ts

<interfaces>
Expected new interface:
```ts
export type OperatorTruthContract = {
  supportedHosts: readonly string[];
  deferredHosts: readonly string[];
  heroLane: "website QA";
  secondProvenFamily: "web markdown";
  lifecycleCommands: readonly string[];
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 repo-owned operator truth contract 与 parity harness</name>
  <files>src/core/operator-truth.ts, tests/shared-home/operator-truth-parity.test.ts</files>
  <read_first>
.planning/phases/04-lock-operator-truth-and-expansion-readiness/04-CONTEXT.md
.planning/codebase/CONCERNS.md
.planning/codebase/CONVENTIONS.md
README.md
README.zh-CN.md
TODOS.md
STATUS.md
tests/shared-home/status.test.ts
  </read_first>
  <behavior>
    - Test 1: one typed contract exposes supported hosts, deferred hosts, hero lane, second proven family, and canonical lifecycle commands.
    - Test 2: parity tests read the real markdown surfaces and fail closed when one surface overclaims or drifts.
    - Test 3: the harness is repo-owned and deterministic, not a loose grep script outside the test suite.
  </behavior>
  <action>按 D-01、D-02、D-03 新增一个 repo-owned operator truth contract（建议落在 `src/core/operator-truth.ts` 或等价可复用位置），把当前支持矩阵、deferred host、website QA hero lane、web markdown second proven family、以及 canonical lifecycle command wording 收成 typed source of truth。随后在 `tests/shared-home/operator-truth-parity.test.ts` 里读取 `README.md`、`README.zh-CN.md`、`TODOS.md`、`STATUS.md` 的真实文本或 canonical block，验证这些表面与 contract 对齐；不要把 parity 退回到 shell script grep，也不要只验证英文面而放过中文面。</action>
  <acceptance_criteria>
- `rg -n "supportedHosts|deferredHosts|heroLane|secondProvenFamily|lifecycleCommands" src/core/operator-truth.ts tests/shared-home/operator-truth-parity.test.ts` 有命中。
- `npm test -- --run tests/shared-home/operator-truth-parity.test.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/shared-home/operator-truth-parity.test.ts</automated>
  </verify>
  <done>repo 有了可复用、可测试的 canonical operator truth contract，不再只靠文档作者手动记忆同步。</done>
</task>

<task type="auto">
  <name>Task 2: 对齐 README / README.zh-CN / TODOS / STATUS 的 operator truth</name>
  <files>README.md, README.zh-CN.md, TODOS.md, STATUS.md</files>
  <read_first>
.planning/phases/04-lock-operator-truth-and-expansion-readiness/04-CONTEXT.md
src/core/operator-truth.ts
tests/shared-home/operator-truth-parity.test.ts
README.md
README.zh-CN.md
TODOS.md
STATUS.md
  </read_first>
  <action>按 D-01、D-02、D-03 同步四个 operator-facing truth surface。四个表面都必须诚实地表达：当前支持 Claude Code 和 Codex；OpenCode 仍 deferred but planned；website QA 是 hero lane；web markdown 是 second proven family；published lifecycle path 用 `npx skills-broker update` / `doctor` / `remove` 表达。`STATUS.md` 的 human summary 与 canonical JSON block 都要保持这个 truth；`TODOS.md` 要继续把 OpenCode 作为 deferred expansion，而不是含糊成“快支持了”；不要把 Phase 4 扩成重写整本 README，也不要让中文文案退化成英文 truth 的宽松近似。</action>
  <acceptance_criteria>
- `rg -n "Supported now: Claude Code, Codex|Deferred but planned: OpenCode thin host shell|website QA|web markdown|npx skills-broker update|npx skills-broker doctor|npx skills-broker remove" README.md README.zh-CN.md TODOS.md STATUS.md` 有命中。
- `npm test -- --run tests/shared-home/operator-truth-parity.test.ts tests/shared-home/status.test.ts` 通过。
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run tests/shared-home/operator-truth-parity.test.ts tests/shared-home/status.test.ts</automated>
  </verify>
  <done>repo docs、backlog、status board 讲的是同一套 operator truth，而不是多个近似版本。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical truth -> repo docs | 如果没有 repo-owned contract，README / README.zh-CN / TODOS / STATUS 会各自漂移 |
| support matrix -> operator expectations | 如果 OpenCode 被写成“快支持了”或支持矩阵写法不一致，operator 会误判当前产品边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | I | `README*.md`, `TODOS.md`, `STATUS.md` | mitigate | 用 repo-owned operator truth contract 和 parity tests 锁住 support matrix、hero lane、lifecycle wording。 |
| T-04-01-02 | R | `tests/shared-home/operator-truth-parity.test.ts` | mitigate | 读取真实文档与 canonical status block，防止“代码里有常量、产品面却没同步”的假安全。 |
</threat_model>

<verification>
1. 运行 `rg -n "supportedHosts|deferredHosts|heroLane|secondProvenFamily|lifecycleCommands" src/core/operator-truth.ts tests/shared-home/operator-truth-parity.test.ts`
2. 运行 `rg -n "Claude Code, Codex|OpenCode|website QA|web markdown|npx skills-broker update|npx skills-broker doctor|npx skills-broker remove" README.md README.zh-CN.md TODOS.md STATUS.md`
3. 运行 `npm test -- --run tests/shared-home/operator-truth-parity.test.ts tests/shared-home/status.test.ts`
</verification>

<success_criteria>
- repo 有一份明确、typed、可复用的 canonical operator truth contract。
- README / README.zh-CN / TODOS / STATUS 对 support matrix、default-entry hierarchy、lifecycle wording 的叙事一致。
- doc parity drift 会被 repo-native tests fail closed。
</success_criteria>

<output>
After completion, create `.planning/phases/04-lock-operator-truth-and-expansion-readiness/04-lock-operator-truth-and-expansion-readiness-01-SUMMARY.md`
</output>
