---
phase: 18-encode-qa-first-family-hierarchy
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01-PLAN.md
files_modified:
  - tests/shared-home/operator-truth-parity.test.ts
  - tests/hosts/host-shell-install.test.ts
  - tests/e2e/shared-home-smoke.test.ts
  - tests/shared-home/doctor.test.ts
  - tests/cli/lifecycle-cli.test.ts
  - src/dev/ci-trust.ts
autonomous: true
requirements:
  - LOOP-01
  - LOOP-02
  - LOOP-03
must_haves:
  truths:
    - "QA-first family hierarchy 的漂移必须被 parity / host-shell / doctor / CI rails 抓住。"
    - "guardrails 继续复用现有 operator-truth parity 与 narrative CI trust，不新增平行 checker。"
    - "generated shell、docs、doctor text 讲的 hierarchy / next-loop truth 必须一起 fail closed。"
  artifacts:
    - path: "tests/shared-home/operator-truth-parity.test.ts"
      provides: "cross-surface hierarchy wording parity coverage"
    - path: "tests/shared-home/doctor.test.ts"
      provides: "doctor hierarchy / next-loop guidance coverage"
    - path: "tests/hosts/host-shell-install.test.ts"
      provides: "generated host shell hierarchy coverage"
    - path: "tests/e2e/shared-home-smoke.test.ts"
      provides: "published shared-home shell wording parity"
    - path: "src/dev/ci-trust.ts"
      provides: "CI snippets covering the v1.5 hierarchy contract"
---

<objective>
把 QA-first family hierarchy 收成 fail-closed 的 repo-native guardrail，而不是继续靠人工 review 保持文案一致。

Purpose: 让 `LOOP-01`、`LOOP-02`、`LOOP-03` 的 hierarchy / next-loop story 在 docs、host shell、doctor 与 CI trust rail 上一起爆红或一起通过。
Output: updated parity, doctor, host-shell, and CI trust checks for the Phase 18 wording contract.
</objective>

<context>
@.planning/phases/18-encode-qa-first-family-hierarchy/18-CONTEXT.md
@.planning/phases/18-encode-qa-first-family-hierarchy/18-01-PLAN.md
@tests/shared-home/operator-truth-parity.test.ts
@tests/shared-home/doctor.test.ts
@tests/hosts/host-shell-install.test.ts
@tests/e2e/shared-home-smoke.test.ts
@src/dev/ci-trust.ts
@src/core/operator-truth.ts
@src/shared-home/format.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 parity / doctor / host-shell tests 去锁 hierarchy contract</name>
  <files>tests/shared-home/operator-truth-parity.test.ts, tests/shared-home/doctor.test.ts, tests/hosts/host-shell-install.test.ts, tests/e2e/shared-home-smoke.test.ts, tests/cli/lifecycle-cli.test.ts</files>
  <behavior>
    - Test 1: README、README.zh-CN、STATUS、TODOS 都必须包含新的 QA-first hierarchy / next-loop canonical snippets。
    - Test 2: `doctor` text output 会暴露 family hierarchy / next-loop guidance。
    - Test 3: generated host shell 与 shared-home smoke 继续复用同一份 hierarchy contract。
  </behavior>
  <action>更新 parity 与 doctor/host-shell assertions，把 current QA-first family-loop hierarchy 变成 repo-owned test contract。</action>
  <acceptance_criteria>
- docs/status/todos drift 会在 parity suite 爆红。
- doctor / host shell drift 会在对应 tests 爆红。
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 更新 CI trust snippets 以覆盖新的 hierarchy wording</name>
  <files>src/dev/ci-trust.ts</files>
  <behavior>
    - Test 1: narrative parity CI rail 要求 docs/status/todos 包含新的 QA-first hierarchy / next-loop snippets。
    - Test 2: CI trust 不会只验证 hero lane / lifecycle / adoption packet，而忽略 v1.5 的 family-loop story。
  </behavior>
  <action>扩展现有 narrative/CI trust snippets，把 hierarchy 和 next-loop guidance 纳入同一条 repo-native guardrail。</action>
  <acceptance_criteria>
- `test:ci:narrative-parity` 能抓到 v1.5 hierarchy drift。
- 没有新增平行 checker。
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. 运行 `node ./node_modules/vitest/vitest.mjs --run tests/shared-home/operator-truth-parity.test.ts tests/shared-home/doctor.test.ts tests/hosts/host-shell-install.test.ts tests/cli/lifecycle-cli.test.ts tests/e2e/shared-home-smoke.test.ts`
2. 运行 `node ./node_modules/typescript/bin/tsc -p tsconfig.build.json --noEmit`
</verification>

<output>
After completion, create `.planning/phases/18-encode-qa-first-family-hierarchy/18-encode-qa-first-family-hierarchy-02-SUMMARY.md`
</output>
