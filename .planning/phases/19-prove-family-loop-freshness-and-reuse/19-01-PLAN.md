---
phase: 19-prove-family-loop-freshness-and-reuse
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared-home/doctor.ts
  - src/shared-home/format.ts
  - tests/shared-home/doctor.test.ts
autonomous: true
requirements:
  - FRESH-01
  - FRESH-02
must_haves:
  truths:
    - "`doctor` 必须能直接显示 `website QA`、`web markdown`、`social markdown` 三段 family loop 的 freshness。"
    - "freshness / next action 必须继续保住 QA-first sequence，而不是把三条 family 打平成并列入口。"
    - "现有 `familyProofs` 结构不能为了 freshness 语义被粗暴扩坏。"
  artifacts:
    - path: "src/shared-home/doctor.ts"
      provides: "top-level family-loop freshness/reuse packet for structured shared-home surfaces"
    - path: "src/shared-home/format.ts"
      provides: "doctor text lines for family-loop freshness, latest activity, host coverage, and next action"
    - path: "tests/shared-home/doctor.test.ts"
      provides: "phase-19 regression coverage for stale/missing/active family-loop signals"
---

<objective>
让 `doctor` 从“只知道 website QA adoption”升级到“能直接读三段 QA-first family loop 哪段 active/stale/missing、最新 activity 在哪、下一步要刷新什么”。

Purpose: 满足 `FRESH-01` 与 `FRESH-02`，把 family-loop freshness / refresh guidance 收成 repo-owned doctor surface。
Output: structured family-loop signal packet plus aligned text rendering and focused regression tests.
</objective>

<context>
@.planning/phases/19-prove-family-loop-freshness-and-reuse/19-CONTEXT.md
@src/shared-home/doctor.ts
@src/shared-home/format.ts
@tests/shared-home/doctor.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 为三段 QA-first family loop 增加 freshness/reuse signal packet</name>
  <files>src/shared-home/doctor.ts</files>
  <behavior>
    - Test 1: `doctor --json` 除 `websiteQaAdoption` 外，还能直接读到 website QA / web markdown / social markdown 三段 freshness status、latest activity、reuse state 与 next action。
    - Test 2: post-QA family 的 next action 会保留 `website QA -> web markdown -> social markdown` 顺序约束。
    - Test 3: 不修改 `familyProofs` 的既有结构与 phase/verdict 语义。
  </behavior>
  <action>在 `doctor.ts` 里新增顶层 family-loop signal 汇总，website QA 可复用 hero-lane adoption freshness，web/social 则基于 acquisition memory 与 verified downstream manifests 计算 active/stale/missing 与 host coverage。</action>
  <acceptance_criteria>
- 三段 family loop 都有统一 freshness/reuse surface 可读。
- website QA 继续保留 hero-lane 特权，不被新 packet 取代。
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 把 family-loop freshness 与 refresh guidance 输出到 doctor text surface</name>
  <files>src/shared-home/format.ts, tests/shared-home/doctor.test.ts</files>
  <behavior>
    - Test 1: text output 直接显示整体 QA-first family freshness summary，以及 web/social 的 latest activity、host coverage 与 next action。
    - Test 2: stale/missing family 的 text guidance 会明确指出该先刷新哪一段 loop。
  </behavior>
  <action>给 `format.ts` 增加 family-loop freshness summary 与 post-QA family detail lines，并用 doctor tests 锁住输出顺序与关键 wording。</action>
  <acceptance_criteria>
- maintainer 不需要手工拼 `familyProofs` + manifests + acquisition memory 才知道哪段 stale。
- text output 仍保持可扫描，不把 hero-lane adoption lines 淹没。
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. 运行 `node ./node_modules/vitest/vitest.mjs --run tests/shared-home/doctor.test.ts`
2. 运行 `node ./node_modules/typescript/bin/tsc -p tsconfig.build.json --noEmit`
</verification>

<output>
After completion, create `.planning/phases/19-prove-family-loop-freshness-and-reuse/19-prove-family-loop-freshness-and-reuse-01-SUMMARY.md`
</output>
