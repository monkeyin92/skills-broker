---
phase: 19-prove-family-loop-freshness-and-reuse
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01-PLAN.md
files_modified:
  - tests/shared-home/doctor.test.ts
  - tests/e2e/shared-home-smoke.test.ts
autonomous: true
requirements:
  - FRESH-03
must_haves:
  truths:
    - "post-QA family loop 的 reuse state 必须能在 Claude Code、Codex、OpenCode 三宿主 surface 上直接读出。"
    - "shared-home proof surface 不能因为扩展 web/social freshness 而把 website QA 的第一步地位打平。"
    - "family-loop freshness / reuse drift 必须落进 repo-owned fixtures 或 smoke tests，而不是留给人工读 trace。"
  artifacts:
    - path: "tests/shared-home/doctor.test.ts"
      provides: "cross-host family-loop freshness/reuse fixture coverage"
    - path: "tests/e2e/shared-home-smoke.test.ts"
      provides: "published-style shared-home smoke for post-QA family reuse visibility"
---

<objective>
证明 post-QA family loop 的 freshness / reuse 在三宿主 shared-home surface 上仍然可读、可复用，并且回归时会 fail closed。

Purpose: 满足 `FRESH-03`，把 cross-host family-loop reuse visibility 锁成 repo-owned proof rail。
Output: strengthened doctor/e2e coverage for multi-host post-QA family-loop signals.
</objective>

<context>
@.planning/phases/19-prove-family-loop-freshness-and-reuse/19-CONTEXT.md
@.planning/phases/19-prove-family-loop-freshness-and-reuse/19-01-PLAN.md
@tests/shared-home/doctor.test.ts
@tests/e2e/shared-home-smoke.test.ts
@src/shared-home/doctor.ts
@src/shared-home/format.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 用 fixtures 证明 post-QA family freshness/reuse 的 stale→fresh 与 cross-host coverage</name>
  <files>tests/shared-home/doctor.test.ts</files>
  <behavior>
    - Test 1: website QA active 时，web/social family 会基于最新 verify / reuse / replay activity 区分 active、stale、missing。
    - Test 2: web/social family 的 host coverage 与 next action 会在跨 Claude Code、Codex、OpenCode 场景下保持可读。
    - Test 3: `social markdown` 在 `web markdown` 未 fresh 时不会越级成为推荐动作。
  </behavior>
  <action>扩展 family proof fixtures 断言，让 doctor tests 直接覆盖 post-QA family freshness 与 cross-host reuse semantics。</action>
  <acceptance_criteria>
- stale / missing / active family-loop branches 都有 fixture 证明。
- next-action hierarchy drift 会直接爆红。
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在 shared-home smoke 中锁住 published-style family-loop surface</name>
  <files>tests/e2e/shared-home-smoke.test.ts</files>
  <behavior>
    - Test 1: shared-home smoke 能读到 post-QA family freshness/reuse packet，而不只剩 `familyProofs`。
    - Test 2: OpenCode / Claude Code / Codex 的 reuse visibility 不会因 surface 漂移而丢失。
  </behavior>
  <action>补 shared-home smoke assertions，确保 published-style doctor surface 继续暴露 post-QA family-loop freshness/reuse truth。</action>
  <acceptance_criteria>
- 三宿主 surface 的 post-QA family visibility 被 smoke 覆盖。
- 这一轮不需要新增平行 smoke harness。
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. 运行 `node ./node_modules/vitest/vitest.mjs --run tests/shared-home/doctor.test.ts tests/e2e/shared-home-smoke.test.ts`
2. 运行 `node ./node_modules/typescript/bin/tsc -p tsconfig.build.json --noEmit`
</verification>

<output>
After completion, create `.planning/phases/19-prove-family-loop-freshness-and-reuse/19-prove-family-loop-freshness-and-reuse-02-SUMMARY.md`
</output>
