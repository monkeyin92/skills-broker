---
phase: 20-lock-family-loop-truth-rails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/operator-truth.ts
  - src/shared-home/format.ts
  - src/hosts/skill-markdown.ts
  - README.md
  - README.zh-CN.md
  - STATUS.md
  - TODOS.md
autonomous: true
requirements:
  - TRUST-03
must_haves:
  truths:
    - "README、README.zh-CN、installed host shell、STATUS、TODOS 与 doctor 必须共享同一份 QA-first family-loop packet wording。"
    - "website QA adoption packet 继续保留，但不能成为唯一 operator-visible freshness story。"
    - "STATUS canonical board 必须把 v1.5 的 hierarchy / freshness / truth-rail phases 记录成 repo-owned audit truth。"
  artifacts:
    - path: "src/core/operator-truth.ts"
      provides: "canonical QA-first family-loop packet wording"
    - path: "src/shared-home/format.ts"
      provides: "doctor text output reusing the canonical family-loop packet snippet"
    - path: "src/hosts/skill-markdown.ts"
      provides: "installed shell wording aligned with the family-loop packet"
    - path: "README.md"
      provides: "english family-loop packet truth"
    - path: "README.zh-CN.md"
      provides: "chinese family-loop packet truth"
    - path: "STATUS.md"
      provides: "canonical v1.5 family-loop status items plus aligned narrative"
    - path: "TODOS.md"
      provides: "current-focus wording aligned to phase-20 truth rails"
---

<objective>
把 Phase 19 的 family-loop freshness/reuse truth 升级成一条跨 docs、doctor、installed shell、STATUS 与 TODOS 复用的 canonical operator contract。

Purpose: 满足 `TRUST-03`，让 maintainer 从任何 repo-owned surface 看到的都是同一份 QA-first family-loop packet 叙事。
Output: updated operator-truth contract, aligned docs/shell/status surfaces, and canonical v1.5 status items.
</objective>

<context>
@.planning/phases/20-lock-family-loop-truth-rails/20-CONTEXT.md
@src/core/operator-truth.ts
@src/shared-home/format.ts
@src/hosts/skill-markdown.ts
@README.md
@README.zh-CN.md
@STATUS.md
@TODOS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 提升 family-loop freshness/reuse wording 为 canonical contract</name>
  <files>src/core/operator-truth.ts, src/shared-home/format.ts, src/hosts/skill-markdown.ts</files>
  <behavior>
    - Test 1: operator-truth contract 除 hierarchy / next-loop 外，还导出 family-loop packet wording。
    - Test 2: doctor text 与 installed host shell 都复用这条 wording，而不是各自描述 family-loop freshness。
  </behavior>
  <action>在 operator-truth 里新增 family-loop packet snippet，并接入 doctor text 与 generated shell copy。</action>
  <acceptance_criteria>
- hierarchy、next-loop、family-loop packet 三条 truth 都有 canonical source。
- doctor / shell wording 不再与 README / STATUS 分叉。
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 对齐 README / STATUS / TODOS narrative，并把 v1.5 phases 写进 canonical board</name>
  <files>README.md, README.zh-CN.md, STATUS.md, TODOS.md</files>
  <behavior>
    - Test 1: README 双语、STATUS、TODOS 都包含新的 family-loop packet wording。
    - Test 2: STATUS canonical block 记录 v1.5 phase18/19/20 的 hierarchy / freshness / truth-rail items。
  </behavior>
  <action>更新 repo docs/status/backlog narrative，让它们讲同一份 QA-first family-loop packet story，并补齐 v1.5 canonical status items。</action>
  <acceptance_criteria>
- operator 不需要从多个 surface 手工拼 phase18/19 真相。
- STATUS canonical board 可直接被后续 audit 与 CI 复用。
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. 运行 `node ./node_modules/vitest/vitest.mjs --run tests/shared-home/operator-truth-parity.test.ts tests/hosts/host-shell-install.test.ts tests/shared-home/doctor.test.ts`
2. 运行 `node ./node_modules/typescript/bin/tsc -p tsconfig.build.json --noEmit`
</verification>

<output>
After completion, create `.planning/phases/20-lock-family-loop-truth-rails/20-lock-family-loop-truth-rails-01-SUMMARY.md`
</output>
