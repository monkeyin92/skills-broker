---
phase: 20-lock-family-loop-truth-rails
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01-PLAN.md
files_modified:
  - tests/shared-home/operator-truth-parity.test.ts
  - tests/hosts/host-shell-install.test.ts
  - tests/shared-home/doctor.test.ts
  - src/dev/ci-trust.ts
  - tests/dev/ci-trust.test.ts
autonomous: true
requirements:
  - TRUST-04
must_haves:
  truths:
    - "family-loop hierarchy、freshness semantics 与 next-step guidance 的 drift 必须在 parity / host-shell / doctor / CI rails 上 fail closed。"
    - "guardrails 继续复用现有 parity 和 ci-trust，不新增平行 checker。"
    - "repo-proof surfaces 必须从 STATUS canonical items 与 doctor tests 直接看到 v1.5 family-loop truth。"
  artifacts:
    - path: "tests/shared-home/operator-truth-parity.test.ts"
      provides: "cross-surface parity for family-loop packet snippets and v1.5 status items"
    - path: "tests/hosts/host-shell-install.test.ts"
      provides: "installed-shell guardrail for family-loop packet wording"
    - path: "tests/shared-home/doctor.test.ts"
      provides: "doctor text guardrail for canonical family-loop packet wording"
    - path: "src/dev/ci-trust.ts"
      provides: "CI trust surfaces updated for v1.5 family-loop contract"
    - path: "tests/dev/ci-trust.test.ts"
      provides: "live repo passes the expanded trust rail"
---

<objective>
把 QA-first family-loop truth 变成 fail-closed guardrail，而不是继续靠人工 review 维持。

Purpose: 满足 `TRUST-04`，让 docs/status/doctor/installed-shell/CI 对 v1.5 family-loop contract 一起通过或一起爆红。
Output: expanded parity, doctor, host-shell, and CI trust rails for the Phase 20 contract.
</objective>

<context>
@.planning/phases/20-lock-family-loop-truth-rails/20-CONTEXT.md
@.planning/phases/20-lock-family-loop-truth-rails/20-01-PLAN.md
@tests/shared-home/operator-truth-parity.test.ts
@tests/hosts/host-shell-install.test.ts
@tests/shared-home/doctor.test.ts
@src/dev/ci-trust.ts
@tests/dev/ci-trust.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展 parity / shell / doctor tests 去锁 family-loop packet contract</name>
  <files>tests/shared-home/operator-truth-parity.test.ts, tests/hosts/host-shell-install.test.ts, tests/shared-home/doctor.test.ts</files>
  <behavior>
    - Test 1: docs/status/todos 都必须包含新的 family-loop packet snippet。
    - Test 2: installed shell 与 doctor text 也必须包含同一条 snippet。
    - Test 3: STATUS canonical board 缺少 v1.5 phase18/19/20 item 时，parity suite 直接爆红。
  </behavior>
  <action>扩展 parity / host-shell / doctor assertions，把 family-loop packet 和 v1.5 status items 变成 repo-owned test contract。</action>
  <acceptance_criteria>
- family-loop wording drift 在对应 suites 里可见。
- v1.5 canonical status item drift 也会直接爆红。
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 更新 CI trust surface specs 以覆盖 v1.5 family-loop contract</name>
  <files>src/dev/ci-trust.ts, tests/dev/ci-trust.test.ts</files>
  <behavior>
    - Test 1: narrative CI rail 会要求 docs/status/todos 包含 family-loop packet snippet。
    - Test 2: repo-proof CI rail 会要求 STATUS canonical items 与 doctor tests 覆盖 phase18/19/20 family-loop truth。
  </behavior>
  <action>扩展现有 `ci-trust` surface specs，把 family-loop packet snippets、v1.5 status items 与 doctor coverage 纳入 live repo gate。</action>
  <acceptance_criteria>
- `test:ci:narrative-parity` 与 live repo trust report 都覆盖 v1.5 family-loop truth。
- 没有新增独立 CI checker。
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. 运行 `node ./node_modules/vitest/vitest.mjs --run tests/shared-home/operator-truth-parity.test.ts tests/hosts/host-shell-install.test.ts tests/shared-home/doctor.test.ts tests/dev/ci-trust.test.ts`
2. 运行 `node ./node_modules/typescript/bin/tsc -p tsconfig.build.json --noEmit`
</verification>

<output>
After completion, create `.planning/phases/20-lock-family-loop-truth-rails/20-lock-family-loop-truth-rails-02-SUMMARY.md`
</output>
