{
  "name": "skills-broker",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "skills-broker": "./dist/bin/skills-broker.js"
  },
  "files": [
    "dist",
    "README.md",
    "README.zh-CN.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node -e \"const fs=require('fs');const file='dist/bin/skills-broker.js';const content=fs.readFileSync(file,'utf8');if(!content.startsWith('#!'))fs.writeFileSync(file,'#!/usr/bin/env node\\n'+content);\"",
    "test": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.5.3",
    "ts-node": "^10.9.1",
    "typescript": "^5.5.2",
    "vitest": "^1.5.7"
  }
}
